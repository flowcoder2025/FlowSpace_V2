# FlowSpace Fix Plan

> WI 워크리스트. 상태값: `READY | ACTIVE | VERIFY | BLOCKED | DONE | DROPPED`. 한 번에 하나만 `ACTIVE`.
> WI ID/타입은 wi-global enum 준수: feat·fix·docs·style·refactor·test·chore·perf·ci·revert.
> 초기 시드 = 2026-06-19 듀얼 블라인드 검증(codex CLI + Claude) 확정 결함.

## Active WI
- (없음) — **✅ WI-031-feat (어드민 대시보드 CSV 내보내기: 멤버/로그/analytics) develop 머지 완료(2026-06-23, PR#32 merge `6bdee98`)**. 머지 후 develop tsc0/vitest364 재확인. 사용자 "대시보드 완성" 선택 범위 **2/2 완료(마지막)**. 설계 codex consult 2R. 신규 순수 `src/lib/csv.ts`(RFC4180 이스케이프 + 수식 인젝션 중화[선두 `= + - @` tab/CR/LF → `'` prefix, neutralize→quote 순서] + `CSV_BOM`) + `src/components/dashboard/csv-export.ts`(매퍼 membersToCsv/logsToCsv/analyticsToCsv/csvFilename + downloadCsv[try/finally]) + 공용 `export-csv-button.tsx` + 3페이지 배선(로그=로드분량만·라벨에 건수 / 멤버=visibleMembers / analytics=두 시계열 날짜 union 머지·결측0·오름차순). 듀얼 블라인드 **3R 수렴 codex PASS(r1 WARNING fixNow P2×2→r2 FAIL[선두 LF]→r3 PASS)·evaluator WARNING 9.85**(P0/P1 0, fixNow 0). **양 검증자 선두 LF 비대칭(`\t`/`\r`은 있고 `\n` 누락) 독립 수렴→r3 즉시 해소**(WI-011 교훈). payload Details raw=accepted-risk defer(CSV는 admin 전용 화면 Details[event-log-table가 이미 JSON.stringify 렌더] 미러링·operational ID·비밀 아님·신규 노출 아님, consult r2 합의). 게이트 4/4(tsc0/lint0err/vitest 333→364[+31]/build0). **WI-029~031 develop만 — main 미승격(승격은 사용자 승인 게이트).** 후속: payload 계약 의존 위험(향후 event type이 PII 추가 시 화면+CSV 동시 노출)→WI-032 후보.
- (직전) **✅ WI-030-feat (어드민 대시보드 고급 필터: 메시지/로그 날짜·타입) develop 머지 완료(2026-06-23, PR#31 merge `a41e2a8`)**. 머지 후 develop tsc0/vitest333 재확인. 사용자 "대시보드 완성" 선택 범위(고급 필터·내보내기) 1/2. 설계 codex consult 반영. 신규 공용 `query-filter.ts`(normalizeEnumFilter 추출+parseDateRangeFilter) + 클라 `date-range.ts`(로컬날짜→ISO instant) + messages/logs 라우트 필터(enum 검증·날짜·where>cursor) + UI. 듀얼 검증 **codex PASS(r1 WARNING P3×2→r2 WARNING P3×1→r3 PASS)·evaluator WARNING 9.85**(P0/P1/P2 0, fixNow 0, P3×3 defer). 게이트 4/4(tsc0/lint0err/vitest 293→333[+40]/build0). **다음: WI-031(CSV 내보내기)** — 사용자 선택 범위 잔여 1건. **WI-029~031 develop만 — main 미승격(승격은 사용자 승인 게이트).**
- (직전) **✅ WI-029-fix (어드민 대시보드 품질 마감) develop 머지 완료(2026-06-23, PR#30 merge `1bdf225`)**. Logs/Messages 에러처리 + Members 로딩/검색·필터 + vitest jsx automatic. 듀얼 **codex PASS·evaluator 9.85**(P0/P1/P2 0). 게이트 4/4(vitest 268→293).
- (직전) **✅ develop→main 승격 완료(2026-06-23, PR#29 rebase merge `ba3a82f`) — WI-017/018/019/021/022/023/024 라이브 반영**. 적대 통합 감사(4에이전트 워크플로우 GO·블로커0) + codex consult GO. **Vercel 프로덕션 ● Ready**(author=flowcoder25 인가) + **OCI 소켓 자동배포 success**(deploy-socket.yml 34s·헬스체크 통과·WI-018 fail-fast 신코드 부팅·핸드셰이크 200+sid=AUTH_SECRET 유효 라이브 실증) + prod DB 신규 마이그레이션 없음(no-op). 라이브: 웹/로그인 200·API 인증게이트 307. **남은 사용자 검증: 인증 소켓 e2e smoke(로그인→입장→동기화/채팅)** — 사용자 자격증명 필요. develop=main back-sync(rebase SHA 분기). BACKLOG: WI-025(parsePageNumber offset 상한)/WI-026(저장 metadata public/internal 분리)/**WI-028**(Vercel↔OCI AUTH_SECRET 불일치 synthetic probe). **남은 사용자 게이트: WI-024 백필 `--apply` prod 실행(선택).** 적대 통합 감사(4에이전트 워크플로우 GO·블로커0) + codex consult GO. **Vercel 프로덕션 ● Ready**(author=flowcoder25 인가) + **OCI 소켓 자동배포 success**(deploy-socket.yml 34s·헬스체크 통과·WI-018 fail-fast 신코드 부팅·핸드셰이크 200+sid=AUTH_SECRET 유효 라이브 실증) + prod DB 신규 마이그레이션 없음(no-op). 라이브: 웹/로그인 200·API 인증게이트 307. **남은 사용자 검증: 인증 소켓 e2e smoke(로그인→입장→동기화/채팅)** — 사용자 자격증명 필요. develop=main back-sync(rebase SHA 분기). READY 큐 비어 있음. BACKLOG: WI-025(parsePageNumber offset 상한)/WI-026(저장 metadata public/internal 분리)/**WI-028**(Vercel↔OCI AUTH_SECRET 불일치 synthetic probe). **남은 사용자 게이트: WI-024 백필 `--apply` prod 실행(선택).**
- (직전) **✅ WI-017-fix (소켓 토큰 발급 재시도 + 원인별 실패 분류 폴백) develop 머지(PR#28 `333df76`)→승격(PR#29)**. 듀얼 검증 **codex PASS(r1 WARNING P2×2 → r2 PASS)·evaluator 9.83**(P0/P1/P2 0). 게이트 4/4(vitest 249→268). 머지 후 재확인.
- (직전) **✅ WI-018-feat (prod env fail-fast — 소켓 서버 부팅 env 검증) develop 머지 완료(2026-06-22, PR#27 merge `d77aff4`)**. 듀얼 블라인드 1R 수렴 **codex PASS 0 issues·evaluator 9.8**(P0/P1/P2 0, fixNow 0, P3×3 defer).
- (이전) **✅ WI-024-fix (assets metadata.error 정보 노출 차단, CWE-209) develop 머지 완료(2026-06-22, PR#26 merge `91079d1`)**. 듀얼 블라인드 1R 수렴 **codex PASS 0 issues·evaluator 9.85**(P0/P1 0, fixNow 0, P3×3 defer). C안(A+B): allowlist에서 error 제거 + 저장 generic 정규화 + 백필 스크립트. impl `3cd7d04`.
- (이전) **✅ WI-023-fix (500 응답 details 정보 노출 일괄 제거, CWE-209) develop 머지 완료(2026-06-22, PR#25 merge `b788009`)**. 듀얼 블라인드 1R 수렴 **codex PASS 0 issues·evaluator 9.85**(P0/P1 0, P3×2 defer). 26파일 37곳 중앙 헬퍼 일괄. impl `d352c45`.
- (이전) **✅ develop→main 승격 완료(2026-06-22, PR#18 rebase)** + V1→V2 컷오버 e2e 검증. WI-001~016+020 라이브(Vercel ● Ready + prod DB migrate deploy 적용). **남은 운영작업: OCI 소켓 신코드 수동 SSH 배포(사용자 전용)** + SOCKET_INTERNAL_* prod env.

## Queue (2026-06-22 승격前 통합감사 발굴 — codex consult + 9축 멀티에이전트 감사 + 적대검증)
| WI | Type | Status | Goal | Notes |
|---|---|---|---|---|
| WI-017 | fix | **DONE** | 소켓 토큰 획득 실패 폴백 | ✅ fetchSocketToken() 재시도(네트워크/408/429/5xx 백오프 [300,900]ms 총3시도, 401/403/형식오류 즉시실패) + SocketTokenError{code} 분류 → use-socket 사용자 메시지. r2: pendingClient 가드(동시 중복 io 차단) + connectAttemptRef generation 가드(토큰 발급 중 unmount orphan 정리·stale 비-disconnect). 듀얼검증 codex PASS·eval 9.83. 아래 Done 참조. (type=improve enum 외 → fix 분류) |
| WI-018 | feat | **DONE** | prod env fail-fast | ✅ `validateSocketStartupConfig()`로 listen 전 eager 검증(prod AUTH_SECRET 무효→crash, SOCKET_INTERNAL_SECRET→warn). 듀얼검증 1R 수렴(codex PASS 0 issues·eval 9.8). 아래 Done 참조. |
| WI-022 | fix | **DONE** | assets 목록 입력 강건화 | ✅ 듀얼검증 3R 수렴(codex PASS·eval 9.88). 아래 Done 참조. |
| WI-023 | fix | **DONE** | 500 응답 details 정보 노출 일괄 제거(CWE-209) | ✅ 26파일 37곳 중앙 헬퍼 일괄. 듀얼검증 1R 수렴(codex PASS 0 issues·eval 9.85). 아래 Done 참조. (원래 등록된 "정보위생+offset 상한" 중 정보위생만 — 실측 결과 assets 단독 아닌 전역 표면이라 일괄로 확대, offset 상한은 WI-025로 분리) |
| WI-024 | fix | **DONE** | assets 상세 metadata.error 누출(CWE-209) | ✅ C안(A+B): allowlist에서 error 제거 + 저장 generic 정규화 + 백필. 듀얼검증 1R 수렴(codex PASS 0 issues·eval 9.85). 아래 Done 참조. |
| WI-025 | perf | BACKLOG | parsePageNumber 큰 offset 상한 | WI-023에서 분리(codex consult). `parsePageNumber` page 상한 부재 → 큰 offset skip 스케일 비효율. 소비처 page 미사용이라 실표면 0. `MAX_PAGE_NUMBER` 정책 정한 뒤 처리. 저우선. |
| WI-026 | fix | BACKLOG | 저장 metadata public/internal 분리 | WI-024 codex consult 위험#5: generate/batch 성공 경로 `metadata: JSON.parse(JSON.stringify(metadata))`가 `GeneratedAssetMetadata` 전체를 DB 저장. 현재 응답은 `PUBLIC_METADATA_KEYS` allowlist가 막으나, 향후 metadata에 prompt/workflow/local path 등 추가 시 DB 저장면이 계속 확대(allowlist 드리프트 시 재노출 위험). 저장 시점에 public/runtime 필드와 internal/debug 필드를 분리(저장면 자체 축소). 저우선(현 응답 누출 0). |
| WI-028 | feat | BACKLOG | Vercel↔OCI AUTH_SECRET 불일치 probe | WI-018 codex consult 놓칠 위험: WI-018 eager 검증은 AUTH_SECRET "존재·길이 충분"만 검증 — Vercel `/api/socket/token` 서명 키 ↔ OCI 소켓 서버 검증 키가 **서로 다른 값**이면 컨테이너는 healthy로 뜨고 전 토큰 검증이 실패한다(silent). 배포 후 synthetic probe(검증 가능한 테스트 토큰 round-trip) 또는 운영 점검으로 다룰 것. 저우선(현 라이브는 동일 키 실증됨). |
| WI-029 | fix | **DONE** | 어드민 대시보드 품질 마감 | ✅ 사용자 "대시보드 완성" 범위. Logs/Messages `catch{}`→setError + Members 로딩/검색·필터/빈상태 + vitest jsx automatic. 듀얼검증 codex PASS·eval 9.85(P0/P1/P2 0). 아래 Done 참조. |
| WI-030 | feat | **DONE** | 대시보드 고급 필터 | ✅ 메시지/로그 날짜·타입 필터. 공용 query-filter(normalizeEnumFilter 추출+parseDateRangeFilter)·date-range(TZ 함정 회피)·라우트 enum/날짜 검증·UI. 듀얼검증 codex PASS(3R)·eval 9.85. 아래 Done 참조. |
| WI-031 | feat | **DONE** | 대시보드 CSV 내보내기 | ✅ 멤버/로그/analytics 클라 측 CSV. 순수 `lib/csv.ts`(RFC4180+수식인젝션중화[`= + - @` \t \r \n]+BOM) + `csv-export.ts`(매퍼+downloadCsv) + 공용 버튼. 로그=로드분량만(라벨 건수). 듀얼검증 3R 수렴 codex PASS·eval 9.85(P0/P1 0, fixNow 0). 양검증자 선두 LF 비대칭 수렴→해소. payload Details=accepted-risk defer(화면 미러링). PR#32 `6bdee98`. 아래 Done 참조. |
| WI-032 | fix | **DONE** | 로그 payload 응답 allowlist 정형화 + lean DTO | ✅ API 응답 DTO 차단(admin/logs + admin/stats recentActivity). 신규 `src/lib/space-event-log-payload.ts`(8키 allowlist + 비객체/빈결과→null + lean DTO[내부 스칼라 제외]). 양 검증자 stats 병렬경로 독립 수렴→r2 해소. 듀얼검증 codex r1 WARNING(P2/fixNow)→r2 PASS·eval 9.85(P0/P1/P2 0). 아래 Done 참조. |

> **감사 false-positive (재발 주의)**: 감사 에이전트가 "main에 `server/handlers/enforce.ts`/`src/lib/auth-secret.ts`/`Dockerfile.socket` 신규파일 없음 → 장애" 류를 P0 블로커로 다수 오분류함. 이는 **develop ⊋ main = 승격의 목적 그 자체**이며 git merge가 원자적으로 함께 가져감(세 파일 모두 develop 트리 존재 확인). develop 내부 정합성은 기계게이트 green(tsc0/lint0/vitest147)으로 검증됨. **결함 아님.** 실제 go-live 게이트는 아래 '미해결 승인 항목' 2건 + WI-016(드리프트).

## 승격 완료 + 남은 운영작업 (사용자 게이트)
- **✅ develop→main 승격 완료 + 라이브 반영**(2026-06-23, PR#34 rebase merge main HEAD `626769e`): 델타 = WI-029/030/031/032(4건). 사용자 "develop→main 승격" 선택. **Readiness GO** — codex consult GO + 적대 통합감사 4축(cross-WI/통합건전성/배포런타임/보안회귀) 전 차원 GO·blocker 0. develop⊇main strict superset(클린 rebase). **배포 영향: Vercel 웹/일부 API 라우트만**(assets route도 WI-030 query-filter 공용화로 touched) — server/·prisma/ 무변경 → **OCI 소켓 재배포·prod DB 마이그레이션 no-op**, 신규 env 0. **실행**: rebase 머지(author=flowcoder25@gmail.com 보존→Vercel 인가) → **Vercel Production ● Ready**(flowspace-v2-8twa6brxj). **라이브**: space.flow-coder.com 200·/login 200·/api/users/me 307. **develop back-sync**(force-with-lease, develop==main==`626769e`). 산출물 `.flowset/promotion-readiness.md`. 롤백=Vercel 이전배포 promote/main revert. **교훈: gh 인증계정(yonghyeon-dev)≠Vercel 인가계정(flowcoder25)→rebase 머지 필수(main HEAD가 flowcoder25 authored 비-merge 커밋)·승격 후 develop back-sync 필수(SHA 분기 방지·codex 놓칠위험)·로컬 main stale 주의(origin/main SHA 기준).**
- **✅ WI-013 + WI-016 prod 적용 완료**(2026-06-22): `prisma migrate deploy`로 두 마이그레이션 적용. WI-013 `Space_status_updatedAt_id_idx` 생성+구 인덱스 제거(Space 7행 → 즉시·무락, CONCURRENTLY 불필요로 판명). WI-016 isShared no-op(IF NOT EXISTS, prod 컬럼·인덱스 기존재). `_prisma_migrations` 3건(0_init+WI-013+WI-016) SoT 정합. EXPLAIN은 7행이라 후속(데이터 증가 후).
- **✅ develop→main 승격 완료**(2026-06-22): PR#18 **rebase 머지** → main HEAD `50d9af0`(author=flowcoder25 보존→Vercel 인가). Vercel Production ● Ready. process 07 산출물(promotion-readiness.md + promotion-approval.json). codex consult 1R + 적대검증 19에이전트(승격차단 코드결함 0). AUTH_SECRET 라이브 OCI 토큰검증 probe로 44자≥32 확정. main↔develop rebase 분기 back-sync 정합.
- **✅ OCI 소켓 신코드 배포 완료**(2026-06-22, `~/.ssh/flowspace-oci` SSH): git pull(06cf9c5→50d9af0 FF)+빌드+재배포. 신 컨테이너 Up(healthy) 포트3002, 실사용자 접속·룸입장 로그 실증. **배포 함정**: 자동배포 OCI_SSH_PRIVATE_KEY 미설정 + OCI docker-compose **v1**(v2 없음)+v1 recreate 버그(KeyError ContainerConfig) → 우회=build→`rm -f -s socket`→`up -d --no-build`. 롤백이미지 flowspace-v2_socket:rollback-pre-promo 보존.
- **✅ OCI 자동배포(CD) 복구 완료**(2026-06-22): `OCI_SSH_PRIVATE_KEY` secret 등록 + `deploy-socket.yml` 수정(docker-compose v1 + build→rm→up --no-build 우회 + workflow_dispatch + 헬스체크, PR#19 main). `gh workflow run` 테스트 ✓25s 성공(`✅ socket healthy`). 향후 main push 시 자동배포.
- **남은(사용자 권한)**: ①🔴 `v2-socket.flow-coder.com` **DNS 레코드 없음(NXDOMAIN)** → 라이브는 임시 `socket-v2.144.24.72.143.nip.io`로 운영(방치해도 정상, 정식 도메인 전환은 DNS 패널 cnmnoc에서 A레코드 추가+Vercel NEXT_PUBLIC_SOCKET_URL 갱신). ②WI-005 실시간제재: OCI+Vercel SOCKET_INTERNAL_*(Vercel env 사용자 권한). ③WI-013 prod EXPLAIN(데이터 증가 후).

## Done
- **WI-032-fix** (보안/정보위생 — 어드민 로그 SpaceEvent payload 응답 allowlist + lean DTO) — `SpaceEventLog.payload`(Json)가 클라 반환 경로로 raw 직렬화되던 표면을 **응답 DTO 계층**에서 차단(deny-by-default, WI-014/019/021/024 동형). **READY 큐 소진 → 설계 codex consult로 BACKLOG 4건(WI-025/026/028/032) 우선순위 협의 → WI-032 선정**(codex: 1순위는 승격 권고[사용자 게이트]이나 코드 WI 한다면 WI-032가 최선 — small·low-risk·순수함수·deny-by-default 레일 동형 / WI-028 코드-WI 부적합=self-probe false-pass·운영성격, WI-025/026 표면0 cheap defense). **해소 (r1 `b3dd931`)**: 신규 순수 모듈 `src/lib/space-event-log-payload.ts` — `PUBLIC_SPACE_EVENT_PAYLOAD_KEYS`(현존 5개 write site에서 실측한 8키: action·messageId·targetMemberId·targetName·senderName·trackType·trackSource·participantName) allowlist + `toPublicSpaceEventPayload`(null/undefined/비객체/배열/빈결과 → null, 화면 "-"·CSV "" 계약 보존) + `toPublicSpaceEventLog` lean DTO(소비처가 쓰는 {id,eventType,payload,createdAt,user}만 — spaceId/userId/guestSessionId/participantId 내부 스칼라 제외). **차단은 API 응답 DTO 계층**(화면/CSV만 필터하면 raw 직접 호출로 우회 — codex consult 놓칠위험). `admin/logs` route가 `buildCursorPage` 후 `items.map(toPublicSpaceEventLog)`로 정규화(`{logs,nextCursor}` 계약 보존). 화면(event-log-table)·CSV(csv-export)·페이지는 payload 타입만 공유 `PublicSpaceEventPayload`로 단일화(런타임 무변경 — 정규화는 API 담당, 이중 책임 회피). `import type { Prisma }`만 사용 client-safe. **r2 `b6e3811` — 듀얼검증 P2 해소**: 양 검증자가 **독립 수렴 발견** — `GET /api/spaces/[id]/admin/stats`의 `recentActivity`도 raw `spaceEventLog.findMany`(payload + 내부 스칼라)를 정규화 없이 반환하던 **병렬 누출 경로**(codex P2/fixNow·evaluator P3) → "응답 DTO 차단" 주장의 구멍. stats route에 `recentActivity.map(toPublicSpaceEventLog)` 동일 적용(오버뷰 페이지는 payload 미렌더·내부 스칼라 미사용 → 무회귀) + 신규 `stats/route.test.ts`(payload allowlist·lean DTO·권한·카운트 무회귀) + page payload 타입 단일화 + logs 주석 정정. **전수 grep 실측: SpaceEventLog 행을 클라에 반환하는 read 경로는 `admin/logs` + `admin/stats` 둘뿐**(나머지 4개는 write site — livekit/webhook·announce·members·messages). **무회귀**: 소비처(table/page/csv) 모두 {id,eventType,payload,createdAt,user}만 사용·내부 스칼라 미참조 실측, payload 정규화는 현 operational 키 전부 보존. 회귀 테스트 +20(순수 모듈 13: allowlist 전수·금지키 제거·빈결과 null·비객체/배열/null/undefined·lean DTO 키셋·user 매핑 / logs route +3: 응답 payload allowlist·lean·빈→null / stats route 신규 4: payload allowlist·lean·권한·카운트). 기계게이트 4/4(tsc0/lint0err[선재 LiveKit 경고1·WI무관]/vitest 364→**384**[+20]/build0). 설계 codex consult 1R + **듀얼 블라인드 2R 수렴: codex r1 WARNING(P2/fixNow stats)→r2 PASS 0 issues · evaluator 최종 HEAD WARNING 9.85**(P0/P1/P2 0, fixNow 0, P3×3 defer: allowlist↔write site 자동 drift 가드 부재[현 정확 일치·deny-by-default 의도]·event-log-table Details JSON.stringify raw 렌더[키 비민감·DTO chokepoint 의존]·허용 키 값 형태 미검증[현 write site 전부 스칼라·실표면 0]) + `.pass`(fingerprint empty `e3b0c44`). evaluator 변이검증 3종 실증(M1 allowlist passthrough→10 FAIL·M2 lean DTO spread→3 FAIL·M3 빈결과→{}→4 FAIL, 전부 원복 clean). **핵심 교훈**: (a) **DB Json 컬럼 응답 정형화는 단일 라우트가 아니라 그 모델 행을 반환하는 모든 read 경로를 전수 grep해야 완결** — admin/logs만 고치면 admin/stats recentActivity가 같은 raw를 새 나감(양 검증자 독립 수렴, WI-023 "전역 표면 grep" 교훈 연장). (b) **차단 chokepoint는 화면/CSV가 아니라 API 응답 DTO** — raw JSON 직접 호출이 표시 계층 필터를 우회(codex consult). (c) **payload allowlist + lean DTO를 같은 헬퍼로 묶으면 향후 모델 컬럼 추가·select 확장에도 응답 키 집합 고정**(WI-019/021 패턴). impl `b3dd931`/r2 `b6e3811`. **develop 머지 완료 (PR#33, merge `bf4a3ea`)**. main 미승격.
- **WI-030-feat** (어드민 대시보드 고급 필터 — 메시지/로그 날짜·타입) — 사용자 "대시보드 완성" 선택 범위(고급 필터·내보내기) 1/2. **설계 codex consult 1R 수렴**: 날짜는 ISO datetime instant 수신(start inclusive gte/end exclusive lt, 클라가 익일 00:00 전송=종료일 포함, date-only 서버파싱 UTC 함정 회피) · enum은 normalizeEnumFilter 재사용(assets/route.ts 내부함수→공용 추출 필요) · 날짜 파서는 pagination.ts 아닌 별도 lib · 잘못된 날짜·start≥end는 무시 아닌 400. **해소**: ① 신규 공용 `src/lib/query-filter.ts` — `normalizeEnumFilter`(WI-022 assets에서 verbatim 추출, assets/route.ts는 import 전환=DRY) + `parseDateRangeFilter`(startDate/endDate→`createdAt:{gte,lt}`, 잘못된 날짜·start>=end→"invalid"→400). ② `admin/messages` 라우트: `type`(MessageType enum 검증) + 날짜 범위, `where`가 cursor보다 우선 적용(권한 밖 row 노출 불가, WI-010), 응답 `{messages,nextCursor}` 불변. ③ `admin/logs` 라우트: `eventType` **enum 검증 보강**(기존 `where.eventType=eventType` 직접 주입 → 오타 시 Prisma 500 위험 → normalizeEnumFilter로 400) + 날짜 범위 + `buildCursorPage` 통일(messages 정합·인라인 hasMore 로직 제거, 응답 계약 불변). ④ 클라 `src/components/dashboard/date-range.ts` — `<input type="date">` 로컬 날짜→ISO instant 변환(start=로컬 자정, end=익일 자정 exclusive, 타임존 함정 회피). ⑤ messages/logs 페이지 UI: 타입 select + 날짜 범위 입력(필터 변경 시 useCallback deps로 cursor 리셋 reload, codex "필터 변경 시 cursor/list 초기화" 정합) + logs `EVENT_TYPES`를 전체 SpaceEventType(VIDEO_*/SCREEN_SHARE_* 포함)로 확장. **r2 `f2135ef` — codex P3×2(입력 strictness) 즉시 강화**: parseInstant에 ISO instant 정규식(date-only/offsetless 거부, 클라 `.toISOString()` Z는 통과) + parseLocalDate에 YYYY-MM-DD 정규식+달력 round-trip(JS Date 롤오버 2026-02-31→3월 거부). **r3 `c7176f9` — codex r2 P3(fixNow) 해소**: parseInstant가 regex 통과 후에도 calendar-invalid instant(`2026-02-31T00:00:00.000Z`→JS Date 3/3 보정)를 허용하던 클라/서버 비대칭 → regex 캡처 그룹으로 month/day(daysInMonth=`Date.UTC(year,month,0)` 윤년 정확)/hour/min/sec 직접 검증. **무회귀**: 필터 미지정 시 `where:{spaceId}`만(기존 전체 조회 보존), 응답 계약 불변, assets normalizeEnumFilter 전환 verbatim(WI-022 계약 보존·route.test 무회귀), logs buildCursorPage 전환 limit/nextCursor 동작 보존. 회귀 테스트 +40: query-filter 19(enum 전수+날짜범위 경계+ISO instant strict+달력/윤년) + date-range 9(TZ 독립·롤오버) + messages route 7(type/날짜/400/cursor 공존/무회귀) + logs route 10(신규: eventType 검증/날짜/buildCursorPage). 기계게이트 4/4(tsc0/lint0err[선재 LiveKit 경고1·WI무관]/vitest 293→**333**[+40]/build0). **듀얼 블라인드 수렴: codex PASS**(r1 WARNING P3×2 → r2 WARNING P3×1 → r3 PASS 0 issues, 능동 실측 tsc0/lint0/git diff --check) **· evaluator WARNING 9.85**(P0/P1/P2 0, fixNow 0, P3×3 defer: 클라 표시용 enum 리터럴[Prisma enum 일치·서버 SSOT 400 거부]·UI 카피 i18n 미도입·날짜 span 상한 부재[cursor가 응답폭 제한·WI-025 계열 저우선]) + `.pass`(fingerprint empty `e3b0c44`). 양측 변이검증 실증(날짜 범위 `>=`→`>`·enum allowlist 무력화[assets 추출 회귀까지 검출]·롤오버 가드 제거·날짜 drop FAIL). **핵심 교훈**: (a) **date-only를 서버에서 `new Date()` 파싱하면 UTC로 해석돼 클라 로컬 날짜와 어긋남 → 클라가 로컬 자정 기준 ISO instant로 변환 전송하고 서버는 instant만 수용**(타임존 함정). (b) **JS `new Date()`는 calendar-invalid를 조용히 롤오버(2026-02-31→3월)·시간도 보정 → regex 형식 검증만으론 불충분, 컴포넌트 범위 직접 검증 필요**(클라/서버 양쪽 일관). (c) **offset instant(`+09:00`)는 UTC 비교로 달력 검증 불가 → 작성된 컴포넌트(캡처 그룹)를 직접 검사**. (d) **enum 미검증 직접 주입(`where.X=raw`)은 Prisma enum에서 오타 시 500 — normalizeEnumFilter 전수 검증으로 400 전환**. impl `a4b3dd0`/r2 `f2135ef`/r3 `c7176f9`. **develop 머지 완료 (PR#31, merge `a41e2a8`)**. main 미승격.
- **WI-029-fix** (어드민 대시보드 품질 마감 — 클라 UX 견고성) — 사용자 지시 "어드민 대시보드 완성"(다음 우선과제). 정밀 조사(Explore 에이전트): 대시보드는 이미 ~85% 완성(7페이지·9컴포넌트·10 API 전부 동작, 스텁/mock/TODO 없음, 권한 게이트 전면). 사용자 선택 범위(AskUserQuestion) = **품질 마감 + 고급 필터·내보내기**(엔터프라이즈 제외). WI-029=품질 마감 분: ① **Logs/Messages 페이지** `catch {}`(에러 조용히 무시)를 `setError`+인라인 표시로 보강 — fetch 실패 시 사용자에게 원인 노출(loadMore 실패가 기존 데이터 wipe 안 함·비파괴). ② **Members 페이지**: `useState<[]>` 타입 결함→`Member[]` + 로딩 상태(`isLoading`)+Loading UI + 이름/이메일 검색 + 역할 필터(순수 `filterMembers`, 클라 측 admin-gated 데이터) + 빈 상태("멤버 없음"/"검색 결과 없음") + `loadMembers` async/try-catch-finally 통일(`set-state-in-effect` 린트[app.md#11]+logs/messages 일관성). ③ **member-table.tsx**: `Member`/`MemberRoleFilter` 타입 export(페이지·필터 재사용, 동일 디렉토리라 경계 위반 아님). ④ **vitest.config.ts**: `esbuild.jsx="automatic"` — Next.js 소스(React 미import)의 컴포넌트 렌더 테스트 가능화(신규 인프라, 기존 268 `.test.ts`는 JSX 무관이라 무회귀 실증). **신규 순수 헬퍼** `member-filter.ts` `filterMembers(members,query,role)` — 표시명 우선순위(`user.name||guestSession.nickname||displayName`, member-table:73 verbatim 정합)·trim·대소문자·역할 AND. **r2 `e1ca2f7` — codex P2(fixNow) 해소 + P3 강화**: (P2) Members 초기 로드 실패 시 `error` 인라인과 "멤버가 없습니다." 빈 상태가 **동시 렌더**(error+members=[])되던 것(실패≠빈목록 미구분) → `members.length===0` 분기를 `error ? null : <빈 상태>`로 분리(refresh 실패로 members 존재 시 테이블 유지=비파괴). (P3) `filterMembers` 표시명 우선순위 ||체인 reorder 약한 오라클 → 우선순위 테스트 2종(user.name 존재 시 하위후보 검색 비매칭 / guestSession 우선)으로 강화. 회귀 테스트 +25: `member-filter.test.ts` 15(검색·역할·AND·trim·우선순위) + `members/page.test.tsx` 6(로딩/빈상태/검색·역할필터/검색무결과/에러+빈상태억제) + `logs`·`messages/page.test.tsx` 각 2(에러표시·성공무에러). 기계게이트 4/4(tsc0/lint0err[선재 LiveKit 경고1·WI무관]/vitest 268→**293**[+25]/build0). **클라 전용**. **듀얼 블라인드 수렴: codex PASS**(r1 WARNING P2 fixNow+P3 → r2 PASS 0 issues, 능동 실측 tsc0/lint0/git diff --check) **· evaluator WARNING 9.85**(P0/P1/P2 0, fixNow 0, P3×3 defer: isLoading 초기값 비대칭[members true vs logs/messages false, 의도된 차이·nit]·성공시 에러클리어 전용테스트 부재[코드 검증 정확·커버리지 gap]·UI 카피 하드코딩[기존 대시보드 패턴·i18n 미도입]) + `.pass`(fingerprint empty `e3b0c44`). 양측 변이검증 실증(에러처리 catch{} 복원→FAIL·역할 AND→4 FAIL·email→1 FAIL·trim→2 FAIL·빈상태 억제 가드 제거→1 FAIL·우선순위 ||reorder→2 FAIL). **핵심 교훈**: (a) **에러 인라인 표시 시 "빈 상태(empty)"와 "실패(error)"는 상호배타로 분기해야 함** — 둘 다 members=[]+isLoading=false 조건이라 `error ? null` 가드 없으면 동시 렌더(codex P2 적출). (b) **vitest esbuild는 기본 classic JSX 런타임 → Next.js 소스(React 미import)는 `esbuild.jsx="automatic"` 없이 컴포넌트 렌더 테스트 불가**(React is not defined). (c) **동기 함수를 effect에서 호출+즉시 setState하면 `set-state-in-effect` 린트(app.md#11) — async/try-finally 패턴이 회피**. impl `580d1de`/r2 `e1ca2f7`. **develop 머지 완료 (PR#30, merge `1bdf225`)**. main 미승격.
- **WI-017-fix** (클라 UX/견고성 — 소켓 토큰 발급 재시도 + 원인별 실패 분류 폴백) — `getSocketClient()`의 토큰 발급 fetch(`/api/socket/token`)가 `!res.ok`면 즉시 `throw new Error("Failed to get socket token")`하고 재시도/명확한 에러가 없어, 세션만료(401)·일시적 5xx(AUTH_SECRET 오설정 fail-closed 포함)·네트워크 오류가 전부 use-socket catch의 generic `"소켓 연결에 실패했습니다."`로 접히던 클라 UX 결함 해소. **READY 큐 소진 → 설계 codex consult로 BACKLOG 4건(WI-017/025/026/028) 우선순위 협의 → WI-017 선정**(실재 클라 결함·socket-client.ts 단일 격리·retry 정책 명확, WI-025 정책미정·실표면0 / WI-026 응답누출 이미 차단·batchId 의존 / WI-028 probe/배포 표면 큼 대비). **type=improve는 wi-global enum 외 → `fix` 분류**(견고성 결함 해소). **해소 (r1 `fa250bc`)**: 신규 `fetchSocketToken(opts?)` 추출 — 일시적 실패(fetch 예외·408/429/5xx)는 `SOCKET_TOKEN_RETRY_DELAYS_MS=[300,900]ms` 백오프로 총 3시도 재시도, 인증 실패(401/403)·비-재시도 4xx·응답 형식 오류(token 누락/빈문자열/json 예외)는 즉시 실패. `SocketTokenError{code}` 분류(`UNAUTHORIZED`/`TEMPORARY_FAILURE`/`NETWORK_FAILURE`/`INVALID_RESPONSE`) → use-socket `socketTokenErrorMessage`가 사용자 안내 메시지로 매핑(communication.md #8 에러 세분화 정합). **익명 폴백 없음**(서버 `verifySocketToken` 토큰 필수 검증 계약 — "폴백"=명확한 실패 상태이지 무인증 접속 아님, codex consult 확인). 테스트 주입 `sleep` seam으로 백오프 즉시화(프로덕션 동작 무변). **r2 `d87ac75` — codex 검증 P2×2(fixNow) 해소**(retry가 토큰 발급 await 윈도우를 넓혀 악화시킨 동시성/생명주기 race, codex consult로 fix 설계 검증): (P2-1) `getSocketClient`에 module-level `pendingClient` promise 가드 — in-flight 생성 중 동시 호출이 동일 promise 공유(중복 fetch/io()로 마지막 생성분만 남고 나머지 누수되던 race 차단), `finally { if (pendingClient === pending) pendingClient = null }`(더 새로운 생성 미덮어씀). (P2-2) use-socket에 `connectAttemptRef` generation 카운터 — effect 시작 `++connectAttemptRef.current`, `!mounted` 분기에서 `connectAttemptRef.current === attemptId`(후속 effect 없음)일 때만 orphan 소켓 정리 + 성공/catch 경로도 generation 가드. **공유 pending이 같은 sock을 반환하므로 socket identity 비교론 불충분 → stale effect가 후속 effect의 소켓을 끊지 않게 generation으로 소유권 판정**(codex 적출). **무회귀**: getSocketClient io() 옵션(`auth:{token}`·transports·RECONNECTION_*)·싱글턴 재사용 보존(communication.md #2 reconnection 무손상), use-socket 기존 generic 폴백·전 이벤트 핸들러 등록 보존, 토큰 계약 `{token}` 무손상. 캡슐화: `fetchSocketToken`/`SocketTokenError`는 socket 배럴 미노출(공개 API 무확장). 회귀 테스트 +19: socket-client.test.ts 17(재시도 분류 전수: 성공/네트워크2회후성공/5xx2회후성공/429·408 재시도/401·403 no-retry/5xx·네트워크 소진/400 비-재시도/token 누락·빈·json예외 INVALID/getSocketClient 401 전파 + pending dedupe·재사용·io 옵션 보존) + 신규 use-socket.test.ts 2(생명주기 race: 순수 언마운트 orphan 정리 / deps 변경 시 stale 비-disconnect, **generation 가드 무력화 시 scenario FAIL 로컬 변이검증 실증**). 기계게이트 4/4(tsc0/lint0err[선재 LiveKit 경고1·WI무관]/vitest 249→**268**[+19]/build0). **클라 전용 — 서버 esbuild 번들 무관**(socket-client는 socket.io-client·window 의존, server/ 미참조 실측). **듀얼 블라인드 검증 수렴: codex PASS**(r1 WARNING P2×2 fixNow → r2 PASS 0 issues, 능동 실측 tsc0/lint0/git diff --check) **· evaluator WARNING 9.83**(P0/P1/P2 0, fixNow 0, P3×3 전부 defer: ①finally `===pending` 가드 변이검증 미보호[M6 거짓통과 — 2세대 인터리빙은 `if(pendingClient)return` early-return이 막아 **현 설계 도달 불가** dead-defensive, 최악=transient fetch 1회·누수 아님] ②socketTokenErrorMessage 매핑 단언 테스트 부재[순수 표시 로직·exhaustive switch] ③NETWORK_FAILURE 메시지 networkErr.message interpolation[상대 URL이라 시크릿0·UI 미전파·console.error 로그만]) + `.pass`(fingerprint empty `e3b0c44` — source 커밋 후 작업트리 clean). evaluator 변이검증 6종 실증(M1 generation 가드/M2 pending dedupe/M3 401 즉시실패/M4 retry off-by-one/M5 orphan cleanup → 전부 검출 FAIL·M6만 미검출=P3 gap, 전부 원복 clean). **핵심 교훈**: (a) **클라 토큰 발급 단계는 io()의 reconnection(소켓 생성 후)과 별개 레이어 — fetch 실패는 재시도가 따로 필요**(io 재연결은 토큰 fetch를 커버 못 함). (b) **비동기 생성 헬퍼에 retry를 넣으면 await 윈도우가 넓어져 기존 동시성/생명주기 race를 악화 — pending 공유 가드 + effect generation 가드가 짝**(retry 단독 추가는 latent race를 더 자주 트리거). (c) **공유 pending이 같은 객체를 반환하면 stale-effect cleanup이 socket identity로 소유권 판정 불가 → generation 카운터 필요**(codex 적출 race). impl r1 `fa250bc`/r2 `d87ac75`. **develop 머지 완료 (PR#28, merge `333df76`)**. main 미승격.
- **WI-018-feat** (운영 안전 — 소켓 서버 부팅 env fail-fast) — 별도 socket.io 서버(`server/index.ts`)가 AUTH_SECRET을 **첫 연결 시점에만 lazy** 검증(`io.use` → `verifySocketToken` → `src/lib/auth-secret.ts` `getAuthSecret()` throw → connection 거부)해, AUTH_SECRET 미설정/단문 컨테이너가 healthcheck(포트 200)를 통과해 "healthy"로 뜬 뒤 **모든 연결을 조용히 거부**하던 운영 위험(silent degrade) 차단. `httpServer.listen()` 전 eager 검증으로 fail-fast(부팅 즉시 crash + 명확한 로그 → lazy connection failure 추적보다 관측성↑). **설계 codex consult 1R 수렴**: 큐 소진 상태에서 BACKLOG 4건(WI-017/018/025/026) 우선순위 협의 → WI-018 선정(서버 fail-fast가 근본 원인, WI-017 클라 UX는 후속) + AUTH_SECRET hard-fail / SOCKET_INTERNAL_SECRET warn 구분 + production gating + `getAuthSecret` 재사용. **해소**: 신규 순수 모듈 `src/lib/socket-startup.ts` `validateSocketStartupConfig()` — (a) **AUTH_SECRET(필수)**: `getAuthSecret()`를 try/catch로 eager 호출(정책 중복 구현 금지), `NODE_ENV==="production"`+미설정/32자 미만이면 **throw**(listen 전 crash), 비-production은 throw 대신 **경고만**(dev/test 동작 보존·연결 시 lazy 검증 유지). (b) **SOCKET_INTERNAL_SECRET(선택)**: 의도된 graceful degrade(enforce 비활성·DB 제재는 동작, WI-005)라 throw 아닌 **production 경고만**(선택값을 필수로 승격 안 함). (c) 시크릿 **값 자체는 메시지에 미포함**(원인·조치만). `server/index.ts`가 `listen()` 전에 호출 + 경고 로깅. **경계/번들**: server는 별도 esbuild 번들(배럴 import 시 React 끌림→빌드깨짐)이라 순수 모듈만 COPY — `Dockerfile.socket`에 `src/lib/socket-startup.ts` COPY 추가(런타임 입력) + `deploy-socket.yml` paths 트리거 추가. **번들 COPY 완전성**: esbuild `--metafile`로 런타임 src 입력 정확히 4개(chat-constants·enforce/contract·auth-secret·**socket-startup**) 실증 — 전부 COPY, `socket-events.ts`는 type-only(elide·계약 추적용)(WI-012-1 교훈: 로컬 esbuild "전 src 존재"가 아닌 metafile src 입력 ↔ COPY diff가 결정적). **라이브 PoC 2종**: `NODE_ENV=production AUTH_SECRET=short` → `server/index.ts:24`(validator 호출)에서 throw, exit1, listen 전(스택트레이스 확인), 시크릿 미노출, "Server running" 미출력 / `NODE_ENV=production` + 유효 AUTH_SECRET + SOCKET_INTERNAL_SECRET 미설정 → 정상 부팅 + 경고(graceful degrade). 회귀 테스트 +11(`socket-startup.test.ts`: production throw·비-prod warn·SOCKET_INTERNAL_SECRET warn 분기·시크릿 미노출 카나리). **무회귀**: `verifySocketToken`→`getAuthSecret()` per-connection lazy 검증 무손상(eager validator는 additive·교체 아님), 전체 vitest 회귀 0. `NODE_ENV=production`은 `Dockerfile.socket:55`·`docker-compose.prod.yml:14` 양쪽 명시(production gating 신뢰). 기계게이트 4/4(tsc0/lint0err[선재 LiveKit 경고1·WI무관]/vitest 238→**249**[+11]/build0) + esbuild 서버 번들 0 + metafile COPY 완전성. **듀얼 블라인드 1R 수렴: codex PASS 0 issues · evaluator WARNING 9.8**(P0/P1/P2 0, fixNow 0, P3×3 전부 defer: ①prod-throw 카나리 약한 오라클[거짓통과 아님·누출 방어는 dev-warning 카나리 Mutation3 실증+getAuthSecret 정적 메시지가 담당] ②NODE_ENV 엄격일치[배포 SoT 2곳 소문자 고정·코드베이스 관례 정합] ③top-level 부수효과[단일 esbuild 진입점·순수 함수 분리, import 소비처 부재]) + `.pass`(fingerprint empty `e3b0c44` — source 커밋 후 작업트리 clean). evaluator 변이검증 3종 실증(prod throw 무력화→3 FAIL·always-throw로 dev/test 깨기→3 FAIL·dev warning 시크릿 누출→1 FAIL, 전부 원복 clean). codex 능동 실측(카나리 길이 29·git diff --check clean). **codex consult 놓칠 위험(Vercel↔OCI AUTH_SECRET 불일치는 존재·길이 검증이 못 잡음)은 WI-028 분리.** impl `e8d2f16`. **develop 머지 완료 (PR#27, merge `d77aff4`)**. main 미승격.
- **WI-024-fix** (보안/정보위생 — assets metadata.error 정보 노출 차단, CWE-209) — 비동기 에셋 생성 실패 시 raw `error.message`(ComfyUI 내부 URL·로컬 파일 경로·프롬프트/스택 단편 포함 가능)가 `GeneratedAsset.metadata.error`에 저장되고, WI-019에서 도입한 응답 allowlist `PUBLIC_METADATA_KEYS`(`'error'` 포함)를 거쳐 `GET /api/assets/[id]`(owner/superAdmin 게이트) 응답으로 도달하던 정보 노출(WI-023 evaluator 발굴·동일 클래스) 차단. **설계 codex consult 1R 수렴 — C안(A+B)**: B(저장 정규화) 단독은 **기존 DB 행이 이미 raw `metadata.error`를 보유**해 불완전(generate/batch만 고쳐도 과거 행이 응답에 새 나감) → A(응답 allowlist 제외)로 기존·신규 행 모두 응답 계층 차단 필수. **해소**: (A) `src/features/assets/internal/public-asset.ts` `PUBLIC_METADATA_KEYS`에서 `'error'` 제거 — `toPublicMetadata`가 allowlist 키만 골라 반환하므로 기존 raw 행도 즉시 응답 차단(심층방어), 폴링 계약은 `status`(FAILED)로 충분. (B) `generate/route.ts`·`batch/route.ts` fire-and-forget `.catch`가 raw error는 `console.error("[POST /api/assets/...]", error)`로 **서버 로그에만**, DB엔 generic `GENERATION_FAILURE_MESSAGE`(="Asset generation failed", `features/assets/internal/constants.ts` 정의 + 배럴 노출, 하드코딩 회피) 저장(`internalErrorResponse` 정책 정합·근본 차단). batch는 `{ batchId, error: GENERATION_FAILURE_MESSAGE }`(batchId 보존). (백필) `scripts/backfill-failed-asset-error.mjs` — 기존 FAILED 행의 raw metadata.error를 generic으로 치환(dry-run 기본·`--apply`만 적용·멱등[이미 generic/error 키 없는 행 스킵]·error 키만 치환·batchId 등 보존), prod 적용 = 사용자 승인 게이트. **무회귀**: `metadata.error` 읽는 런타임 소비처 부재(전수 grep — game-loader는 metadata spread하나 `.error` 미사용·FAILED는 filePath 없어 미도달, sprite-generator는 frameWidth/Height만, 유일 참조는 합성 테스트였음), 소비처(game-loader/sprite-generator/asset-loader/배치 GET 폴링) 보존. 회귀 테스트 +6: `public-asset.test.ts`(L170 "보존"→"공개되지 않음(차단)" 뒤집기 + generic 제거 단언 + `PUBLIC_METADATA_KEYS` forbidden 키에 `error` 추가) + `generate/route.test.ts`(신규 3) + `batch/route.test.ts`(신규 2: fire-and-forget `.catch`를 `vi.waitFor`로 검증 — raw 미저장·generic 정규화·console.error로 raw 보존·비-Error 거부값). 기계게이트 4/4(tsc0/lint0err[선재 LiveKit 경고1·WI무관]/vitest 232→**238**[+6]/build0). **듀얼 블라인드 1R 수렴: codex PASS 0 issues · evaluator WARNING 9.85**(P0/P1 0, fixNow 0, P3×3 전부 defer: 백필 상수 미러링[드리프트 가설·1회성 ops]·`.catch` 2차 update unhandled rejection[선재 구조·무회귀]·DB 잔존 raw[backfill 전·응답 누출 0인 의도된 심층방어]) + `.pass`(fingerprint empty `e3b0c44` — source 커밋 후 작업트리 clean). evaluator 변이검증 3종 실증(allowlist에 error 복원→public-asset 3 FAIL·generate raw 저장 복원→2 FAIL·console.error 제거→1 FAIL, 전부 원복 clean). **WI-023 evaluator defer P3(metadata.error 누출) 흡수 완료.** codex consult 위험#5(성공 경로 metadata 전체 저장면 확대)는 **WI-026** 분리. impl `3cd7d04`. **develop 머지 완료 (PR#26, merge `91079d1`)**. main 미승격.
- **WI-023-fix** (보안/정보위생 — 500 응답 details 정보 노출 일괄 제거, CWE-209) — 다수 API 라우트가 catch 블록 500 응답에 `details: error instanceof Error ? error.message : undefined`를 그대로 반환해 원본 에러(Prisma 제약/컬럼명·내부 파일 경로·스택 단편)를 클라이언트에 노출하던 것을 중앙 헬퍼로 일괄 제거. **실측 발굴**: WI-022 듀얼검증 defer P3는 `assets/route.ts:124` 단독으로 등록됐으나, 전수 grep 결과 **26개 라우트 파일 37곳**(spaces/route·spaces/[id]·members·messages·admin/*·map/*·join·assets/*·workflows·guest·auth/register 등)에 동일 패턴이 퍼진 전역 표면이었음(messages 라우트도 포함 — "messages와 정렬" 전제가 틀림). **설계 codex consult 1R 수렴**(후보 A 채택·26파일 일괄·중앙 헬퍼·offset 분리·dev에서도 details 미반환·code 미추가). **해소**: (a) 신규 `src/lib/api-error.ts` `internalErrorResponse(context, error, message)` — 원본 에러는 `console.error("[context]", error)`로 **서버 로그에만**, 클라엔 `NextResponse.json({ error: message }, { status: 500 })`만 반환. `details`는 환경 무관 영구 미반환, Prisma 에러 원문 절대 미반환. (b) 26 라우트 500 catch 37곳을 `internalErrorResponse("<METHOD> <route path>", error, "<기존 메시지 verbatim>")`로 치환(기존 메시지 보존, `code` 미추가 → 응답 키집합 정확히 `{ error }`). **디버깅 정보 보존**: 기존 26 라우트 catch는 서버 로깅이 없어 details 제거 시 정보 소실 위험 → 헬퍼가 console.error로 보존. (c) DB metadata 저장용 `error.message`(500 아님 — assets generate/batch `metadata.error`)는 의도적 미변경. **제외 3라우트**(socket/token·livekit/webhook·livekit/token)는 원래부터 `{error}`+console.error라 누출 클래스 무속. 회귀 테스트: 헬퍼 유닛 5종(`api-error.test.ts`) + 대표 라우트 3곳(assets·assets/[id]·spaces) 500 details 미노출·정확 키집합 단언. 무회귀(`.details` 읽는 클라 소비처 0건, app.md #4 정합). 기계게이트 4/4(tsc0/lint0err[선재 LiveKit 경고1·WI무관]/vitest 227→**232**[+5]/build0). 잔여 누출 grep `details: error instanceof Error` → **0**. **듀얼 블라인드 1R 수렴: codex PASS 0 issues · evaluator WARNING 9.85**(P0/P1 0, P3×2 전부 defer: ①metadata.error→GET/api/assets/[id] 잔존[owner-gated·범위밖→**WI-024**] ②console.error 직접사용[기존 25라우트 패턴 정합·결함 아님]) + `.pass`(fingerprint empty `e3b0c44` — source 커밋 후 작업트리 clean). evaluator 변이검증 실증(헬퍼에 details 복원 시 4파일 6테스트 FAIL·원복 diff0). offset 상한(`parsePageNumber`)은 codex consult대로 분리 → **WI-025**. impl `d352c45`. **develop PR 대기**.
- **WI-022-fix** (robustness/입력 강건화 — assets 목록 GET 입력 검증) — `GET /api/assets`(목록)의 선재 입력 검증 부채 해소 (WI-021 듀얼검증 발굴, codex consult 별 WI 분리 권고; 누출 아님 — 응답 누출은 WI-021 완료). **세 결함**: ① `type`/`status`를 `type.toUpperCase()`로 enum 무검증 직접 주입(오타→Prisma enum 불일치 500) ② `limit = parseInt(...||"20")` 무상한·NaN(`limit=100000`→`take:100000` 스케일 위험) ③ `page = parseInt(...||"1")` 무가드(`page=0/-5`→음수 skip→Prisma throw 500). **해소**: (a) 신규 `normalizeEnumFilter(values:string[], allowed)` — `searchParams.getAll()` 전 값을 `trim().toUpperCase()` 후 `Object.values(AssetType)`/`Object.values(AssetStatus)`(Prisma 런타임 enum, 하드코딩 배열 회피) allowlist로 **전수 검증**(중복 파라미터 `?type=character&type=weapon`의 invalid 2번째 값 우회 차단 — codex r2), 빈값/공백-only → 필터 미적용(미지정 동일, codex r1), 하나라도 불일치 → 400 `{error,code:"INVALID_FILTER"}`(WI-009 패턴·app.md#4), 전부 유효 → 첫 값(Prisma scalar equality). (b) `src/lib/pagination.ts` `parsePageLimit`에 **optional `defaultLimit` param 추가** → `parsePageLimit(raw,20)`로 assets default 20 보존(회귀 회피, codex consult B) + MAX 100 cap. (c) 신규 `parsePageNumber(raw)` — NaN/0/음수 → 1 클램프(음수 skip 차단). 검증 순서 auth(401)→enum(400)→page/limit 정규화→쿼리. **무회귀 실측**(2 소비처): `asset-loader.ts:124`(소문자 `status=completed`+`limit=100` — `.toUpperCase()` 후 검증이라 통과·cap 100 정확)·`asset-palette.tsx:38`(`COMPLETED`+`limit=100`). `parsePageLimit` optional param이 기존 무인자 호출부 3곳(spaces/messages×2) 무영향, 기존 계약 `"12.9"→12` 보존. 회귀 테스트: pagination.test.ts +6(defaultLimit·parsePageNumber, 13→19), route.test.ts +15(enum 400/소문자 무회귀/전 enum/trim/공백-only/중복 파라미터/limit cap·default/page 클램프, 10→25). 기계게이트 4/4(tsc0/lint0[선재 LiveKit 경고1·WI무관]/vitest 204→**227**[+23]/build0). 설계 codex consult 1R(B/C/A/400 채택 + page 상한 backlog) + **듀얼 블라인드 3R 수렴: codex r1 WARNING(fixNow P3 공백-only)→r2 WARNING(fixNow P3 중복 파라미터)→r3 PASS 0 issues · evaluator WARNING 9.88**(최종HEAD 재채점, P0/P1 0, P3×3 defer: 500 details 선재[diff 무변경]·page 상한 의도[소비처 미사용]·PublicAssetListItem 타입 중복 nit[WI-021 도입]) + `.pass`(fingerprint `0c66c8a5`). evaluator 변이검증 3종 실증(getAll→get 1 FAIL·invalid enum→undefined 3 FAIL·parsePageNumber→raw parseInt 2 FAIL, 원복 diff0). **두 codex fixNow P3를 매 라운드 즉시 해소**(WI-021 교훈 — 사소한 검증 우회/엣지도 defer 말고 즉시 강화). **WI-021 evaluator defer P3 2건(enum 무검증·limit 무상한) 흡수 완료.** 잔여 defer→WI-023(선택). impl `50a6e6e`/r2 `f2748a3`/r3 `0cad729`. **develop 머지 완료 (PR#24, merge `5469f99`)**. main 미승격.
- **WI-021-fix** (보안/정보위생 — assets 목록 GET 응답 lean DTO) — `GET /api/assets`(목록)가 `select`에 `prompt: true`를 포함해 raw 행을 그대로 반환하던 것을 lean 전용 DTO(`toPublicAssetListItem`)로 정형화. **핵심: 이 라우트의 `shared=true` 분기는 `where.isShared=true`로 owner 게이트 없이 타인의 공유 자산을 누구에게나 반환** → 상세(WI-019, owner-gated)보다 노출 표면이 넓다. **설계 codex consult 적출**: 상세용 `toPublicGeneratedAsset` 전체 재사용을 의도적으로 회피 — 재사용 시 (a) `metadata`가 목록에 추가돼 game-loader frameWidth 폴백(64)이 실측값으로 바뀌고(보안 수정의 부수효과), (b) `shared=true`에서 타인 `user{id,name}`·metadata(seed/generatedAt 등)가 신규 노출. 따라서 lean DTO(`id,type,name,status,filePath,thumbnailPath,createdAt,updatedAt` 8키)로 분리하고 `prompt`/`workflow`/`comfyuiJobId`(민감)·`metadata`·`user`·`userId`를 **select(쿼리단) + transform(응답단) 이중 제외**(우발적 select 확장에도 응답 키 고정). 신규 `toPublicAssetListItem`+`PublicAssetListItem`/`GeneratedAssetForListItem`(`features/assets/internal/public-asset.ts`+배럴). 무회귀(목록 소비처 2곳 실측: `asset-loader.ts`=id/type/filePath/metadata.frameWidth[현재도 metadata 미반환·64폴백이 기존 동작]·`asset-palette.tsx`=id/type/name/thumbnailPath/filePath 전부 보존). 회귀 테스트 12(`route.test.ts` 신규: owner/shared 두 분기 exact-key-set allowlist + 민감필드 미노출 + select 인자 단언 + 필터/페이지네이션 전달 + 401/500 가드). 기계게이트 4/4(tsc0/lint0[선재 LiveKit 경고1·WI무관]/vitest 192→**204·204**[+12]/build0). 설계 codex consult 1R + **듀얼 블라인드 1R 수렴: codex PASS 0 issues · evaluator WARNING 9.81**(P0/P1 0, P3x3 전부 defer: ①type/status enum 무검증[선재→WI-022] ②limit 무상한·NaN[선재→WI-022] ③PublicAssetListItem↔GeneratedAssetForListItem 중복 인터페이스 nit) + `.pass`(fingerprint `d8a1b53f`). evaluator 변이검증 2종 실증(DTO에 user+metadata 주입→3 FAIL·select에 prompt 복원→1 FAIL, 양쪽 원복 diff0). **WI-019 evaluator defer P3(목록 선재누출) 흡수 완료.** 선재 입력검증 부채는 codex consult 권고대로 **WI-022** 분리. **develop 머지 완료 (PR#23, merge `13c3b55`, impl `5cd3f85`)**. main 미승격.
- **WI-019-fix** (보안/정보위생 — assets 상세 GET 응답 allowlist) — `GET /api/assets/[id]`가 `prisma.generatedAsset.findUnique({ include })`로 raw `GeneratedAsset` 전체 행(+민감 `prompt`/`workflow`/`comfyuiJobId`)을 그대로 JSON 반환하던 것을 `select` + 공개 DTO(`toPublicGeneratedAsset`)로 정형화 (WI-014 동일 클래스, 별 도메인, owner/superAdmin 게이트 하 저위험·정보위생). **핵심(설계 codex consult 적출)**: 민감 3필드가 `metadata`(Json) 컬럼에도 중복 저장됨(`generate/route.ts:90`·`batch/route.ts:79`가 `GeneratedAssetMetadata` 전체를 JSON 저장) → top-level allowlist만으로는 metadata 우회 노출 → 순수 헬퍼 `toPublicGeneratedAsset`(`features/assets/internal/public-asset.ts`+배럴)가 top-level allowlist + `PUBLIC_METADATA_KEYS`(`width,height,frameWidth,frameHeight,columns,rows,format,seed,generatedAt,processingTime,error`)로 metadata도 정규화. `userId`는 권한판정용 select하되 응답 DTO 미포함. 무회귀(소비처 3곳 실측): `game-loader`(id/type/name/filePath/metadata)·`sprite-generator`(filePath/metadata.frameWidth·frameHeight)·`test-chibi-generation`(status/filePath/metadata.error) 필요 필드 전부 보존. 회귀 테스트 18(헬퍼 9 + 라우트 9): 정확 키집합·민감필드 미노출·metadata 우회차단·null/error/배열·select 인자 단언. 기계게이트 4/4(tsc0/lint0[선재 LiveKit 경고1·WI무관]/vitest 192·192/build0). 설계 codex consult 1R + **듀얼 블라인드 1R 수렴: codex WARNING**(P3x1 defer: metadata allowlist 테스트 self-referential 오라클 — 실누출 없음 명시 → **WI-011 교훈대로 즉시 해소**: fixture 민감 extras + 리터럴 exact-key-set 단언 + 상수 가드, 변이검증 실증[accessSecret allowlist 추가 시 4 테스트 FAIL]) **· evaluator WARNING 9.78**(P0/P1 0, P3x3 defer: 목록라우트 선재누출→WI-021·allowlist드리프트[deny-by-default 안전]·userId 미선언 nit, 변이검증 3종 전부 검출) + `.pass`(fingerprint `0b92337e`). **develop 머지 완료 (PR#22, merge `221f8df`, impl `d725837`/강화 `5fef58d`)**. 머지 후 develop tsc0/신규테스트 18 재확인. main 미승격(라이브 미반영).
- **WI-020-fix** (미들웨어 public 정적에셋 회귀 — 로그인 로고) — 미인증 시 `/Logo.png` 이 `/login?callbackUrl=%2FLogo.png` 로 307 리다이렉트 → Next Image 최적화기가 이미지 대신 리다이렉트 수신 → 로그인 페이지 로고 깨짐. WI-001 미들웨어 strict화(`startsWith("/")` no-op 제거)가 public 루트 파일까지 보호 라우트로 잡은 부작용(이번 승격으로 라이브화, 사용자 발견). 인라인 분류 로직을 순수함수 `src/lib/route-access.ts`(`isPublicRequest` + `PUBLIC_FILES=Set(["/Logo.png"])`)로 추출, 미들웨어가 호출(matcher 무변경). **확장자 전체통과(`/\.\w+$/`) 거부·명시 allowlist 채택** — codex 적대 설계 협의 권고(`/api/spaces/foo.png`·`/space/foo.png` 확장자 우회 표면 회피, deny-by-default 인증경계 보존). 회귀테스트 9(route-access.test.ts): /Logo.png 통과·대소문자/하위경로 거부·확장자 우회(페이지+API) 차단·공개 prefix exact 보존. 기계게이트 4/4(tsc0/lint0/vitest 174·174[+9]/build0). 듀얼검증 1R 수렴 **codex WARNING(P3×2 defer: _next/favicon prefix inherited·테스트 순수함수레벨)·evaluator WARNING 9.78(P3×3 defer, 변이검증 5종 전부 검출)** + `.pass`(fingerprint b0757a19). 설계 codex consult 1R. **PR#20→develop(rebase) → PR#21 승격→main `9cde274`(flowcoder25) → Vercel 재배포. 라이브 검증: `/Logo.png`+`/_next/image` 둘 다 307→200 image/png**(space.flow-coder.com+vercel.app). defer: callbackUrl open-redirect(consult 위험E·변경밖 선재·후속), allowlist 완전성 드리프트(향후 robots/og 동기화·주석 명시).
- **WI-016-chore** (마이그레이션 베이스라인 정합) — `GeneratedAsset.isShared`(+`@@index([isShared,status,type])`)가 schema.prisma:402/410엔 있으나 `0_init`엔 없던 드리프트(2026-03 `db push`로 prod 직접 추가) 정합 — `prisma/migrations`를 스키마 SoT로. **schema.prisma 무변경**(이미 isShared 보유) — 마이그레이션 파일만 추가. 신규 `20260622130000_generated_asset_is_shared/migration.sql`(`ADD COLUMN IF NOT EXISTS isShared` + `CREATE INDEX IF NOT EXISTS GeneratedAsset_isShared_status_type_idx`) + `migration_lock.toml`(`provider=postgresql`) force-add(Prisma 공식 권장, 기존 0_init/WI-013 폴더는 lock 누락이라 이번에 SoT 완전성까지 정합 — evaluator 적출). 설계 codex consult 1R: **IF NOT EXISTS 채택**(prod 컬럼 존재하나 복합 인덱스 미검증 → 일반 ADD COLUMN은 prod서 실패·`resolve --applied`는 인덱스 부재 영구 은닉하는 위험[consult 놓친위험]을 멱등 자가치유로 회피: prod 컬럼 skip·인덱스 없으면 생성·fresh 둘 다 생성) · timestamp WI-013 이후(append-only) · prod 적용 승인게이트 분리. 오프라인 검증: `migrate diff --from-schema-datamodel <isShared 제거본> --to-schema-datamodel schema --script` 델타=정확히 이 2 DDL(evaluator 양방향 재현, 0_init+WI-013 외 발산 없음). 기계게이트 5/5(tsc0/lint0[선재 LiveKit 경고1·WI무관]/vitest 165·165/next build0/prisma validate OK). 소스(.ts/.tsx/.prisma) 무변경 → 런타임 무회귀, Stop fingerprint empty(changed=0). **듀얼검증 1R 수렴: codex WARNING**(유일 P3는 codex 샌드박스 EPERM 환경문제·파일결함 아님·오케스트레이터 게이트로 충족) **· evaluator WARNING 9.83**(P3×2 defer: IF NOT EXISTS 정의동일성 미보장[prod \d 확인 권고]·AssetWorkflow.id format nit[선재·스코프밖]) + `.pass`(fingerprint empty `e3b0c44`). **prod 적용은 WI-013과 함께 사용자 승인 게이트(미실행).** **develop PR 대기**(impl `3e8705b`).
- **WI-015-fix** (안정성 — Phaser tween 생명주기 누수) — **2026-06-22 승격前 통합감사 phaser-lifecycle 축 발굴(2건)이 듀얼검증 3R에서 동일 클래스 5곳으로 확장 해소.** 핵심 원리: **Phaser는 GameObject.destroy() 시 그 대상을 가리키는 tween을 자동 제거하지 않음** → destroy에서 `killTweensOf` 명시 필요. (1) `RemotePlayerSprite.destroy()`: `killTweensOf(this/sprite/nameText)` — jump tween(targets:this) + moveTo tween(sprite/nameText). (2) `LocalPlayer.destroy()`(실 프로덕션 점프/이동 호출자, 고영향): `killTweensOf(this/jumpState/sprite)` — startStep(this)·jump 궤적(jumpState)·스케일(sprite). (3) `InteractiveObject.destroy()`: `killTweensOf(indicator)` 추가 — showIndicator의 `repeat:-1` 무한 tween이 object-manager 직접 destroy 경로(setNearby(false) 미경유)서 누수. (4) `MainScene.onAssetGenerated`: `if(!this.scene.isActive(SCENE_KEYS.MAIN)) return` 가드(BootScene:71 패턴 미러) — shutdown 중 ASSET_GENERATED 비동기 발화 시 비활성 씬 로더 손상 차단. game/ 전체 `tweens.add`는 위 3파일에만 존재 → 동일 클래스 소진 확인. 테스트 3파일 신규(remote/local/interactive-object, killTweensOf 대상 변이검증). vitest 157→165(+8). 기계게이트 4/4(tsc0/lint0 --max-warnings 0/vitest 165·165/build0). **듀얼검증 3R 수렴: codex r1 FAIL(P2×2: moveTo 미정리+LocalPlayer) → r2 FAIL(P2: interactive-object 무한 tween) → r3 PASS 0 issues · evaluator 9.46→9.75→WARNING 9.78**(P3×2 defer: LiveKit 선재 경고·glowTween killTweensOf 심층방어) + `.pass`(fingerprint `64feec04`). **두 검증자가 매 라운드 동일 클래스 새 인스턴스를 독립 수렴 적출** — 감사 2건이 5곳으로 확장. **develop PR 대기**(impl `57475c7`/r2 `fe302d3`/r3 `33cdbb0`).
- **WI-014-fix** (보안/정보위생 — 응답 allowlist) — POST `/api/spaces`·PATCH `/api/spaces/[id]`·PATCH+GET `/api/spaces/[id]/(admin/)members`가 전체 Prisma 객체/`inviteCode`를 반환하던 것을 GET 경로의 allowlist 원칙(app.md 불변식 #2)과 정합하도록 정형화. **2026-06-22 승격前 통합감사(9축 멀티에이전트+적대검증) 발굴.** (1) POST 응답 `inviteCode` 제거 + create `include`→`select{id,name,template}`(민감 스칼라 미fetch). (2) PATCH `[id]` `update`에 select allowlist(accessSecret/inviteCode/ownerId/templateId/mapData 미반환). (3) PATCH `[id]/members` `update` select{id,role,restriction}(restrictedBy/Reason/Until·userId·guestSessionId 미반환). (4) PATCH+**GET** `[id]/admin/members`: PATCH update select{id,role,restriction}; **GET findMany `include`→`select`**(restricted*·guestSessionId·spaceId·updatedAt 미반환, `userId`는 media-management 스포트라이트 선택용 보존=user.id 동등·신규누출 아님). 4개 응답 모두 소비처 본문 미사용(create-space-form·space-settings-form·member-table는 res.ok만, [id]/members PATCH 호출처 부재)→무회귀. PATCH/GET select arg exact-key-set 단언 회귀 테스트 3파일 신규 + POST 갱신(vitest 147→157). 기계게이트 4/4(tsc0/lint0/vitest 157·157/build0). **듀얼검증 2R 수렴: codex r1 FAIL(P1 fixNow: GET admin/members raw row가 member-table onRefresh GET로 PATCH 정형화 무력화) → r2 PASS 0 issues · evaluator r1 9.72 → r2 WARNING 9.79**(P3×2 defer: ① POST create select arg 미단언[응답 키집합 단언이 실제 누출차단 커버, 심층방어 공백] ② assets/[id] GET raw row[동일 클래스·별 도메인·owner-gated, 범위 밖→WI-019]) + `.pass`(fingerprint `2ac6070e`). **두 검증자 모두 r1 GET 무력화 결함 코드로 타당 확인 → r2 GET까지 정형화로 해소.** **develop PR 대기**(impl `e5397fc`/r2 `45b67ae`).
- **WI-013-perf** (DB 인덱스/쿼리플랜) — `GET /api/spaces` 핫 쿼리(3 scope 전부 `WHERE status=ACTIVE` + `ORDER BY updatedAt DESC, id DESC` + cursor)의 정렬을 복합 인덱스로 지원. WI-010 듀얼검증 공통 defer 해소. `prisma/schema.prisma` Space: `@@index([status])` 제거 → `@@index([status, updatedAt(sort: Desc), id(sort: Desc)])` + 마이그레이션 `20260622120000_space_hot_query_index`(CREATE→DROP 순, `git add -f` — `prisma/migrations/` gitignore·0_init force-add 선례). 설계 codex consult D1~D5: **단일 복합**(owned/joined 별도 인덱스 보류 — owned 저카디널리티·joined 세미조인 한계, prod EXPLAIN 후속) · **Prisma 네이티브 full**(drift 없음) · **redundant `status` 인덱스 제거**(복합 선두가 prefix 커버, grep으로 `prisma.space` status-only 쿼리 부재 확인 — 핫쿼리 1곳뿐) · **prod는 `CONCURRENTLY` via direct(비-PgBouncer) connection**(트랜잭션 내 불가→운영 절차 분리). 로컬 검증: `prisma validate`/`migrate diff`(SQL↔schema end-state 정합)/기계게이트 5/5(prisma generate 0/tsc 0/lint 0/vitest 147·147/next build 0). 인덱스 변경은 client 타입·런타임 무변(무회귀). **듀얼검증 1R 수렴 codex PASS 0 issues·evaluator WARNING 9.78**(P3×2 defer: ① migration DROP→CREATE 순서→`eca0ff2`에서 CREATE→DROP 정렬 해소[트랜잭션/CONCURRENTLY 양쪽 인덱스 무공백], ② D2 joined 인덱스 보류=의도된 prod-gate 후속) + `.pass`(`c206e20b`). **prod 적용 + 프로덕션 EXPLAIN(cursor seek 효율, codex 지목)은 사용자 승인 게이트(미실행).** 스펙: `.claude/specs/architecture/2026-06-22-space-hot-query-index.md`. **develop 머지 완료 (PR#14, merge `f8f4ede`, impl `5a45b6d`/r2 `eca0ff2`)**.
- **WI-012-2-refactor** (cleanup/EventBridge·recorder·DRY·커버리지) — WI-012 분할 2/2(codex consult D6). S3~S6 저위험·독립, **server/ 무관**. **S3 EventBridge 공개계약 단일 진입점**: 최상위 `game/index.ts` 배럴이 `game/events` payload 타입을 4개만 재노출하던 것을 전체 30개 명시 재노출 + cross-module 소비자(editor `editor-system`/`use-editor`, avatar `sprite-generator`)를 events 서브배럴 우회 import → 최상위 배럴(`@/features/space/game`)로 통일 + ESLint `no-restricted-imports`에 `**/game/events`·`**/game/events/**` 차단 추가(공개 진입점 강제, game 내부 상대 `../events`는 game 세그먼트 없어 미매칭·오탐0). **S4 `useScreenRecorder` `audioContext.close()` `.catch` 통일**(WI-006 흡수): close() 4곳 중 `.catch` 없던 L263(startRecording)·L357(stopRecording)에 `.catch(() => {})` 추가(unmount/onerror와 동일 no-op, 반환 `Promise<void>` 계약 무변) + AudioContext 스텁 변이검증 테스트 2건(`process.on("unhandledRejection")`로 누수 검출 — `.catch` 제거 시 FAIL 실증). **S5 messages 라우트 cursor DRY**(WI-010 흡수): 비-admin(`[id]/messages`)·admin(`[id]/admin/messages`) 2라우트 인라인 cursor/limit/절단을 `src/lib/pagination.ts`(`parsePageLimit`/`buildCursorPage`) 통일. 응답 계약 보존(비-admin `{messages 매핑,nextCursor,hasMore}` 변환 유지, admin `{messages,nextCursor}` hasMore 미추가=무변경 codex D5) + 라우트 회귀 테스트 11(하니스 재사용). **S6 POST /api/spaces 커버리지**(WI-011 흡수, **테스트 전용**·`route.ts` 무변경): 403만 커버하던 POST에 201(+create ownerId/OWNER 인자)/401/400(code 부재)/404/500 + GET500 폴백 6건. vitest 128→147(+19). 기계게이트 4/4(tsc 0/lint 0/vitest 147·147/next build 0). **듀얼검증 1R 수렴 codex PASS 0 issues·evaluator WARNING 9.8**(P3×1 defer: `parsePageLimit` 통일의 limit 파싱 엣지 변동=strict improvement[구 take:NaN/음수 잠재버그 제거], fixNow:false) + `.pass`(`16c001a0`). 양측 변이검증 독립 실증(evaluator: S3 ESLint probe로 game/events 차단+game-internal 미오탐 확인, S4 L361 `.catch` 제거 FAIL 확인, S5 응답 exact-key allowlist+admin hasMore 미노출 lock, S6 `route.ts` diff 빈결과). 설계는 WI-012 통합 codex consult(D3~D5)에서 합의. **develop 머지 완료 (PR#13, merge `986747a`, impl `f3058be`)**.
- **WI-012-1-refactor** (경계/통신 순수계약 SSOT) — WI-012(6서브스코프)를 위험 프로파일로 분할(codex consult D6)한 **1/2**. S1 protocol 모듈 분리 + S2 소켓상수 물리이동. **순수 리팩토링 — 런타임 동작 변경 0.** (1) 신규 `src/features/space/protocol`: `internal/socket-events.ts`(이벤트/페이로드 타입, 구 `socket/internal/types.ts` verbatim) + `internal/socket-constants.ts`(`RECONNECTION_*`/`MOVE_THROTTLE_MS`, 구 chat-constants 오배치분) + `index.ts` 순수 배럴(타입/상수만, React/Node 무의존). (2) `socket/internal/types.ts` 삭제 → `socket/index.ts`가 `@/features/space/protocol`에서 facade 재노출(타입 소비자 4개소 무변경). (3) `socket-client`/`use-socket` 소비처를 protocol 배럴로(`DEFAULT_NICKNAME`만 chat 잔류 — codex D2). (4) `chat-constants`/`chat/index.ts` 소켓상수 정의·WI-003 임시 re-export 제거. (5) server 9개소(index + 8 handlers) import 경로를 `protocol/internal/socket-events`로. (6) `Dockerfile.socket` COPY + `deploy-socket.yml` paths 갱신. enforce 계약은 이미 독립 안정화(WI-005)되어 잔류(codex D1). **추가 선재 P1 해소**: `server/middleware/auth.ts`가 런타임 import하는 `src/lib/auth-secret.ts`(WI-001 `80a3adb` 도입)가 `Dockerfile.socket` COPY 누락이던 것 보강(`import type` 아닌 런타임 import라 Docker 빌드 esbuild resolve 실패 → 향후 main 승격 시 OCI 빌드 깨질 결함). **esbuild metafile로 서버 번들 런타임 src 입력 전수(chat-constants·enforce contract·auth-secret 3개) COPY 완전성 실증**(socket-events.ts는 type-only라 esbuild elide → 번들 미포함, 계약 추적용 COPY 유지). 기계게이트 5/5(tsc 0/lint 0(기존 LiveKit 경고 1)/vitest 128·128/next build 0/**서버 esbuild 번들 0**) + esbuild metafile. **듀얼검증 codex 3R 수렴**(r1 WARNING P3-doc stale포인터 → r2 FAIL P1-authsecretCOPY+P3-원장불일치 → r3 **PASS 0 issues**) · **evaluator 10.0**(run1 3dac762 PASS 10.0 → run2 최종HEAD d028764 WARNING 10.0 P3-스펙표[806d07b 해소], metafile로 COPY 완전성 독립 실증) + `.pass`(fingerprint `2fc1cc67`). 설계 codex consult 1R(D1~D6 + 놓친위험: protocol 순수성 유지). 스펙: `.claude/specs/architecture/2026-06-22-protocol-contract-module.md`. **develop 머지 완료 (PR#12, merge `84ea3d8`, impl `3dac762`)**. 남은 S3~S6 → WI-012-2-refactor.
- **WI-011-test** (테스트 부채/회귀 가드) — `GET /api/spaces` filter 분기 회귀 테스트 + 재사용 API 라우트 테스트 하니스 신규 도입 + `useScreenRecorder` unmount 경로 테스트(WI-006 evaluator P3 흡수). **테스트 전용 — 소스(.ts) 무수정**(양 검증자 `git diff HEAD -- src/` 빈결과 확인). (1) 신규 하니스 `src/__tests__/helpers/api-route.ts`(순수 빌더 `buildGetRequest`/`buildJsonRequest`/`makeSession`/`makeSpaceRow`/`readJson`; `vi.mock` 프리앰블은 vitest 파일로컬 호이스팅이라 중앙화 불가 → 빌더만 공용화, 설계 codex consult 합의). (2) `src/app/api/spaces/route.test.ts` 17케이스: filter→scope(owned/joined/null·all × 일반/슈퍼어드민 — `filter=all`+일반사용자가 전역 아닌 멤버십 scope로 제한되는 **권한 격리** 핵심 + 슈퍼어드민 owned/joined도 전역화 안 됨), INVALID_FILTER 400 + prisma 미접근, status:"ACTIVE" 상시 부착, 페이지네이션(take=limit+1·cursor:{id}·skip:1·trimming), cursor에도 scope 보존, 응답 **exact-key-set allowlist**(inviteCode/accessSecret/ownerId/templateId/updatedAt 미노출), POST superAdmin 가드 403. (3) `useScreenRecorder.test.ts` unmount 3케이스: stopping 중 언마운트 시 cleanup이 `pendingStopResolveRef` settle(WI-006 영구 pending 흡수, 변이검증), 언마운트 후 stopRecording 즉시 resolve, saveFile await 중 언마운트 시 mountedRef 가드(React 19라 setState-after-unmount 관측 불가 → 가드가 차단하는 `showNotification` setTimeout을 고유 delay로 식별해 변이검증 가능하게). vitest 108→128(+20). 기계게이트 4/4 PASS(tsc 0/lint 0(기존 LiveKit 경고 1)/vitest 128·128/next build 0). 설계 codex consult 1R(하니스 분리·POST 1건 제한·status 검증 추가·민감필드 fixture) + 듀얼검증 2R: **codex r1 WARNING P3x3 → r2 PASS 0 issues**(가드 false-pass·superAdmin scope·allowlist 3건 전부 해소 확인) · **evaluator r1 9.3 → r2 9.625**(3건 해소 변이검증 실증, 잔여 P3x3 전부 defer=WI-012). 두 검증자 r1에서 'mountedRef 가드 smoke false-pass' 동일 수렴 발견 → 선제 강화. + `.pass`(fingerprint cfdd572c). 남은 P3(POST 전체 커버리지·하니스 미사용 경로·GET500 폴백)→WI-012. **develop 머지 완료 (PR#11, merge `2b91419`, impl `a066359`/r2 `baa2f0c`)**.
- **WI-010-perf** (스케일/페이지네이션) — `GET /api/spaces`가 페이지네이션 없이 전체 ACTIVE 스페이스를 `findMany`로 로드하던 스케일 부채(P3, WI-009 슈퍼어드민 전역 `scope={}`에서 무한정 증가) 해소. (1) cursor 페이지네이션: `parsePageLimit`(기본 50·최대 100, NaN/0/음수/초과 정규화) + `cursor`, `take: limit+1` 후 `buildCursorPage`로 `hasMore`/`nextCursor` 도출, 응답 `{ spaces, nextCursor, hasMore }`(기존 `spaces` 유지 = 하위호환). (2) 정렬 안정성: `orderBy: [{ updatedAt: "desc" }, { id: "desc" }]` — updatedAt 가변·비유니크라 id(PK) 타이브레이커로 동일 시각 내 결정적 cursor 순서(`cursor:{id}`, `skip:1`). (3) 모든 스코프 일관 적용. (4) 클라: `space-store`에 `nextCursor`/`hasMore`/`isLoadingMore`/`loadMore` + `_reqId` 토큰으로 stale 응답 무시(필터 변경 중 이전 페이지 append 차단), `space-list-view` "더 보기" 버튼(슈퍼어드민 전역 가시성 회귀 방지). 신규 순수 헬퍼 `src/lib/pagination.ts`(Prisma/React 무의존) + 테스트. vitest +16(92→108). 기계게이트 4/4 PASS(tsc 0/lint 0/vitest 108·108/next build 0). **듀얼검증 codex 3R 수렴**(r1 **FAIL** P2/fixNow: 필터 변경 중 loadMore 무효화 시 `isLoadingMore` 고착 → "더 보기" 영구 비활성 → fetchSpaces 시작 시 `isLoadingMore:false` 선제 해제로 수정[`529108e`] / r2 WARNING P3: 역방향 누수[리로드 중 loadMore → `isLoading` 고착] → loadMore 가드에 `isLoading` 추가로 선제 해소[`ed73930`] / r3 **WARNING** P3×1 defer) · **evaluator WARNING 9.7**(최종 HEAD 재검증, P3×4 전부 defer, 변이검증 5종 전부 검출[두 수정 가드 모두 테스트 보호 실증], 보안 sound[scope가 cursor보다 우선 → 권한 밖 노출 없음]) + `.pass`(fingerprint `2ef1dd0b`). 설계 codex consult 1R(updatedAt+id 유지/limit 50/클라 UI 포함/race 가드 — 위험 E 경쟁상태 지목). 핵심: **두 검증자가 r1에서 동일 실결함 독립 발견**. 남은 P3(복합 인덱스→**WI-013-perf** / cursor 검증·DRY messages 라우트→WI-012). **develop 머지 완료 (PR#10, merge `47022a6`, impl `ed73930`)**.
- **WI-006-fix** (안정성/Promise 종결) — `useScreenRecorder`의 `recorder.onerror`가 handleError + setRecordingState("idle") + stopTimer()만 하고 `stopRecording()`이 보관한 `pendingStopResolveRef`를 settle하지 않아, MediaRecorder 'error' 이벤트 후 `onstop`이 발화되지 않으면 `stopRecording()`이 반환한 Promise가 영구 pending이던 P3 결함 해소. `onerror`를 '정상 abort'로 강화: (1) `onstop=null`(error 우선, 이후 onstop 중복 저장/처리 차단), (2) `audioContext close` + `chunks 폐기`(실패 녹화 자원 회수), (3) `mediaRecorderRef.current===recorder`일 때 null(idle인데 errored recorder 참조 불일치 해소), (4) `pendingStopResolveRef` settle(영구 pending 차단). resolve 시맨틱은 기존 `onstop`의 `result.status==="error"` 경로(reject 아닌 resolve)와 일관 — 오류는 `error` 상태/`onError` 콜백으로 전달, `Promise<void>` 계약 유지(호출부 `ScreenShare.handleToggleRecording`는 await 후 후속 UI 없음 → 회귀 무). 신규 vitest 4(88→92, onerror settle/onstop=null/즉시 resolve/정상 onstop 회귀). 기계게이트 4/4 PASS(tsc 0/lint 0/vitest 92·92/build 0). 클라이언트 훅만 변경 — 서버 esbuild 무관. 듀얼검증(codex **r1 PASS 0 issues**·evaluator WARNING 9.5, P3×3 전부 defer: unmount settle 테스트부재→WI-011 / onerror mountedRef가드→실결함 아님(cleanup이 onerror=null) / stopRecording close `.catch` 누락→선재 범위밖) + `.pass`(fingerprint `19ecaf43`). 설계 codex consult 1R. evaluator 뮤테이션 테스트 실증(pre-fix 복원 시 test1·3 타임아웃 FAIL). **develop 머지 완료 (PR#9, merge `2644b3c`, impl `5c9fa16`)**.
- **WI-005-fix** (보안/실시간추방) — 접속 중 dashboard 제재(ban/kick/mute/unmute/changeRole)가 DB만 갱신하고 별 호스트의 socket.io(OCI 단일 인스턴스, Redis 없음)의 살아있는 연결엔 무영향이던 P2 결함 해소. ban=재접속까지 채팅/이동 지속, kick=row 삭제돼도 소켓 잔존, **changeRole 강등 시 인메모리 role이 admin으로 남아 권한 회수 실패**(codex consult 최대 위험 지목)가 근본 결함. 크로스프로세스 채널 = socket httpServer 내부 `POST /internal/enforce` webhook. 보안 다층: `SOCKET_INTERNAL_SECRET` HMAC-SHA256(`{ts}.{body}`) 서명(비-hex/길이불일치/홀수 선제거 후 timingSafeEqual, 시크릿 없으면 503) + replay window(±30s) + byte size limit(4096) + 스키마 검증 + **DB postcondition 재확인**(ban=BANNED/kick=row null/mute=MUTED/unmute=NONE/role=role) 후 소켓 조작(시크릿 유출·핸들러 버그→임의추방 확대 차단). ban/kick은 **detachSocketFromSpace로 동기 권한 회수**(자기 id 제외 모든 room[공간+파티] 이탈 + socket.data 무효화 → 전 핸들러 spaceId 가드로 grace 동안 잔존권한 0) 후 grace(250ms) 물리 disconnect, 주변엔 member:kicked + player:left 사용자 단위 1회(removeUserPresence). leave:space도 클라 인자 무시(invariant #3)·leaveSpace가 socket.data 무효화하도록 하드닝. Next PATCH는 DB 갱신 후 dispatchEnforcement await(serverless fire-and-forget 금지, 2s timeout, throw 없이 realtimeEnforced). 경계: enforce 독립 feature 모듈(순수 contract.ts + Next측 dispatch.ts), server는 배럴 우회해 contract.ts만 상대 import/COPY. SOCKET_INTERNAL_SECRET은 AUTH_SECRET과 분리. 신규 vitest 17(71→88). 기계게이트 5/5 PASS(tsc 0/lint 0/vitest 88·88/next build 0/서버 esbuild 번들) + 듀얼검증 **codex 3R 수렴**(r1 FAIL P1 grace-window → r2 FAIL P1×2 party잔존+leave경로 → r3 **PASS 0 issues**)·evaluator WARNING 9.62(P3×3 전부 defer: rate-limit→WI-012/replay nonce→멱등무해/소켓통합테스트→WI-011) + `.pass`(fingerprint `aa917d91`). 설계 codex consult 1R + 검증 codex 3R. **develop PR 대기**(commit `e8e714c`).
- **WI-004-fix** (보안/경로격리) — `DELETE /api/assets/[id]` path traversal(CWE-22, P2) 차단. DB 저장 `filePath`/`thumbnailPath`를 검증 없이 `join(cwd,"public",path)` 후 `unlink` → 오염 경로(`../`/절대경로/백슬래시/null byte) 주입 시 `public` 밖 임의 파일 삭제 가능했던 문제. 신규 `resolveGeneratedAssetPath()`(`features/assets/internal/safe-path.ts` + 배럴)로 `public/assets/generated/` 경계 격리: 백슬래시/null byte 즉시 거부 + POSIX URL 정규화로 `..` 평탄화(호스트 OS 무관) + generated prefix 검사 + `path.relative` 이중 검사. DELETE 핸들러는 해석 성공 시만 unlink, 경계 밖이면 스킵+`console.warn`, **DB row 삭제는 그대로 진행**(오염 row 정리). vitest 18케이스 신규(정상 5+차단 13, 53→71). 컨테인먼트 루트는 설계 codex consult에서 내 `public/` 초안 대비 `public/assets/generated/`로 타이트화(전 write 경로가 generated로 귀결 실증→무회귀) + null byte/백슬래시 거부 보강. 기계게이트 4/4 PASS(tsc 0/lint 0/vitest 71·71/build 0) + 듀얼검증(codex **PASS 0 issues**·evaluator WARNING 9.62, P3×3 전부 defer: %2e%2e 무해·relative이중검사 방어중복·generated밖 정상자산 부재) + `.pass`(fingerprint `9fae5a70`). write-side(processor/register-characters) 중앙화는 WI-012 후속. 설계 codex consult 1R + 검증 codex 1R PASS. **develop 머지 완료 (PR#7, merge `67531b4`, impl `bc334a7`)**.
- **WI-003-refactor** (경계/캡슐화) — 타 모듈 `internal/*` 직접 import 위반 정리. 인앱 cross-module internal import 7건을 배럴 routing으로 해소: editor↔game(game 배럴에 `TILE_INDEX`/`extractDefaultMapData`/`TilemapResult`, editor 배럴에 `EditorSystem` — MainScene `await import` lazy 로드라 정적 순환 없음), socket→chat(chat 배럴에 `MOVE_THROTTLE_MS`/`RECONNECTION_*`), stores/editor-store→editor 배럴. 재발 방지 이중 방어: ESLint `no-restricted-imports`(`@/**/internal[/**]` alias 정밀, 오탐0) + `src/__tests__/module-boundaries.test.ts`(import 경로 실해석으로 alias+상대경로 cross-module internal 정밀 차단, vitest 게이트 강제·음성검증 완료). 서버(별도 esbuild 번들, 배럴 import 시 React 끌려와 빌드깨짐)·scripts dev툴의 순수 계약/internal import는 범위 밖→WI-012-refactor. 기계게이트 5/5 PASS(tsc/lint/vitest 53·53/build/서버 esbuild 번들) + 듀얼검증(codex 4R 최종 PASS·evaluator WARNING 9.5, P3×4 전부 defer) + `.pass`(fingerprint fc768c9d). 설계 codex consult 1R + 검증 codex 4R 수렴. 스펙: `.claude/specs/architecture/2026-06-22-module-boundary-encapsulation.md`. **develop 머지 완료 (PR#6, merge `edd2918`, feat `58c9025`)**.
- **WI-008-fix** (정리/일관성) — WI-007 듀얼검증 defer P3×2 해소. (1) `scripts/set-super-admin.mjs` 회수 인자 화이트리스트화: `argv[3] !== "false"` 파싱이 오타('flse'/'False'/'0')를 조용히 부여로 처리하던 문제 차단 → `flagArg`를 'true'/'false'만 명시 인정, 그 외/잉여 인자는 usage 출력 후 exit 1. `--list`/무인자 분기 최우선 유지 + 기본 부여(인자 없음→true) 계약 보존(codex consult 반영). (2) `POST /api/spaces` 403 응답에 `code: "SUPER_ADMIN_REQUIRED"` 병기(app.md invariant #4 정합). 기계게이트 4/4 PASS(tsc/lint/vitest 52·52/build) + 듀얼검증(codex PASS·evaluator WARNING 9.875, P3×3 전부 defer: 2건 해소검증기록·1건 범위밖 선재엣지) + `.pass`(fingerprint 6a288939). 설계 codex consult 1R. 변경 2파일(scripts/set-super-admin.mjs, src/app/api/spaces/route.ts). **develop PR 대기**(commit `e19d8be`).
- **WI-009-feat** (접근제어/UX) — 슈퍼어드민 전역 스페이스 뷰. `GET /api/spaces` filter 분기 명시화(owned/joined/all|null) + 슈퍼어드민 "전체"=모든 ACTIVE(`scope={}`, 일반 사용자 동작 불변) + 미허용 filter 400(`code:INVALID_FILTER`) + 목록 응답 `inviteCode` 제거(죽은 필드·전역 누출 차단). `SpaceCard`/`SpaceListView` `isSuperAdmin` prop → myRole 무관 '관리' 노출(서버 세션 권위, 클릭 후 `requireSpaceAdmin` 재검증). `space-store` 죽은 `inviteCode` 필드 제거. 기계게이트 4/4 PASS + 듀얼검증(codex PASS·evaluator WARNING 9.8, P3×2 defer→WI-010-perf/WI-011-test) + `.pass`(fingerprint 2dedbb42). 설계 codex consult 1R. **develop 머지 완료 (PR#4, merge `08fc2b9`, feat `fb484d6`)**.
- **WI-007-feat** (접근제어) — 스페이스 생성 슈퍼어드민 전용 제한 + 슈퍼어드민 부트스트랩 스크립트. 생성 진입점 전수 게이팅(navbar 데스크톱/모바일 + my-spaces toolbar/empty-state) + `POST /api/spaces` 403 + `/spaces/new` 서버 redirect. 기계게이트 4/4 PASS(**main 베이스 자기완결성 실증**) + 듀얼검증 2R(codex PASS·evaluator WARNING 9.85, P3 2건 defer→WI-008) + `.pass`. **타깃 base=`main`** (사용자 지시, 별도 작업): feat `49d272e` → **PR#3 → main `2a6e2ed`**. back-sync develop 완료(`d8495f9`). **라이브 프로덕션 배포 완료** `38459d5`(작성자 인가 계정 재트리거). 스펙: `.claude/specs/auth/2026-06-21-superadmin-space-creation.md`.
- **WI-002-fix** (안정성) — Phaser 씬 생명주기 cleanup 미연결 누수 + useScreenRecorder unmount 정리. 기계게이트 4/4 PASS + 듀얼검증 3라운드(codex PASS·evaluator WARNING 9.6, P3 1건 defer→WI-006) + `.pass` 생성. 구현: MainScene `create()`에서 SHUTDOWN+DESTROY once 연결 + 멱등 가드 + 반대편 리스너 해제; useScreenRecorder useEffect cleanup + mountedRef 가드 + pendingStopResolveRef. **develop 머지 완료 (PR#2, merge `256ae57`)**.
- **WI-001-fix** (보안) — 인증/인가 우회·데이터 노출 8건 차단. 기계게이트 4/4 PASS + 듀얼검증(codex PASS·evaluator PASS 9.375) + `.pass` 생성. 구현: join 동기인가, members IDOR, GET select allowlist, 멤버 PII 게이트, guest PASSWORD, middleware exact, AUTH_SECRET fail-closed, 역할계층 canActOn. **develop 머지 완료 (PR#1, merge `f8e7ba2`)**.
