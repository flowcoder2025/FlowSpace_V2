# HANDOFF

## ✅ develop→main 승격 완료·라이브 반영 (2026-06-23, PR#44 rebase main HEAD `150428d`)
**사용자 "develop→main 승격 진행" 선택**(AskUserQuestion, codex 권고 순서 WI-041→승격). **승격 델타 = WI-026/033/034/035/036/037/038/039/041(9건)** — 직전 승격(PR#34=WI-029~032) 이후 누적. **이번 승격은 server/(WI-036 enforce.ts/room.ts)+prisma/(WI-036 Space.deletedBy) 포함 = OCI 재배포+prod DB 마이그레이션 동반**(직전 PR#34는 Vercel 전용·no-op이라 가벼웠음).

**codex readiness = 조건부 GO**: 코드 정합성 GO(role SoT `SpaceMember.role` 일관·권한모델·보안 sound). **핵심 위험 적출**: 직전 권고 순서 `prod DB→main→OCI`는 NO-GO 근접 — **구 OCI는 `archive` enforce action을 하위호환 무시 안 하고 400 거부**(구 contract=ban/kick/mute/unmute/role·userId 필수) → 새 Vercel이 archive 보내면 구 OCI 거부 → DELETE 성공(DB ARCHIVED·신규 join 차단)이나 **접속자 추방/detach/purge 누락**. → **OCI 선배포(역호환)로 창 차단**(새 OCI는 구·신 action 모두 처리).

**실행(안전 순서)**: (1) **prod DB** — `Space.deletedBy` 마이그레이션 적용. ★**Supabase 직결(5432) IPv4 폐기→IPv6 전용**으로 `prisma migrate deploy`(directUrl) P1001 불가(직전 승격에선 작동) → **transaction pooler(6543·DATABASE_URL)로 `@prisma/client $executeRaw` 멱등 DDL(`ADD COLUMN IF NOT EXISTS`) 직접 적용 + `_prisma_migrations` 기록**(기존 3개 마이그레이션 checksum을 로컬 파일 sha256과 대조해 prisma 알고리즘 검증 후 삽입 — col BEFORE=false→AFTER=true). (2) **OCI 선배포** — `~/.ssh/flowspace-oci`로 SSH → 롤백 태그(`rollback-pre-promo`) → `git checkout <develop-SHA>`(detached) → build(컨테이너 무중단) → rm→up(v1 recreate 버그 우회) → healthy. 새 이미지 `91d1622f`(WI-036 archive 코드 확인). (3) **main rebase 머지**(PR#44, author=flowcoder25 보존→Vercel 인가). (4) main push 자동배포가 **detached HEAD에서 `git pull origin main` `set -e` 조기 실패**(컨테이너 미변경=안전)→ OCI `git checkout main && reset --hard origin/main` 정렬 + `workflow_dispatch`로 **CD success(30s) 재확인**. (5) **Vercel Production ● Ready**(`flowspace-v2-42jofa0l4`, 2분). (6) **라이브 스모크**: web `/` 200·`/login` 200·`/api/users/me` 307 / **socket auth probe connect 성공**(jose HS256 로컬 AUTH_SECRET 44자 서명→라이브 OCI 접속, sid=aBPTiO6fr2 = 새 OCI 코드 인증 수락·미파손). (7) **develop back-sync**(reset --hard origin/main, develop==main==`150428d`).

**✅ SOCKET_INTERNAL 활성화 완료(2026-06-24, 승격 후속)**: 사용자 "버셀 인증 띄워봐 flowcoder25@gmail.com 인증해줄게" → **현 CLI 계정 `flowcoder25-1055`이 이미 FlowCoder 팀 env 쓰기 권한 보유 확인(재인증 불요)**. **Vercel**(`--scope flowcoder`): `SOCKET_INTERNAL_URL`=`https://space-socket.flow-coder.com`+`SOCKET_INTERNAL_SECRET`(64hex) 추가→`vercel redeploy`(새 env 주입·`ed12391rj` ● Ready·`space.flow-coder.com` alias). **OCI** `.env`: 동일 시크릿 추가(`.env.bak-preenforce` 백업)+컨테이너 재생성(`env_file:.env` 런타임 주입·degrade 경고 사라짐). **검증**: 합성 enforce probe — 유효 HMAC→**409**(postcondition만 거부·fake space 무영향=시크릿 일치)·틀린 시크릿→**403**(검증 실동작). → **WI-005 실시간 제재 + WI-036 archive 즉시추방 prod ACTIVE.** 교훈: **Vercel env 쓰기 전 `vercel env add`로 권한 먼저 테스트**(현 계정이 이미 팀 member라 재인증 불필요였음)·**env 추가는 `vercel redeploy`로 기존 배포 재빌드해야 함수에 주입**(추가만으론 현 배포 미반영). **🟡 잔여(비차단)**: 인증 archive/mute **e2e 심층 스모크**(실제 로그인→입장→삭제/mute→접속자 끊김 관측, 자격증명+다중 클라이언트 필요). 이제 enforce 활성이라 실제 추방까지 작동 예상 — 사용자 라이브 검증만 남음.

**롤백**: Vercel 이전 배포 promote / OCI `flowspace-v2_socket:rollback-pre-promo` / main revert PR. 산출물 `.flowset/promotion-readiness.md`. **교훈**: (a) **Supabase 직결 IPv6 전용 전환 → prisma migrate deploy 불가, pooler `$executeRaw` 멱등 DDL + checksum 검증 기록이 안전 우회**(기존 마이그레이션 checksum 대조로 알고리즘 입증). (b) **server 변경 동반 승격은 OCI를 main 머지보다 먼저 배포**(구 OCI가 새 enforce action 400 거부 — 역호환 방향은 OCI-first). (c) **OCI 선배포 detached HEAD는 자동배포 1회 실패시키나 `set -e`로 컨테이너 무중단**(안전), 머지 후 main 정렬+workflow_dispatch로 CD 재확인. **다음 코드 WI(codex 권고 순서): WI-042(대시보드 archived 가드)·WI-040(귓속말 발견성·저가치)·WI-043(공용 Dialog/focus-trap a11y).**

## 직전 Active — ✅ WI-041-fix (설정 편집 권한 정합 — STAFF 읽기전용 폼) develop 머지 완료(2026-06-23, PR#43 merge `821c6d9`, impl `070b371`)
**큐 분기점에서 codex 협의(2R)로 다음 작업 선정 → WI-041 처리.** 직전 broad consult: 사용자 기능 요청 큐 7/8(WI-040만 남음·DM 이미 완전구현이라 저가치) + develop 8 WI 누적 미승격(server/prisma 포함). codex 권고 우선순위 = **WI-041 → develop→main 승격 → WI-042 → WI-040 → WI-043**. WI-041(STAFF "보이는데 저장 실패" 실사용 혼란)을 승격 전 짧게 닫고 바로 승격 권고. WI-041은 내가 완결 가능한 코드 WI, 승격은 사용자 승인 게이트(prod DB+OCI)라 별도 → **WI-041 먼저 처리**.

**WI-041 결함(코드 실측)**: `src/app/dashboard/spaces/[id]/settings/page.tsx`(서버 컴포넌트)는 `requireSpaceAdmin(id)`로 게이트해 OWNER/STAFF/superAdmin 모두 진입하나, 저장 라우트 `PATCH /api/spaces/[id]`(route.ts:94)는 `if (space.ownerId !== session.user.id && !session.user.isSuperAdmin) return 403`으로 **owner/superAdmin만** 허용 → STAFF가 설정 폼 보고 값 바꿔 저장 시 403("Forbidden") "보이는데 저장 실패" 혼란.

**설계 codex consult 1R 수렴**(consult-wi041.md): (Q1)STAFF UX=**읽기전용 폼**(폼 자체 숨김 기각 — requireSpaceAdmin 진입 허용한 의미·현재 설정값 열람 유지) (Q2)변수=`canEditSettings`(PATCH 미러)·`canDelete`(DELETE 미러) **별도 유지**(표현식 같지만 의미상 다른 게이트·향후 정책 발산 대비·`canManageSpace` 광범 이름 오재사용 위험, 내부 `isOwnerOrSuperAdmin` const로 DRY) (Q3)`handleSubmit` `if(!canEdit) return` 가드=**둘 가치 있음**(보안 경계는 서버 403, 클라 가드는 Enter 제출·DOM 조작·disabled 누락·리팩터 실수 차단=불필요 요청·혼란 방지) (Q4)**놓칠 위험=입력 계열 전수 비활성화**(input뿐 아니라 select/color/textarea/저장버튼까지 빠짐없이 canEdit 타야·하나 빠지면 STAFF 일부 편집 가능). **PATCH를 STAFF로 넓히는 건 권한 정책 변경이라 이번 WI 안 함**(별도 결정).

**해소 (impl `070b371`)**: (A) `settings/page.tsx` — `const isOwnerOrSuperAdmin = ctx.isSuperAdmin || space.ownerId === ctx.userId` 공유 const → `canEditSettings`(**PATCH 게이트 정확한 De Morgan 미러** — 동일 SoT `space.ownerId`+session.isSuperAdmin, `requireSpaceAdmin`의 role 아님)·`canDelete`(DELETE 게이트 미러) 별도 변수. `<SpaceSettingsForm canEdit={canEditSettings} .../>`. (B) `space-settings-form.tsx` — `canEdit: boolean` prop. false면 입력 6개(name/description/maxUsers/accessType/primaryColor/loadingMessage = input/textarea/select/color) **전부 `disabled`** + 저장 버튼 미렌더 + 상단 `readOnlyNotice` 안내(`role="status"`) + `handleSubmit` 시작 `if (!canEdit) return` 가드(closure prop 기반·우회 불가). (C) `dashboard-copy.ts` — `SETTINGS.readOnlyNotice` 한글 카피(하드코딩0). 회귀 테스트 +11(form 7 · page canEdit 게이팅 4: owner→true·superAdmin→true·STAFF→false·드리프트 OWNER[ownerId 불일치·비-super]→false). 게이트 **tsc0/lint0err[선재 LiveKit 경고1·WI무관]/vitest 550→561[+11]/build0**.

**듀얼 블라인드 1R 수렴: codex PASS 0 issues·evaluator PASS 9.83**(P0/P1/P2/fixNow=0, P3×3 defer) + `.pass`(fingerprint empty `e3b0c44`). 양 검증자 독립 확인: canEditSettings PATCH 게이트 정확 미러(De Morgan)·입력 6/6 disabled(INPUT_IDS 완전 일치)·저장버튼 미렌더·submit 가드·서버 403 근본 게이트로 **STAFF 편집 도달 경로 0**·데이터 드리프트 시 canEdit=false 일관·무회귀(canEdit 필수 prop tsc0·유일 호출부 settings/page·DeleteSpaceSection WI-037 보존)·하드코딩/영문 누출 0·server/prisma 무변경. evaluator **변이검증 3종 검출**(M1 submit 가드 제거→form FAIL·M2 access-type disabled 제거→matrix FAIL·M3 canEditSettings=true 강제→page STAFF·드리프트 2건 FAIL, 원복 clean). **Vercel 전용(server/·prisma/ 무관). main 미승격.** 교훈: (a) **권한 UI 게이팅은 그 액션의 서버 게이트를 정확히 미러**(PATCH=`ownerId===userId||isSuperAdmin`, requireSpaceAdmin role 아님·superAdmin role:"OWNER"·드리프트 OWNER 가능). (b) **"보이는데 저장 실패"는 진입 게이트와 액션 게이트의 불일치** → 진입 허용·액션 불가 사용자엔 읽기전용+안내(현재 값 열람 유지). (c) **읽기전용은 입력 계열 전수가 빠짐없이 게이트** — 하나 빠지면 부분 편집. (d) **동일 표현식 게이트라도 의미상 다른 서버 게이트 미러면 변수 분리**(발산 대비). (e) **클라 가드는 보안 경계 아님(서버 403 근본)이나 이중방어**. 상세 fix_plan.md Done.

**다음: develop→main 승격 검토 = codex 권고 1순위(WI-041 완료 후 바로 승격).** 승격 델타=**WI-026/033/034/035/036/037/038/039/041(9건)** — **server/(WI-036 enforce.ts/room.ts)+prisma/(WI-036 Space.deletedBy 마이그레이션) 포함** → **OCI 소켓 재배포 + prod DB `migrate deploy` 승인 게이트(사용자 승인 필요·내 단독 실행 불가)**. 승격 위험=**Vercel↔OCI 버전 불일치 구간**(codex — main push로 Vercel 새 클라/API 먼저 뜨나 OCI 구버전이면 archive detach/deny-cache·moderator mute 계약 잠깐 어긋남). 순서 권고: prod DB migrate deploy → main 승격(**rebase 머지로 author=flowcoder25 보존** — gh=yonghyeon-dev라 일반 merge면 Vercel 인가 실패) → OCI 재배포 확인 → Vercel 확인 → archive/mute 핵심 플로우 라이브 스모크. **승격 미선택 시 후속 코드 WI**: WI-042(대시보드 archived 가드)·WI-040(귓속말 발견성·저가치)·WI-043(공용 Dialog/focus-trap a11y).

## 이전 Active — ✅ WI-039-feat (인-스페이스 음성 강제 음소거 UI 연결 — WI-038 백엔드를 멤버 메뉴에 배선) develop PR 대기(2026-06-23, impl `21f5c4f`)
사용자 기능 요청 큐 **7/8 완료**. WI-038 서버 라우트 `POST /api/spaces/[id]/livekit/moderate {identity,muted}`(타인 음성 강제 음소거)를 **인-스페이스 참가자 패널 UI에 연결**하는 순수 프론트엔드 작업. 백엔드(라우트/스키마/소켓/Prisma) 무변경 = **Vercel 전용**. **설계 codex consult 1R 수렴**(Q1~Q5+놓칠위험): (Q1)음성 액션은 **LiveKit room 참가자(participantTracks/VideoTile)에만** 노출·판정=LiveKit identity 존재(audioTrack 유무 아님 — 마이크 끈 사람도 participant) (Q2)**`track.isAudioMuted`로 토글 라벨 결정 금지**(self-mute/moderator-mute 구별 불가 → self-mute 상태에서 "해제" 표시 시 unmute가 force-unmute 아니라 무변화=혼란) → **두 액션 명시**(음성 강제 음소거 muted:true / 음성 발언 허용 muted:false)·unmute=publish 권한 복원만이라 "해제" 아닌 "발언 허용" (Q3)WI-039는 **등록 멤버만**·guest-* 음성음소거 후속 WI·단 prop은 identity 받게 설계(게스트 확장 차단 안 함) (Q4)음성 액션 후 **refetch(onActionDone) 미호출**(SpaceMember/restriction 무변경·LiveKit 이벤트로 갱신)·per-action loading 가드·에러 code→한글 SPACE_COPY(영문 미노출) (Q5)identity는 **별도 prop**(participantIdentity)·`user-${userId}` 재구성 금지(계약 복제·게스트 차단 회피). **놓칠위험(반영)=메뉴 가시성 게이트와 액션별 게이트 분리** — 음성 액션이 기존 채팅/킥/밴 권한 모델을 흐리지 않게. **해소 (impl `21f5c4f`)**: (A) `member-actions-menu.tsx` — `participantIdentity?` prop(LiveKit identity=track.participantId) + `runVoiceAction(muted)`가 POST /livekit/moderate {identity,muted} 호출. 음성 제어 섹션(`showVoiceActions = !!participantIdentity`)에 두 액션·시각 구분(divider+"음성 제어" 라벨). 성공→`closeMenu()`·refetch 안 함. 실패→code별 한글 매핑(`COPY.voiceError[code] || actionFailed`)·networkError. **`runVoiceAction` useCallback은 early-return 앞 배치(hooks 규칙).** loading 가드(중복 클릭/race 차단). (B) `ParticipantPanel.tsx` — VideoTile 메뉴에만 `participantIdentity={track.participantId}` 전달(비-미디어 아바타 리스트 플레이어는 미전달→음성 액션 미노출·getParticipant 404 회피). (C) `space-copy.ts` — voiceSectionLabel/voiceActions/voiceError(INVALID_IDENTITY/SELF_TARGET/TARGET_NOT_MEMBER/PARTICIPANT_NOT_FOUND/LIVEKIT_NOT_CONFIGURED/LIVEKIT_OPERATION_FAILED→한글). 회귀 테스트 +9(identity 게이트·moderate POST 계약·refetch 미호출·code 한글 매핑·미지code 폴백·networkError·성공 메뉴 닫힘·게이트 미통과 시 메뉴 미렌더). 게이트 **tsc0/lint0err[선재 LiveKit 경고1·WI무관]/vitest 541→550[+9]/build0**. **듀얼 블라인드 1R 수렴: codex WARNING(구현 결함 0·유일 P3=read-only 샌드박스 vitest 미실행 환경제약·tsc/lint 직접 통과)·evaluator WARNING 9.77**(P0/P1/P2/fixNow=0, P3x4 defer, 변이검증 5/5 검출) + `.pass`(empty `e3b0c44`). **양 검증자 독립 확인: 음성 액션 게이트가 멤버 권한 게이트(canActOn/self/OWNER 보호) 우회/약화 0(participantIdentity만으론 노출 불가·라우트가 hard gate)·식별자 정확성(track.participantId=participant.identity, userId/memberId 혼입 0·body not.toContain memberId)·영문 누출 0·하드코딩 0·경계 0.** evaluator 변이검증 5종(M1 identity 게이트·M2 refetch 억제·M3 code 매핑·M4 mute 버튼 방향·M5 networkError 경로 — 전부 검출·원복 clean). **Vercel 전용(server/·prisma/ 무관 — OCI 소켓 재배포·prod DB 마이그레이션 불요). main 미승격.** 교훈: (a) **moderator-mute는 영속 상태가 없다(세션 내·self/moderator 구별 불가) → isAudioMuted로 토글하지 말고 두 액션 명시**(unmute가 force-unmute 아님을 라벨로 드러냄). (b) **세션 액션(LiveKit)을 멤버 관리 메뉴에 섞을 때 메뉴 가시성 게이트와 액션별 게이트를 분리**(participantIdentity로만 음성 액션 추가·기존 권한 모델 무영향). (c) **LiveKit moderate 식별자는 SpaceMember.id도 userId도 아닌 LiveKit identity → ParticipantPanel이 가진 track.participantId를 별도 prop으로 전달(재구성 금지).** (d) **상태 무변경 액션은 refetch 미호출**(LiveKit 이벤트로 자연 갱신). 상세 fix_plan.md Done. **다음: WI-040(feat·P3·선택) 귓속말 발견성 개선(DM 이미 완전 구현 → 신규 가치 낮음). 또는 사용자 기능 요청 큐 8/8 완료로 보고 후 develop→main 승격 검토.** **후속 BACKLOG: guest-* 음성 제어(신규)·WI-041(설정 PATCH 권한 정합·STAFF)·WI-042(대시보드 archived 가드)·WI-043(공용 Dialog/focus-trap a11y).**

## 이전 Active — ✅ WI-038-feat (LiveKit moderator mute 서버 계약 — 타인 음성 강제 음소거) develop 머지 완료(2026-06-23, PR#41 merge `6f197bb`, impl `0ede616`/r2 `3b8fed4`)
사용자 기능 요청 큐 **6/8 완료**. 운영자(OWNER/STAFF/superAdmin)가 다른 참가자의 마이크를 **서버 측에서 강제 음소거/해제**하는 전용 라우트(순 신규). 현 LiveKit은 self-mute만. **채팅 음소거(WI-035 restriction=MUTED·socket enforce)와 별개의 음성/미디어 레이어** — RoomServiceClient는 LiveKit 서버 직접 HTTP 호출이라 socket enforce 채널 무관. UI 연결은 후행 WI-039. **설계 codex consult 1R 수렴**: 전용 라우트(admin/members PATCH 확장 기각 — identity vs memberId·게스트 포함·transient)·stickiness=(b) 권한회수(advisory soft mute는 토큰 grant canPublish:true라 즉시 재-unmute / 영속은 schema 변경=별도 WI)·unmute=권한복원만·실패 503/404/502·**놓칠위험=canPublishSources 복원 시 타 제재 해제 → 현재 permission 읽어 mic만 add/remove**. **실측 보정**: codex 권고 `livekit/internal/moderation.ts`는 livekit 배럴이 **클라 전용**(React provider/hook)이라 라우트 import 시 클라 코드 오염 → **enforce 패턴 server-safe 신규 모듈로 분리**(WI-012 교훈). **SDK 실측**: `mutePublishedTrack(room,identity,trackSid,muted)`는 **trackSid 필요** → `getParticipant` 1회로 tracks+permission 동시 획득. `updateParticipant` permission은 **atomic**. **해소 (impl `0ede616`)**: (A) 신규 `src/features/space/livekit-moderation/`(배럴+internal/moderation.ts+test) — `parseParticipantIdentity`(user-/guest- allowlist·dev-anon/bare/빈 거부)·`findMicrophoneTrack`·`computePublishSourcesForMute/Unmute`(canPublishSources `[]`=전체허용: mute `[]`→`[CAMERA,SCREEN_SHARE,SCREEN_SHARE_AUDIO]`/non-empty→mic 필터, unmute `[]`→`[]`/non-empty→mic 추가·타 제재 보존)·`buildModeratedPermission`. (B) `POST /api/spaces/[id]/livekit/moderate {identity,muted}`: 인증→파싱(400 INVALID_IDENTITY)→self 거부(400 SELF_TARGET)→actor 게이트(OWNER/STAFF/superAdmin·admin/members 미러)→target 게이트(user-* SpaceMember.role 조회·없으면 404 TARGET_NOT_MEMBER·OWNER 보호·canActOn / guest-* 최하위)→config(503 LIVEKIT_NOT_CONFIGURED·dev devkey)→getParticipant(404 PARTICIPANT_NOT_FOUND)→**권한회수 먼저(updateParticipant)→기존 mic 트랙 mutePublishedTrack**(이미 muted 스킵·race 창 축소)→감사(SpaceEventLog ADMIN_ACTION voiceMute/voiceUnmute·best-effort)→200{identity,muted,trackSid}. op throw→502 LIVEKIT_OPERATION_FAILED(raw 미노출). unmute=권한복원만(force-unmute 안함). stickiness=세션 내(재접속 시 토큰 grant로 초기화=의도된 한계·토큰 라우트 무변경). **r2 (`3b8fed4`) — codex r1 FAIL(P2 fixNow) 해소**: `buildModeratedPermission`이 4필드만 보존했으나 `ParticipantPermission`은 **10필드**(+recorder/canUpdateMetadata/agent/canSubscribeMetrics/canManageAgentSession)이고 permission이 atomic이라 미열거 5필드가 **false로 silently reset**(codex risk #1 심층) → `{...current, canPublishSources}` **전체 spread 보존**·없을 때만 기본값 합성. 회귀 +1. 회귀 테스트 +40(moderation 21·route 19). 게이트 **tsc0/lint0err[선재 LiveKit 경고1·WI무관]/vitest 501→541[+40]/build0**. **듀얼 블라인드 2R 수렴: codex r1 FAIL(P2 fixNow permission atomic-reset)→r2 WARNING**(유일 잔여=환경 EPERM P3·구현 결함 0)**·evaluator r1 9.83→최종HEAD WARNING 9.88**(P0/P1/P2/fixNow=0, P3x3 defer) + `.pass`(empty `e3b0c44`). **핵심: codex가 r1에서 단독으로 permission atomic-reset P2 적출(evaluator도 r1 놓침) → r2 evaluator SDK 10필드 독립 실측+변이검증으로 실재성/수정정확성 확인 — 듀얼 블라인드 가치 실증.** evaluator 변이검증 4종(permission 보존·idempotency·revoke-then-mute 순서·r2 보존 가드). **Vercel 전용(server/·prisma/ 무관 — OCI 소켓·prod DB 무관). main 미승격.** 교훈: (a) **LiveKit updateParticipant permission은 atomic — 부분 enumeration이 미열거 필드 false 리셋 → 현재 permission 전체 spread 보존+변경 필드만 덮어쓰기.** (b) **mutePublishedTrack은 trackSid 필요(source 아님)·getParticipant 1회로 tracks+permission 동시 획득.** (c) **서버 라우트용 도메인 로직은 클라 전용 배럴(React)과 분리**(livekit 배럴=provider/hook → enforce 패턴 server-safe 모듈). (d) **canPublishSources []=전체허용 시맨틱**(mute 시 명시 목록으로 좁힘·unmute 시 [] 유지). (e) **moderator mute stickiness=권한회수**(단순 mutePublishedTrack은 canPublish:true라 즉시 재-unmute). 상세 fix_plan.md Done. **다음: WI-039(feat·P2) 인-스페이스 UI에 음성 강제 음소거 액션 연결 — "채팅 음소거" vs "음성 음소거" 라벨 명확 구분.** **후속 BACKLOG: WI-041(설정 PATCH 권한 정합·STAFF)·WI-042(대시보드 archived 가드)·WI-043(공용 Dialog/focus-trap a11y).**

## 이전 Active — ✅ WI-037-feat (설정 화면 스페이스 삭제 UI — 이름 타이핑 확인 모달 + DELETE + /my-spaces 이동) develop 머지 완료(2026-06-23, PR#40 merge `3b6d671`, impl `f023e77`/r2 `ed266b6`)
사용자 기능 요청 큐 **5/8 완료**. 설정 화면에서 OWNER/superAdmin이 스페이스를 삭제(soft delete=ARCHIVED)하는 **순수 UI**. 백엔드 `DELETE /api/spaces/[id]`는 WI-036에서 완성 — **이 WI는 백엔드 무변경·Vercel 전용**(server/·prisma/ 무관). **설계 codex consult 1R 수렴**: 삭제 진입점 설정 페이지 전용(카드 삭제는 오클릭 위험 기각)·게이팅은 `ownerId` 직접 비교(DELETE 게이트 정확한 미러)·이름 타이핑 확인 모달·`/my-spaces` redirect + 마운트 refetch(removeSpace 불필요)·멱등 archived 200 성공취급·**놓칠위험=설정 PATCH 권한 불일치**(STAFF 진입 가능하나 PATCH는 owner만 403→WI-041). **해소**: (A) 신규 `src/components/dashboard/delete-space-section.tsx`(client) — 위험 구역 카드 + 이름 타이핑 확인 모달(`role=dialog`·`aria-modal`·`aria-labelledby`·ESC·백드롭/취소 닫기·**진행 중 닫기 차단**). `canConfirm = spaceName.length>0 && confirmInput===spaceName && !isDeleting` → `DELETE /api/spaces/[id]` → 200(멱등 archived 포함)이면 `router.replace("/my-spaces")`. non-ok→서버 에러메시지·모달 유지·**redirect 안 함**, throw→기본 에러·모달 유지. 성공 후 isDeleting 미리셋(언마운트 무해+중복 제출 방지). (B) `src/app/dashboard/spaces/[id]/settings/page.tsx` — findUnique select에 `ownerId` 추가 + `canDelete = ctx.isSuperAdmin || space.ownerId === ctx.userId`(**DELETE 게이트 `ownerId!==userId && !isSuperAdmin`의 De Morgan 등가 미러** — `requireSpaceAdmin`은 superAdmin에 role:"OWNER" 반환·데이터 드리프트 OWNER 멤버 가능이라 **role 기반은 틀림**) → canDelete일 때만 `<DeleteSpaceSection>` 렌더(**STAFF·비소유자 미노출**). (C) `DASHBOARD_COPY.SETTINGS.dangerZone` 한글 카피(하드코딩0). **r2**: codex fixNow=true(빈 이름 스페이스에서 빈 입력 즉시 일치로 삭제 무타이핑 활성 — PATCH가 비-빈 이름 미강제로 도달) → `spaceName.length>0` 선행 가드 + 회귀 테스트. 회귀 테스트 +23(delete-space-section 17 / settings/page 6: 게이팅 ownerId 미러·STAFF 차단·드리프트 OWNER·select). 게이트 **tsc0/lint0err[선재 LiveKit 경고1·WI무관]/vitest 478→501[+23]/build0**. **듀얼 블라인드 2R 수렴: codex r1 WARNING(P3×2)→r2 PASS 0 issues·evaluator 최종HEAD WARNING 9.9**(P0/P1/P2/fixNow=0, P3×2 defer: focus-trap[전역 a11y 후속 WI-043·기존 모달은 role/aria조차 없음·이름 정확일치 게이트로 별도 보호]·delete-space-title 하드코딩 id[단일 인스턴스·충돌0]) + `.pass`(empty `e3b0c44`). 양 검증자 독립 확인: canDelete ownerId 미러(De Morgan)·서버 hard gate·멱등/에러/중복제출·`/my-spaces` redirect(status=ACTIVE 자연제외·zustand stale 없음)·하드코딩0·server/prisma 무변경. evaluator **변이검증 5종 검출**(M1 length>0·M2 이름일치·M3 중복제출·M4 non-ok no-redirect·M5 canDelete role 변이, 원복 clean). **main 미승격.** 교훈: 권한 UI 게이팅은 그 액션의 서버 게이트를 정확히 미러(role 출력≠ownerId 게이트, superAdmin role:"OWNER"·드리프트 OWNER) / 되돌릴 수 없는 파괴적 액션=이름 타이핑+빈값 가드(빈 이름 스페이스 즉시 일치 엣지 차단) / soft-delete 후 목록=redirect+마운트 refetch로 충분(GET status=ACTIVE·zustand 재마운트 해소·removeSpace 불요) / 중복 제출 방지=성공 경로 isDeleting 미리셋+비-성공 전부 리셋. 상세 fix_plan.md Done. **다음: WI-038(feat·P2) LiveKit moderator mute 서버 계약.** **후속 BACKLOG: WI-041(설정 PATCH 권한 정합·STAFF)·WI-042(대시보드 archived 가드)·WI-043(공용 Dialog/focus-trap a11y).**

## 이전 Active — ✅ WI-036-fix (스페이스 archive 하드닝 — deletedBy 감사 + 접속자 추방/알림) develop 머지 완료(2026-06-23, PR#39 merge `c3a084b`, impl `3646378`)
사용자 기능 요청 큐 **4/8 완료**. `DELETE /api/spaces/[id]`가 `status=ARCHIVED+deletedAt`만 기록하고 **누가 삭제했는지(deletedBy) 미기록**·**접속 중 사용자 미추방**(enforce 채널은 per-user ban/kick/mute/role만, space-level archive 브로드캐스트 없음)하던 결함 해소. **설계 codex consult 1R 수렴**(놓칠위험 다수 적출): 단일 enforce 채널에 archive 추가·**userId optional 전역화 기각→discriminated union**·archive postcondition=`Space.status===ARCHIVED`·**최초 archive 행위자 보존**(재삭제 시 deletedBy 미덮어쓰기=감사 무결성, codex 최대 누락위험)·applyEnforcement **snapshot 후 detach**(순회 중 room mutation 방지)+presence 전 인덱스 직접 정리·**TOCTOU deny cache**(in-flight join 차단)·클라 리다이렉트는 WI-037. **해소(impl `3646378`)**: (A) `Space.deletedBy String?`(FK 없는 순수 스칼라·ChatMessage 선례·응답 미노출) + 마이그레이션 `20260623180000_space_deleted_by`(`ADD COLUMN IF NOT EXISTS` 멱등·WI-016 패턴·`git add -f`·**prod 적용=승인 게이트**·오프라인 `migrate diff` 생성[**git HEAD 스키마를 baseline으로** — `grep -v deletedBy`는 ChatMessage.deletedBy도 잡아 오염]). (B) `enforce/internal/contract.ts` `EnforceRequest`→**discriminated union**(`UserEnforceRequest` userId 필수/`ArchiveEnforceRequest` userId 없음), `ENFORCE_USER_ACTIONS`+`ENFORCE_ACTIONS`(+archive), `parseEnforceRequest` archive 분기(userId 불필요·입력 userId/role **무시**=식별자 혼입 차단·멤버제재 userId 필수 유지=회귀0), admin/members route는 `EnforceUserAction`로 dispatch 타입 강제(archive 오발송 컴파일 차단). (C) DELETE: findUnique `select:{id,ownerId}` lean + `updateMany({where:{id,status:{not:"ARCHIVED"}},data:{status,deletedAt,deletedBy}})` **원자적 최초-행위자 보존**(count0=이미 archived→보존·동시 DELETE race 안전) + `dispatchEnforcement({action:"archive"})` best-effort(`realtimeEnforced` 응답). (D) 소켓서버: `verifyPostcondition` archive→`Space.status==="ARCHIVED"` 재확인(시크릿 유출 차단=409) / `applyEnforcement` archive→`markSpaceArchived` 선등록+`socketsInSpace` **스냅샷**→각 소켓 `space:error{SPACE_ARCHIVED}` emit→`detachSocketFromSpace`→grace 후 disconnect→`purgeSpaceState`(spacePlayersMap+recordingStates+spotlightStates+proximityStates **4개 space-키 맵**·chat 맵은 userId/messageId 키라 제외) / room.ts `archivedSpaces` deny cache+`join:space`가 모든 await 후·socket.join 직전 재확인(**TOCTOU**·DB status 게이트가 근본방어·deny cache는 best-effort·재시작 청소·archived 단방향이라 bounded). (E) 클라 변경0(기존 `space:error`→`setSocketError`→chat-panel 알림·강제 리다이렉트=WI-037). **무회귀**: 멤버제재 파싱·적용 무변경, deletedBy 4경로(GET/PATCH/DELETE/join) 미노출, **Dockerfile.socket COPY 무변경**(room.ts `../state`는 server/ 전체 COPY·번들 런타임 src 입력 4개 불변·esbuild metafile 실증). 회귀 테스트 +14(contract archive 5·route DELETE 9). 게이트 **tsc0/lint0err[선재 LiveKit 경고1·WI무관]/vitest 464→464→478[+14]/build0/esbuild0**. **듀얼 블라인드 1R 수렴: codex WARNING(유일=read-only 샌드박스 EPERM으로 vitest/prisma 미실행=환경적 P3·코드 인스펙션 0 defect·tsc/esbuild exit0 / 권위 환경 vitest 478·build0 실증)·evaluator WARNING 9.81**(P0/P1/P2/fixNow=0, P3×2 defer: ①server/ 핵심 실시간 로직 자동 테스트0[vitest include:src/** 구조적·WI-018 동일·테스트 WI 분리] ②PATCH findUnique 전체행[응답 allowlist 차단·실누출0·선재·심층방어]) + `.pass`(fingerprint empty `e3b0c44`). 양 검증자 archive 핵심로직(union narrowing·snapshot-before-detach·deletedBy 원자보존·postcondition·식별자혼입 차단·deletedBy 미노출) 독립 클린확인. evaluator 변이검증 3종 검출(status!=ARCHIVED 가드·deletedBy 기록·archive userId 혼입차단, 복원 clean). **server/·prisma/·src/ 변경 — 승격 시 OCI 소켓 재배포(server/handlers)+prod DB 마이그레이션 승인(Space.deletedBy) 필요. main 미승격.** 교훈: 감사 컬럼=최초 행위자만 기록(`updateMany status!=ARCHIVED` 조건부 원자 갱신이 재삭제/동시요청 보존·단순 update는 최초삭제자 덮어씀=조사 치명) / per-user enforce를 space-level 확장 시 discriminated union(userId optional 전역화 금지) / room 순회 중 detach=mutation-during-iteration→스냅샷 선고정 / 공간 영구종료 시 spaceId-키 인메모리 전부 직접 정리(presence+media) / TOCTOU는 DB 게이트 근본+메모리 deny cache 보강 / 마이그레이션 baseline은 git HEAD 스키마. 상세 fix_plan.md Done. **다음: WI-037(설정 스페이스 삭제 UI).**

## 이전 Active — ✅ WI-035-feat (인-스페이스 참가자 패널 멤버관리 UI — 채팅 음소거/해제·강퇴·차단, role 게이팅) develop 머지 완료(2026-06-23, PR#38 merge `bc9dad6`)
사용자 기능 요청 큐 **3/8 완료**. **실측 정정(핸드오프 가정 2건 틀림·착수 시 import-grep으로 발견)**: ①`src/components/space/player-list.tsx`는 **죽은 코드**(어디서도 import/렌더 안 됨) → 실 인-스페이스 참가자 패널은 `src/components/space/video/ParticipantPanel.tsx`(LiveKit 미디어 결합). ②in-space `players`(socket PlayerData)는 `{userId,nickname}`만 — role·SpaceMember.id 없음(admin PATCH는 `memberId`=SpaceMember.id 요구). **설계 codex consult 1R 수렴**: 컴포넌트 타깃=ParticipantPanel(player-list 부활 금지)·VideoTile `actionsSlot` 렌더프롭(미디어 참가자도 관리)·게스트 1차 등록사용자만(enforce가 target.userId 있을때만 dispatch)·actor role을 stale prop 대신 admin GET self행 도출(GET 403=자연 권한게이트)·socket role-sync 범위 밖(서버 PATCH hard gate)·액션 mute/unmute/kick/ban(changeRole 제외=대시보드)·copy 신규상수·**놓칠위험 #1=식별자 혼동**(SpaceMember.id/userId/LiveKit identity user-*·guest-*/socket 이벤트 memberId[실제 userId]가 섞임 → PATCH엔 admin GET의 SpaceMember.id만·`guest-` 벗겨 매칭 금지). **해소**: 신규 `SPACE_COPY`(in-space 카피 SoT) + `useSpaceMembers` 훅(admin GET→`Map<userId,{memberId,role,restriction}>`[등록사용자만], GET 403→미인가+빈맵[stale prop 우회], actor role=GET self행) + `managedUserIdFromIdentity`(LiveKit identity는 user- 접두사만 userId) + `MemberActionsMenu`(canActOn+self+멤버존재 게이팅·PATCH는 memberId만·kick/ban 인라인확인[kick 재입장가능·ban 차단 문구구분]) + ParticipantPanel/VideoTile(actionsSlot 좌상단)/SpaceMediaLayer/space-client(spaceId 전달). **r2**: codex P3(훅 비활성시 stale 스냅샷 미정리) 선제해소(early-return 전 폐기+회귀테스트). codex P3-2(eval-results diff)는 프로젝트 컨벤션이라 미변경. **듀얼 2R 수렴 codex r1 WARNING(P3×2 defer·fixNow=0)→r2 PASS 0 issues·evaluator 9.88**(P0/P1/fixNow=0, P3×3 defer). 양검증자 식별자혼동 차단 독립확인. 변이검증 8/8. 게이트 4/4(tsc0/lint0err/vitest 442→**464**[+22]/build0)+`.pass`(empty `e3b0c44`). impl `f370b3a`/r2 `b8079de`. **Vercel 전용(server/·prisma/ 무관). main 미승격.** 교훈: 핸드오프 "현재 X 컴포넌트" 가정은 import-grep 실측(player-list=죽은코드) / 인-스페이스 식별자(SpaceMember.id·userId·LiveKit identity·socket memberId[실제 userId]) 공존 → PATCH엔 admin GET SpaceMember.id만·식별자별 해석함수 분리 / stale 클라 role(WI-034 놓칠위험)은 관리 UI에서 admin GET self행으로 대체 / 죽은코드 부활보다 실 사용 컴포넌트 통합. 상세 fix_plan.md Done.

## 이전 Active — ✅ WI-034-fix (인-스페이스 role 전달 — page가 SpaceMember.role 조회해 SpaceClient.user.role 주입) develop 머지 완료(2026-06-23, PR#37 merge `70a7bac`)
사용자 기능 요청 큐 **2/8 완료**. `src/app/space/[id]/page.tsx`가 `SpaceClient`에 `{id,nickname,avatar}`만 넘기고 **role 미전달** → `user.role` 세션 내내 undefined(prop·소켓 갱신 경로 없음) → OWNER/STAFF도 ①에디터 진입(`space-client.tsx:80 canEdit`) ②관리 채팅명령 파싱(`use-chat.ts:132 isAdmin` @mute/@kick) ③`ChatPanel` 관리 UI가 비활성이던 선결 버그(WI-035 인-스페이스 관리 UI 권한 게이팅 선결). **설계 codex consult 1R**: 권한 SoT=`SpaceMember.role`(소켓 `join:space` room.ts:48·`requireSpaceAdmin` 정합 — ownerId 파생/합성OWNER/superAdmin 특례는 클라/소켓 발산) · 합성OWNER 금지(owner 멤버행 누락시 self-heal create) · superAdmin 인-스페이스 특례 없음 · 순수헬퍼 분리 · 놓칠위험=세션중 role 변경 stale(WI-035 범위). **해소**: 신규 순수헬퍼 `resolveSpaceRoleDecision`(space-role.ts) + page 통합 1쿼리(role+restriction)→결정→redirect/upsert→prop 주입. **듀얼 3R 수렴**: codex r1 FAIL(P2 BANNED page미검사·P3 bare create race, 둘 fixNow)→r2 FAIL(P2 upsert 충돌분기 BANNED race·fixNow 신규)→r3 PASS 0 issues / evaluator r1 9.85→r2 10.0→r3 WARNING 9.94(P0/P1/P2=0). **양 검증자 BANNED·race 독립 수렴**. **r2**: BANNED 멤버 role보다 먼저 redirect(소켓 정합 — **맵 편집 HTTP `map/tiles`·`map/objects`가 BANNED 미검사라 r1이 BANNED admin에게 role 주면 에디터 우회편집 가능 = 신규회귀** 차단)+create→원자적 upsert(P2002). **r3**: upsert 후 post-upsert BANNED 재평가(TOCTOU 마감). 변이검증 9종(MUT-A~I). 게이트 4/4(tsc0/lint0err/vitest 416→**442**[+26]/build0)+`.pass`(empty `e3b0c44`). impl `c75e351`/r2 `22aa279`/r3 `5379485`. **Vercel 전용(server/·prisma/ 무관). main 미승격.** 교훈: 인-스페이스 클라 role SoT=소켓 권위와 동일 / role 신규 주입 시 그 role 게이팅 경로의 우회 가드(BANNED) 재점검 / 자동가입은 upsert(P2002 race·멱등) / TOCTOU는 write 후 불변식 재평가 / vitest mock은 select 무시→`arg.select` 단언+tsc 이중방어. 상세 fix_plan.md Done.

## 이전 Active — ✅ WI-033-feat (어드민 대시보드 한글화 + copy constants 분리) develop 머지 완료(2026-06-23, PR#36 merge `bde92f0`)
사용자 기능 요청 큐 **1/8 완료**. 어드민 대시보드(18 소스파일) 100% 영문 → 단일 SoT `src/constants/dashboard-copy.ts`(`DASHBOARD_COPY`, 키 영문/값 한글, `as const`)로 분리·한글화. i18n 미도입(단일 한국어 제품). **설계 codex consult 1R**: 단일 파일·scope dashboard 엄격 고정(인-스페이스 player-list 별도 후속)·CSV/에러/aria-label 전부 한글·동적 함수 엔트리. **핵심 위험(codex Q5)=표시 라벨/도메인 값 분리**: `<option value>`·필터키·액션토큰·색상맵 키 전수 enum 코드 유지, `*Label()` 헬퍼로 표시만 매핑(미정의 코드 폴백). CSV는 헤더+표시값 화면과 동일 한글 라벨 SoT 공유, payload JSON 키 미번역. **듀얼 2R 수렴 codex r1/r2 PASS 0 issues·evaluator r1 9.81→r2 9.88**(P0/P1/P2 0). evaluator 유일 P3(accessType만 패턴 이탈·SoT 중복) r2 즉시 해소(accessTypeLabel 단일 SoT·consistency 10.0). 변이검증 2종. 게이트 4/4(tsc0/lint0err/vitest 407→**416**/build0)+`.pass`(empty `e3b0c44`). impl `77f77e6`/r2 `82b39ed`. **Vercel 전용(server/·prisma/ 무관). main 미승격.** 교훈: 표시 라벨↔도메인/API 값 분리(`*Label(code)` 코드 폴백)·CSV↔화면 동일 SoT·헬퍼 패턴 통일=단일 SoT. 상세 fix_plan.md Done.

## 다음 세션 — 순차 진행 큐 (사용자 지시 2026-06-23)
**사용자 기능 요청 4건**(①어드민 대시보드 한글 통일[✅WI-033] ②스페이스 삭제 ③인-스페이스 멤버관리 강퇴/음소거/귓속말 ④어드민/공통 권한별 구별)을 read-only 멀티에이전트로 실측 + **codex consult로 분해·우선순위 산정 완료**. **다음 세션은 fix_plan.md Queue의 WI-035부터 1개씩 순서대로 처리**(각 WI 착수 시 READY 승격 → 분기 → 기계게이트 → 듀얼검증 → .pass → develop PR).

**확정 시퀀스** (상세·근거는 fix_plan.md Queue 표):
1. ✅ **WI-033** (feat) 대시보드 한글화 + copy constants 분리 — **완료**(develop 머지).
2. ✅ **WI-034** (fix) `/space/[id]` page가 SpaceMember.role→SpaceClient.user.role 전달 (선결 버그) — **완료**(develop PR). user.role 주입 완료 → WI-035 권한 게이팅 선결조건 충족.
3. ✅ **WI-035** (feat) 인-스페이스 참가자 패널 멤버관리 UI(채팅음소거/해제·강퇴·차단, role 게이팅) — **완료**(develop 머지). ParticipantPanel(실 패널·player-list 죽은코드)에 통합·dashboard HTTP admin API 재사용·식별자혼동 방지(PATCH는 SpaceMember.id만). 듀얼 2R codex PASS·eval 9.88.
4. **WI-036** (fix) 스페이스 archive 하드닝(deletedBy 감사+접속자 추방) — **다음 착수**. **Space.deletedBy 컬럼=Prisma schema 변경→마이그레이션 승인 게이트**(WI-013/016 패턴). 현 DELETE는 status=ARCHIVED+deletedAt만(deletedBy 미기록·접속중 사용자 미추방). enforce 채널에 space-level archive 추가. UI(WI-037) 선결.
5. **WI-037** (feat) 설정 스페이스 삭제 UI(확인 모달+DELETE+이동) — WI-036 후행.
6. **WI-038** (feat, P2) LiveKit moderator mute 서버 계약(음성 강제 음소거·순 신규).
7. **WI-039** (feat, P2) 인-스페이스 음성 강제 음소거 UI 연결 — WI-038 후행.
8. **WI-040** (feat, P3·선택) 귓속말 발견성 개선(DM은 이미 완전 구현).

**실측 정정(가정≠실제·착수 전 필독)**: 스페이스 **DELETE API 이미 존재**(soft delete, `route.ts:143`) — UI+하드닝만 / **강퇴·채팅음소거 백엔드+소켓 enforce+채팅명령 이미 존재**(WI-005) — 발견가능 UI만 / **귓속말 이미 완전 구현**(whisper:send 소켓+DB+파서+UI) / **음성 강제 음소거만 순 신규**(LiveKit moderator) / 권한모델(`canActOn` 역할랭크)은 견고 — UI 게이팅만. 협의 산출물 `scratchpad/consult-feature-decomp.md`. 기존 저우선 BACKLOG(WI-025 offset 상한·표면0 / WI-028 AUTH_SECRET probe·운영 runbook)는 이 시퀀스 뒤.

## 이전 — ✅ WI-026-fix (저장 metadata public/internal 분리) develop 머지 완료(2026-06-23, PR#35)
**READY 큐 소진 → 사용자 "핸드오프 읽고 코덱스와 협의하여 작업 진행" → 설계 codex consult로 BACKLOG 3건(WI-025/026/028) 우선순위 협의 → WI-026 1순위 선정**(WI-025 실표면0·임의정책 / WI-028 운영 runbook 성격). generate/batch가 생성 성공 시 `GeneratedAssetMetadata` 전체(prompt/workflow/comfyuiJobId)를 metadata Json에 통째 저장하던 것을 공개 런타임 키만 저장하도록 정규화(응답 누출은 WI-019 allowlist로 이미 0 — **저장면 축소 심층방어**, WI-024 "응답+저장 양쪽" 패턴의 저장 측). 신규 `buildStoredAssetMetadata`(PUBLIC_METADATA_KEYS를 저장/응답 단일 SoT 공유) + batchId storage-only 보존 + 순수 코어 분리 백필(prod 게이트). 듀얼 블라인드 **5R 수렴 codex PASS 0 issues·evaluator 9.92**(P0/P1/P2 0, P3×2 defer). **핵심 WI(저장 빌더·라우트·batchId·응답 불변)는 5R 내내 0 findings — 클린**; 모든 이슈는 백필 ops prod 게이트(codex가 libpq 연결문자열 우회 벡터 4R 적대 적출: 전체-URL regex→hostname→host/hostaddr query→반복키 → 매 라운드 fail-safe 차단). 게이트 4/4(tsc0/lint0err/vitest 384→407/build0). impl `6b02fa6`→r5 `02614ce`. **Vercel 전용(server/ 미참조 — OCI/prod-DB 무관). main 미승격(승격은 사용자 승인 게이트).** 남은 BACKLOG: WI-025(parsePageNumber offset 상한·표면0)/WI-028(Vercel↔OCI AUTH_SECRET probe·운영 runbook). 상세는 fix_plan.md Done 참조.

## 이전 — ✅ develop→main 승격 완료 + 라이브 반영(2026-06-23, PR#34 rebase merge main HEAD `626769e`) 사용자 "develop→main 승격" 선택(AskUserQuestion, codex consult 1순위 권고). **승격 델타 = WI-029/030/031/032(4건)**. **Readiness GO**: codex consult GO + 적대 통합감사 4축(cross-WI/통합건전성/배포런타임/보안회귀) 전 차원 GO·blocker 0. develop⊇main strict superset(클린 rebase·충돌0). **배포 영향**: Vercel 웹/일부 API 라우트만(assets route도 WI-030 query-filter 공용화로 touched) — **server/·prisma/ 무변경 → OCI 소켓 재배포·prod DB 마이그레이션 no-op**, 신규 env 없음. **실행**: rebase 머지(author=`flowcoder25@gmail.com` 보존→Vercel 인가, gh=yonghyeon-dev라 일반 merge면 인가 실패) → **Vercel Production ● Ready**(`flowspace-v2-8twa6brxj`, push 직후). **라이브 검증**: `space.flow-coder.com` 200·`/login` 200·`/api/users/me` 307(인증 게이트). **develop back-sync 완료**(force-with-lease, develop==main==`626769e`·SHA 분기 없음 — codex 놓칠위험 해소). 산출물 `.flowset/promotion-readiness.md`. **롤백**: Vercel 이전배포 promote / main revert. **남은 사용자 게이트(비코드)**: 인증 소켓 e2e smoke(자격증명 필요)·WI-024 백필 --apply prod·WI-005 SOCKET_INTERNAL_*·WI-013 prod EXPLAIN. **READY 큐 비어 있음. BACKLOG: WI-025(표면0)/026(누출0)/028(코드-WI 부적합→runbook).**

## 이전 Active (참고) — WI-032-fix
**✅ WI-032-fix (어드민 로그 SpaceEvent payload 응답 allowlist 정형화 + lean DTO) develop 머지 완료(2026-06-23, PR#33 merge `bf4a3ea`)**. **READY 큐 소진** 상태에서 사용자 지시("핸드오프 읽고 코덱스와 협의하여 작업 진행")로 진행: 설계 codex consult로 BACKLOG 4건(WI-025/026/028/032) 우선순위 협의 → **codex 1순위는 develop→main 승격 권고(사용자 게이트)**이나 코드 WI 한다면 **WI-032 최선**(small·low-risk·순수함수·deny-by-default 레일 동형 / WI-028 코드-WI 부적합=self-probe false-pass·운영성격, WI-025/026 표면0) → WI-032 선정. **해소**: `SpaceEventLog.payload`(Json)가 클라 반환 경로(`admin/logs` + `admin/stats` recentActivity → 화면/CSV)로 raw 직렬화되던 표면을 **응답 DTO 계층**에서 차단(deny-by-default, WI-014/019/021/024 동형). 신규 순수 `src/lib/space-event-log-payload.ts`(`PUBLIC_SPACE_EVENT_PAYLOAD_KEYS` 8키 실측[action/messageId/targetMemberId/targetName/senderName/trackType/trackSource/participantName] allowlist + `toPublicSpaceEventPayload`[비객체/배열/빈결과→null·화면 "-"·CSV "" 계약 보존] + `toPublicSpaceEventLog` lean DTO[내부 스칼라 spaceId/userId/guestSessionId/participantId 제외]). `import type`만 client-safe. logs route `items.map` + stats route `recentActivity.map` 정규화, `{logs,nextCursor}` 계약 보존. 화면/CSV/페이지 payload 타입 공유 `PublicSpaceEventPayload` 단일화(런타임 무변경 — 정규화는 API 담당). **듀얼 블라인드 2R 수렴: codex r1 WARNING(P2/fixNow admin/stats 병렬 누출경로)→r2 PASS 0 issues · evaluator 최종 HEAD WARNING 9.85**(P0/P1/P2 0, fixNow 0, P3×3 defer). **양 검증자 stats recentActivity 병렬경로 독립 수렴 발견→r2 즉시 해소**(stats 정규화+회귀테스트+전수grep으로 제3경로 부재 실증 — SpaceEventLog 클라 반환 read 경로는 logs+stats 둘뿐, 나머지 4는 write site). 변이검증 M1(allowlist passthrough→10 FAIL)/M2(lean DTO spread→3 FAIL)/M3(빈결과→{}→4 FAIL) 검출·원복 clean. 게이트 4/4(tsc0/lint0err[선재 LiveKit 경고1·WI무관]/vitest 364→**384**[+20]/build0) + `.pass`(empty `e3b0c44`). impl `b3dd931`/r2 `b6e3811`. **교훈**: (a) **DB Json 컬럼 응답 정형화는 단일 라우트가 아니라 그 모델 행을 반환하는 모든 read 경로를 전수 grep해야 완결**(admin/logs만 고치면 admin/stats recentActivity가 같은 raw 누출 — 양 검증자 독립 수렴, WI-023 전역표면 grep 교훈 연장). (b) **차단 chokepoint는 화면/CSV가 아니라 API 응답 DTO**(raw JSON 직접 호출이 표시계층 우회 — codex consult). (c) **payload allowlist + lean DTO를 같은 헬퍼로 묶으면 향후 모델 컬럼 추가·select 확장에도 응답 키 고정**(WI-019/021 패턴). **WI-029~032 develop만 — main 미승격(승격은 사용자 승인 게이트, codex consult 1순위 권고). BACKLOG: WI-025(표면0)/026(누출0)/028(코드-WI 부적합→runbook 재분류).**

## 이전 Active (참고)
**✅ WI-031-feat (어드민 대시보드 CSV 내보내기: 멤버/로그/analytics) develop 머지 완료(2026-06-23, PR#32 merge `6bdee98`)**. 사용자 "대시보드 완성" 선택 범위 **2/2 완료(마지막)** — WI-029(품질 마감)·WI-030(고급 필터)에 이은 마무리. 설계 codex consult 2R. 신규 순수 `src/lib/csv.ts`(`toCsv` RFC4180 이스케이프[`,"`·CR·LF→따옴표·내부따옴표 이중화, CRLF 구분] + 수식 인젝션 중화[셀 선두 `= + - @` tab/CR/LF → `'` prefix, neutralize→quote **순서**] + `CSV_BOM` Excel 한글) + `src/components/dashboard/csv-export.ts`(도메인 매퍼 `membersToCsv`[이름 폴백 `||`+Unknown=MemberTable 일치]/`logsToCsv`[payload JSON.stringify·null→빈·순환→`[unserializable]`]/`analyticsToCsv`[두 시계열 날짜 union·결측0·오름차순]/`csvFilename` + `downloadCsv` try/finally[click throw에도 a.remove+revokeObjectURL 보장]) + 공용 `export-csv-button.tsx` + 3페이지 배선(**로그=클라 로드 분량만**·버튼 라벨에 건수 노출[스코프 가시화·codex 위험#1]·멤버=visibleMembers[화면 필터=내보내기]·analytics=data). 듀얼 블라인드 **3R 수렴 codex PASS·evaluator WARNING 9.85**(P0/P1 0, fixNow 0). **r1: codex fixNow P2×2**(이름 폴백 `??`vs MemberTable`||` / logs payload Details raw) **→ r2: 이름 폴백 `||`+Unknown 수정·downloadCsv finally·payload는 consult r2 합의로 defer / r2 codex FAIL: 선두 LF(`\n`) 미중화**(`\t`·`\r`은 있는데 비대칭, **evaluator도 동일 P3 독립 수렴**) **→ r3: `\n` 추가+회귀테스트 → codex PASS**. 게이트 4/4(tsc0/lint0err/vitest 333→364[+31]/build0). **교훈**: (a) **CSV/수식 인젝션 중화는 RFC4180 quoting **전에** 적용해야 `'` prefix가 따옴표 안에 들어감**(neutralize→quote 순서 — evaluator 변이검증 M3로 가드 실증). (b) **공백류 인젝션 prefix는 `\t`·`\r`·`\n` 대칭이어야**(LF만 빠지면 양 검증자가 즉시 적출 — 일부 스프레드시트가 선두 공백 무시 후 수식 해석). (c) **표시 폴백은 `??`(nullish) 아닌 `||`(truthy)** — 빈 문자열 이름이 화면과 CSV에서 어긋남(MemberTable·filterMembers와 일치 필수). (d) **payload raw export는 화면(event-log-table가 이미 `JSON.stringify(payload)` 렌더)·API(raw 반환)와 동일 admin-scoped 데이터라 신규 노출 아님 → accepted-risk defer가 옳음**(CSV만 sanitize하면 화면≠CSV 발산·실노출 미감소, consult r2 codex 동의). (e) **codex 위험: payload 계약 의존 — 향후 event type이 PII/secret 추가 시 화면+CSV 동시 노출 → 금지 키 리뷰**(WI-032 후보). **WI-029~031 develop만 — main 미승격(승격은 사용자 승인 게이트). 어드민 대시보드 완성 범위 종료.**

## ✅ WI-030-feat (2026-06-23) — 어드민 대시보드 고급 필터 [PR#31 머지] — 상세는 fix_plan.md/이전 원장 참조 사용자 "대시보드 완성" 선택 범위(고급 필터·내보내기) 1/2. 설계 codex consult 1R. 신규 공용 `src/lib/query-filter.ts`(normalizeEnumFilter 공용 추출[assets/route.ts import 전환]+parseDateRangeFilter[ISO instant→createdAt {gte,lt}, start incl/end excl, 잘못된 날짜·start≥end→400]) + 클라 `date-range.ts`(로컬 날짜→ISO instant, 타임존 함정 회피) + admin/messages·admin/logs 라우트 필터(type/eventType enum 검증·날짜·where>cursor·응답 계약 불변·logs eventType 미검증 보강) + messages/logs 페이지 UI(타입 select+날짜 입력, logs EVENT_TYPES 전체 enum). **r2/r3: codex P3 입력 strictness 즉시 강화**(ISO instant 정규식·달력 round-trip·parseInstant calendar 컴포넌트 검증 — JS Date 롤오버/offset 함정 차단, 클라/서버 일관). 듀얼 수렴 **codex PASS(r1 WARNING P3×2→r2 P3×1→r3 PASS)·evaluator WARNING 9.85**(P0/P1/P2 0, fixNow 0, P3×3 defer). 게이트 4/4(tsc0/lint0err/vitest 293→333[+40]/build0). **다음: WI-031(CSV 내보내기: 멤버/로그/analytics)** — 사용자 선택 범위 잔여 1건. **교훈: date-only 서버파싱은 UTC 함정→클라가 로컬자정 ISO instant 변환 전송 / JS new Date()는 calendar-invalid 조용히 롤오버→컴포넌트 범위 직접 검증(offset은 UTC 비교 불가) / enum 직접주입(where.X=raw)은 Prisma enum 오타 시 500→normalizeEnumFilter 전수검증 400.**

## ✅ WI-029-fix (2026-06-23) — 어드민 대시보드 품질 마감 [PR#30 머지] — 상세는 fix_plan.md Done 참조

## ✅ develop→main 승격 완료(2026-06-23, PR#29 rebase merge `ba3a82f`) — WI-017/018/019/021/022/023/024(7건) **라이브 반영**. 승격 readiness: 적대 통합 감사 4에이전트 워크플로우 **GO·블로커0**(P3 1건=test-chibi 진단스크립트 라이브무관) + codex consult GO. **실행**: Vercel rebase 머지로 author=flowcoder25 보존(gh 계정=yonghyeon-dev라 일반 merge 시 비-인가 작성자 위험 → rebase 필수, 직전 PR#18 패턴) → **프로덕션 ● Ready**(flowcoder25-1055, 1m) + **OCI 소켓 자동배포 success**(main push가 deploy-socket.yml 트리거, 34s, 헬스체크 통과, WI-018 fail-fast 신코드 부팅 → 핸드셰이크 200+sid=AUTH_SECRET 유효·prod 부팅 정상 라이브 실증) + prod DB 신규 마이그레이션 없음(no-op). **라이브 검증**: 웹/로그인 200·API 인증게이트 307(보호 정상). **남은 사용자 검증(자격증명 필요)**: 인증 소켓 e2e smoke(로그인→공간입장→이동동기화/채팅). **롤백**: Vercel 이전배포 promote / OCI `flowspace-v2_socket:rollback-pre-promo` / main revert PR. develop=main back-sync(rebase SHA 분기 정합). READY 큐 비어 있음. BACKLOG: WI-025/026/028. **선택 사용자 게이트: WI-024 백필 `--apply` prod 실행**. **다음 우선: 어드민 대시보드 완성(범위 확정 필요 — 기존 9컴포넌트+6서브페이지 구축됨, 미완성 마커 없음).**

## ✅ WI-017-fix (2026-06-23) — 소켓 토큰 발급 재시도 + 원인별 실패 분류 폴백 (클라 UX/견고성) [PR#29 승격됨]
`getSocketClient()`의 토큰 발급 fetch(`/api/socket/token`)가 `!res.ok`면 즉시 throw + 재시도/명확한 에러 부재 → 세션만료(401)·일시적 5xx(AUTH_SECRET 오설정 fail-closed 포함)·네트워크 오류가 전부 use-socket catch의 generic `"소켓 연결에 실패했습니다."`로 접히던 클라 UX 결함 해소. **READY 큐 소진 → 설계 codex consult로 BACKLOG 4건(WI-017/025/026/028) 우선순위 협의 → WI-017 선정**(실재 클라 결함·단일 격리·정책 명확 / WI-025 정책미정·실표면0 / WI-026 응답누출 이미 차단 / WI-028 표면 큼). **type=improve enum 외 → `fix` 분류**. **해소 (r1 `fa250bc`)**: `fetchSocketToken(opts?)` 추출 — 일시적 실패(fetch 예외·408/429/5xx) `SOCKET_TOKEN_RETRY_DELAYS_MS=[300,900]ms` 백오프 총3시도, 인증(401/403)·비-재시도 4xx·형식오류(token 누락/빈/json예외) 즉시실패. `SocketTokenError{code}`(UNAUTHORIZED/TEMPORARY_FAILURE/NETWORK_FAILURE/INVALID_RESPONSE)→use-socket `socketTokenErrorMessage` 사용자 메시지 매핑(communication.md #8 정합). 익명 폴백 없음(서버 verifySocketToken 토큰 필수 계약, codex consult 확인 — 폴백=명확한 실패 상태). 테스트 주입 `sleep` seam으로 백오프 즉시화(프로덕션 무변). **r2 `d87ac75` — codex 검증 P2×2(fixNow) 해소**(retry가 토큰 발급 await 윈도우를 넓혀 악화시킨 동시성/생명주기 race, codex consult로 fix 설계 검증): (P2-1) `getSocketClient` module-level `pendingClient` promise 가드 — in-flight 생성 중 동시 호출 동일 promise 공유(중복 fetch/io로 마지막만 남고 나머지 누수되던 race 차단), `finally { if (pendingClient === pending) pendingClient = null }`. (P2-2) use-socket `connectAttemptRef` generation 카운터 — `!mounted` 분기에서 `connectAttemptRef.current === attemptId`(후속 effect 없음)일 때만 orphan 정리, 성공/catch도 generation 가드. **공유 pending이 같은 sock 반환 → socket identity로 소유권 판정 불가 → generation 필요**(codex 적출 — stale effect가 후속 effect 소켓 끊는 race). **무회귀**: io() 옵션·싱글턴·reconnection(communication.md #2) 보존, generic 폴백·이벤트 핸들러·토큰 계약 무손상, `fetchSocketToken`/`SocketTokenError` 배럴 미노출. 회귀 테스트 +19(socket-client 17: 재시도 분류 전수 + pending dedupe/재사용/io 옵션 / use-socket 2: 생명주기 race, generation 가드 무력화 시 FAIL 변이검증 실증). 기계게이트 4/4(tsc0/lint0err[선재 LiveKit 경고1·WI무관]/vitest 249→**268**[+19]/build0). **클라 전용 — server/ 미참조 실측(서버 esbuild 번들 무관)**. **듀얼 블라인드 수렴: codex PASS**(r1 WARNING P2×2 → r2 PASS 0 issues, 능동 실측 tsc/lint/git diff --check) **· evaluator WARNING 9.83**(P0/P1/P2 0, fixNow 0, P3×3 defer: ①finally `===pending` 가드 변이 미보호[M6 — 2세대 인터리빙은 `if(pendingClient)return` early-return이 막아 **현 설계 도달 불가** dead-defensive·최악 transient fetch 1회·누수 아님] ②메시지 매핑 단언 테스트 부재[순수 표시·exhaustive switch] ③networkErr.message interpolation[상대 URL 시크릿0·UI 미전파·console 로그만]) + `.pass`(empty `e3b0c44`). evaluator 변이검증 6종(M1 generation/M2 pending dedupe/M3 401 즉시실패/M4 retry off-by-one/M5 orphan cleanup 검출·M6만 미검출=P3, 원복 clean). **핵심 교훈**: (a) **클라 토큰 발급은 io() reconnection(소켓 생성 후)과 별개 레이어 — fetch 실패 재시도가 따로 필요**. (b) **비동기 생성 헬퍼에 retry 추가는 await 윈도우를 넓혀 기존 동시성/생명주기 race를 악화 → pending 공유 가드 + effect generation 가드가 짝**. (c) **공유 pending이 같은 객체 반환 시 stale-effect cleanup은 socket identity로 소유권 판정 불가 → generation 카운터 필요**(codex 적출). impl r1 `fa250bc`/r2 `d87ac75`.

## ✅ WI-018-feat (2026-06-22) — 소켓 서버 부팅 env fail-fast (운영 안전)
별도 socket.io 서버(`server/index.ts`)가 AUTH_SECRET을 **첫 연결 시점에만 lazy** 검증(`io.use`→`verifySocketToken`→`getAuthSecret()` throw→connection 거부)해, AUTH_SECRET 미설정/단문 컨테이너가 healthcheck(포트 200)를 통과해 "healthy"로 뜬 뒤 **모든 연결을 조용히 거부**하던 운영 위험(silent degrade) 차단. `httpServer.listen()` 전 eager 검증으로 fail-fast. **설계 codex consult 1R 수렴**: 큐 소진 상태에서 BACKLOG 4건 우선순위 협의 → WI-018 선정(서버 fail-fast가 근본, WI-017 클라 UX는 후속) + AUTH_SECRET hard-fail / SOCKET_INTERNAL_SECRET warn 구분 + production gating + `getAuthSecret` 재사용. **해소**: 신규 순수 모듈 `src/lib/socket-startup.ts` `validateSocketStartupConfig()` — (A) AUTH_SECRET(필수): `getAuthSecret()` try/catch eager 호출(정책 재구현 금지), production+미설정/32자 미만→throw(listen 전 crash), 비-prod→경고만(dev/test 동작 보존·lazy 검증 유지). (B) SOCKET_INTERNAL_SECRET(선택): 의도된 graceful degrade(enforce 비활성·DB 제재 동작)라 production 경고만(선택값 필수 승격 안 함). (C) 시크릿 값 메시지 미포함. `server/index.ts` listen 전 호출+경고 로깅. `Dockerfile.socket` COPY + `deploy-socket.yml` paths. **번들 COPY 완전성**: esbuild `--metafile`로 런타임 src 입력 4개(chat-constants·enforce/contract·auth-secret·**socket-startup**) 실증·전부 COPY(socket-events.ts는 type-only elide, WI-012-1 교훈). **라이브 PoC 2종**: prod+단문 AUTH_SECRET→`server/index.ts:24`에서 throw·exit1·listen 전(스택)·시크릿 미노출·"Server running" 미출력 / prod+유효+SOCKET_INTERNAL_SECRET 미설정→부팅+경고(degrade). 회귀 테스트 +11. **무회귀**: per-connection lazy 검증 무손상(eager validator는 additive). `NODE_ENV=production`은 Dockerfile.socket:55·docker-compose.prod.yml:14 양쪽 명시(gating 신뢰). 기계게이트 4/4(tsc0/lint0err/vitest 238→**249**[+11]/build0) + esbuild 번들 0 + metafile. **듀얼 블라인드 1R: codex PASS 0 issues · evaluator WARNING 9.8**(P0/P1/P2 0, fixNow 0, P3×3 defer: prod-throw 카나리 약한 오라클[거짓통과 아님]·NODE_ENV 엄격일치[SoT 정합]·top-level 부수효과[단일 진입점]) + `.pass`(empty `e3b0c44`). evaluator 변이검증 3종(prod throw 무력화→3 FAIL·always-throw→3 FAIL·dev 시크릿 누출→1 FAIL, 원복 clean). **핵심 교훈**: (a) **lazy 검증 컨테이너는 healthcheck를 통과해 silent degrade — 필수 env는 listen 전 eager 검증으로 fail-fast해야 오설정이 트래픽을 받지 않는다.** (b) **선택 기능(graceful degrade)을 fail-fast WI에서 필수로 올리지 말 것**(운영 동작 변경 폭 확대) — AUTH_SECRET만 hard-fail, SOCKET_INTERNAL_SECRET은 warn. (c) **vitest `include: src/**`라 server/ 미스캔 → 테스트 가능한 순수 validator는 `src/lib/`에 위치**(getAuthSecret 선례와 동일·서버 번들도 import). (d) **codex consult 놓칠 위험: eager 검증은 AUTH_SECRET 존재·길이만 봄 — Vercel↔OCI 키 불일치는 못 잡음**(컨테이너 healthy+전 토큰 검증 실패)→WI-028(배포 후 synthetic probe). impl `e8d2f16`.

## ✅ WI-024-fix (2026-06-22) — assets metadata.error 정보 노출 차단 (CWE-209, A+B+백필)
비동기 에셋 생성 실패 시 raw `error.message`가 `GeneratedAsset.metadata.error`에 저장되고 WI-019 `PUBLIC_METADATA_KEYS`('error' 포함) 경유 `GET /api/assets/[id]`(owner-gated) 응답에 도달하던 CWE-209(WI-023 evaluator 발굴·동일 클래스). **설계 codex consult 1R 수렴 — C안(A+B)**: B 단독은 **기존 DB 행이 이미 raw 보유**라 불완전(과거 행이 응답에 새 나감) → A(allowlist 제외)로 기존·신규 응답 계층 차단 필수. **해소**: (A) `PUBLIC_METADATA_KEYS`에서 `error` 제거(기존 raw 행 즉시 차단·심층방어, 폴링은 status=FAILED로 충분). (B) generate/batch `.catch`가 raw는 `console.error` 서버 로그만·DB엔 generic `GENERATION_FAILURE_MESSAGE`(constants.ts 정의+배럴) 저장(internalErrorResponse 정책 정합·근본 차단, batch는 batchId 보존). (백필) `scripts/backfill-failed-asset-error.mjs`(dry-run 기본·`--apply`만·멱등·error 키만 치환·prod=사용자 게이트). **무회귀**: `metadata.error` 읽는 런타임 소비처 부재(전수 grep — game-loader는 metadata spread하나 `.error` 미사용, sprite-generator는 frameW/H만, 유일 참조는 합성 테스트). 회귀 테스트 +6(public-asset L170 보존→차단 뒤집기+generic 제거+forbidden에 error / generate·batch 실패경로 raw 미저장·console.error 보존·비-Error 거부값, fire-and-forget은 `vi.waitFor` 검증). 기계게이트 4/4(tsc0/lint0err/vitest 232→**238**/build0). 듀얼 블라인드 1R: **codex PASS 0 issues · evaluator WARNING 9.85**(P0/P1 0, fixNow 0, P3×3 defer: 백필 상수 미러링·`.catch` 2차 update unhandled rejection[선재]·DB 잔존 raw[backfill 전·응답누출0 심층방어]) + `.pass`(fingerprint empty `e3b0c44`). evaluator 변이검증 3종 실증(error 복원→3 FAIL·raw 저장 복원→2 FAIL·console.error 제거→1 FAIL·전부 원복 clean). **핵심 교훈**: (a) **응답 allowlist 제거(A)와 저장 정규화(B)는 보완 — A 없이 B만으론 기존 DB 행이 새 나간다(codex consult 적출 결정적 허점)**. (b) **DB metadata에 중복 저장된 민감값은 응답 계층(allowlist)과 저장 계층(정규화) 양쪽에서 막아야 완결**(WI-019 패턴 연장). (c) **fire-and-forget `.catch` 검증 = mock reject + `vi.waitFor`로 detached promise settle 대기**(비-플래키). codex 위험#5(성공 경로 metadata 전체 저장면 확대)→**WI-026** 분리. impl `3cd7d04`.

## ✅ WI-023-fix (2026-06-22) — 500 응답 details 정보 노출 일괄 제거 (CWE-209, 중앙 에러 헬퍼)
다수 API 라우트가 catch 블록 500 응답에 `details: error instanceof Error ? error.message : undefined`로 원본 에러(Prisma 제약/컬럼명·내부 경로·스택)를 클라이언트에 노출하던 것을 중앙 헬퍼로 일괄 제거. **실측 발굴**: WI-022 듀얼검증 defer P3는 `assets/route.ts:124` 단독 등록이었으나 전수 grep 결과 **26파일 37곳** 전역 표면(spaces·members·messages·admin/*·map/*·join·assets/*·workflows·guest·auth/register — messages 라우트 포함, "messages와 정렬" 전제 오류). 설계 codex consult 1R 수렴(A 채택·26파일 일괄·헬퍼·offset 분리·dev에서도 details 미반환·code 미추가). **해소**: ①신규 `src/lib/api-error.ts` `internalErrorResponse(context, error, message)` — 원본 에러는 `console.error("[context]", error)`로 서버 로그에만, 클라엔 `{ error: message }` 500만(details 환경 무관 영구 미반환). ②26 라우트 500 catch 37곳을 `internalErrorResponse("<METHOD> <path>", error, "<메시지 verbatim>")`로 치환(메시지 보존·code 미추가·키집합 `{error}`). ③DB metadata.error(500 아님)·제외 3라우트(socket/token·livekit/webhook·livekit/token, 원래 안전) 미변경. 테스트: 헬퍼 5종 + 대표 라우트 3곳 500 details 미노출 단언. 무회귀(`.details` 소비처 0). **핵심 교훈**: (a) **deny-by-default 정보위생은 단일 라우트가 아닌 전 catch 블록 전역 표면을 grep으로 실측해야 한다** — defer P3 등록 시의 "특정 파일·다른 라우트와 정렬" 전제가 실측과 다를 수 있음(messages 라우트도 누출 중이었음). (b) **details 제거만으로는 디버깅 정보 소실** — 기존 26 라우트 catch엔 서버 로깅이 없어 헬퍼가 console.error로 보존해야 함. (c) **mechanical sweep은 Workflow 팬아웃(파일별 1에이전트)으로 처리하되 중앙 grep+게이트로 검증** — 26에이전트 37치환·0 not-clean, DB metadata.error(비-500) 정확히 미변경. 기계게이트 4/4(tsc0/lint0err/vitest 227→**232**/build0). 잔여 누출 grep 0. 듀얼 블라인드 1R: **codex PASS 0 issues · evaluator WARNING 9.85**(P3×2 defer: metadata.error→WI-024 · console.error 패턴 정합) + `.pass`(fingerprint empty `e3b0c44`). evaluator 변이검증(details 복원→6테스트 FAIL·원복 diff0). impl `d352c45`. **develop PR 대기**.

## ✅ WI-022-fix (2026-06-22) — assets 목록 GET 입력 강건화 (enum 검증 + page/limit 정규화)
`GET /api/assets`(목록) 선재 입력 검증 부채 3건 해소(WI-021 듀얼검증 발굴, codex consult 별 WI 분리; **누출 아님** — 응답 누출은 WI-021 완료). ① `type`/`status` enum 무검증(`type.toUpperCase()` 직접 주입→오타시 Prisma 500) → 신규 `normalizeEnumFilter(values:string[], allowed)`가 `searchParams.getAll()` 전 값을 `trim().toUpperCase()` 후 `Object.values(AssetType)`/`Object.values(AssetStatus)`(**Prisma 런타임 enum** — 하드코딩 배열 회피) allowlist로 **전수 검증**(`@prisma/client`가 enum을 런타임 객체로 export 실측), 빈값/공백-only → 필터 미적용, 하나라도 불일치 → 400 `{error,code:"INVALID_FILTER"}`(WI-009 패턴), 전부 유효 → 첫 값. ② `limit` 무상한/NaN → `src/lib/pagination.ts` `parsePageLimit`에 **optional `defaultLimit` param 추가** → `parsePageLimit(raw,20)`로 assets default 20 보존(codex consult: 자매 50 통일[B']보다 무회귀 우선[B]) + MAX 100 cap. ③ `page` NaN/0/음수→음수 skip 500 → 신규 `parsePageNumber(raw)` 1 클램프. **핵심 교훈**: (a) **`searchParams.get()`은 첫 값만 봐 중복 파라미터(`?type=character&type=weapon`)의 invalid 2번째가 검증 우회 → `getAll()` 전수 검증 필요**(codex r2 fixNow 적출). (b) **공백-only(`?type=%20`)는 trim-first로 빈값=미지정 취급**(codex r1 fixNow). (c) **기존 공용 헬퍼 계약 보존**: `pagination.test.ts`가 `parsePageLimit("12.9")→12`(소수절삭)를 못박아 codex의 '엄격 파싱' 권고는 회귀 유발 → optional param만 추가하고 부분파싱 동작 보존. (d) **무회귀 핵심 = `.toUpperCase()` 후 검증**(소비처 `asset-loader`가 소문자 `status=completed` 전송). 설계 codex consult 1R + 듀얼 블라인드 **3R 수렴**(codex WARNING→WARNING→PASS, 매 라운드 fixNow P3 즉시 해소 · evaluator 최종HEAD 9.88, P3×3 defer, 변이검증 3종 실증) + `.pass`(`0c66c8a5`). 기계게이트 4/4(tsc0/lint0/vitest 204→**227**[+23]/build0). impl `50a6e6e`/r2 `f2748a3`/r3 `0cad729`. **WI-021 evaluator defer P3 2건(enum 무검증·limit 무상한) 흡수.** 잔여 defer(500 details 노출·큰 offset 상한·타입 nit)→WI-023(선택). **develop 머지 완료 (PR#24, merge `5469f99`)**. main 미승격.

## ✅ WI-021-fix (2026-06-22) — assets 목록 GET 응답 lean DTO 정형화
`GET /api/assets`(목록)가 `select`에 `prompt: true`를 포함해 raw 행을 그대로 반환하던 것을 lean 전용 DTO(`toPublicAssetListItem`)로 정형화. **핵심: 이 라우트의 `shared=true` 분기는 `where.isShared=true`로 owner 게이트 없이 타인 공유 자산을 누구에게나 반환** → 상세(WI-019, owner-gated)보다 노출 표면이 넓다. **설계 codex consult 적출**: 상세용 `toPublicGeneratedAsset` 전체 재사용을 **의도적으로 회피** — 재사용 시 (a) `metadata`가 목록에 추가돼 game-loader frameWidth 폴백(64)이 실측값으로 바뀌고(보안 수정의 부수효과 = 동작 변경), (b) `shared=true`에서 타인 `user{id,name}`·metadata가 신규 노출. 따라서 lean DTO(8키: `id,type,name,status,filePath,thumbnailPath,createdAt,updatedAt`)로 분리, `prompt`/`workflow`/`comfyuiJobId`(민감)·`metadata`·`user`·`userId`를 **select(쿼리단) + transform(응답단) 이중 제외**(우발적 select 확장에도 응답 키 집합 고정). 무회귀(목록 소비처 2곳 실측: `asset-loader.ts`=id/type/filePath/metadata.frameWidth — **현재도 목록 metadata 미반환·64 폴백이 기존 동작**이라 metadata 제거가 game-loader를 깨지 않음 / `asset-palette.tsx`=id/type/name/thumbnailPath/filePath 전부 보존). 회귀 테스트 12(`route.test.ts` 신규). 기계게이트 4/4(tsc0/lint0/vitest 192→**204·204**[+12]/build0). 설계 codex consult 1R + **듀얼 블라인드 1R 수렴: codex PASS 0 issues · evaluator WARNING 9.81**(P0/P1 0, P3x3 defer: enum 무검증·limit 무상한[둘 다 선재→**WI-022**]·중복 인터페이스 nit) + `.pass`(`d8a1b53f`). evaluator 변이검증 2종 실증(DTO에 user+metadata 주입→3 FAIL·select에 prompt 복원→1 FAIL). **교훈: 목록(공유 분기 owner 게이트 없음)과 상세(owner-gated)는 권한 경계가 달라 응답 DTO를 통일하면 누출 회귀가 쉬워진다 — lean 분리가 옳음.** impl `5cd3f85`. **develop 머지 완료 (PR#23, merge `13c3b55`)**. 선재 입력검증 부채(type/status enum 무검증·limit 무상한)는 codex consult 권고대로 WI-022 분리(limit 무상한 우선순위 높게).

## ✅ WI-019-fix (2026-06-22) — assets 상세 GET 응답 allowlist
`GET /api/assets/[id]`가 raw `GeneratedAsset` 전체 행(+민감 `prompt`/`workflow`/`comfyuiJobId`)을 반환하던 것을 `select` + 공개 DTO(`toPublicGeneratedAsset`, `features/assets/internal/public-asset.ts`+배럴)로 정형화 (WI-014 동일 클래스·별 도메인, owner/superAdmin 게이트 하 정보위생). **핵심(설계 codex consult 적출)**: 민감 3필드가 `metadata`(Json) 컬럼에도 중복 저장(generate/route.ts:90·batch/route.ts:79가 `GeneratedAssetMetadata` 전체를 JSON 저장) → top-level allowlist만으론 metadata 우회 노출 → `PUBLIC_METADATA_KEYS`(`width,height,frameWidth,frameHeight,columns,rows,format,seed,generatedAt,processingTime,error`)로 metadata 정규화. `userId`는 권한판정용 select·DTO 미포함. 무회귀(소비처 3곳 실측: game-loader/sprite-generator/test-chibi-generation 필요필드 전부 보존). 듀얼 블라인드 1R 수렴 **codex WARNING**(P3x1 defer: metadata 테스트 self-referential 오라클 — 실누출 없음 명시 → **WI-011 교훈대로 즉시 해소**: fixture 민감 extras + 리터럴 exact-key-set 단언 + 상수 가드, 변이검증[accessSecret allowlist 추가 시 4 테스트 FAIL])·**evaluator WARNING 9.78**(P0/P1 0, P3x3 defer) + `.pass`(`0b92337e`). 기계게이트 4/4(tsc0/lint0/vitest 192/build0). impl `d725837`/강화 `5fef58d`. **교훈**: 응답 정형화 시 raw 스칼라뿐 아니라 **`metadata`(Json) 컬럼에 중복 저장된 민감필드까지 정규화**해야 함(generate/batch가 `GeneratedAssetMetadata` 전체를 통째 저장 → top-level allowlist 우회). 순수 헬퍼+배럴(WI-004 safe-path 패턴) 재사용 → **WI-021**(목록 `GET /api/assets`)에서 `toPublicGeneratedAsset` 재활용 가능.

## 이전 상태 (참고)
**✅ develop→main 승격 완료(2026-06-22)** + **✅ WI-020-fix(로그인 로고 회귀) 완료·승격·라이브검증** + V1→V2 컷오버 e2e 검증(사용자 정상확인). WI-001~016 + WI-020 라이브 반영. BACKLOG: WI-017(소켓토큰 폴백)/WI-018(prod env fail-fast)/WI-021(assets 목록 GET).

## ✅ WI-020-fix (2026-06-22) — 미들웨어 public 정적에셋 회귀 (사용자 발견)
미인증 시 `/Logo.png`이 `/login`으로 307 리다이렉트 → 로그인 페이지 로고 깨짐(WI-001 strict화 부작용, 승격으로 라이브화). 순수함수 `src/lib/route-access.ts`(isPublicRequest + PUBLIC_FILES allowlist) 추출, 미들웨어 호출. **확장자 전체통과 거부·명시 allowlist**(codex 적대 협의: 확장자 우회 표면 회피). 회귀테스트 9. 듀얼검증 codex WARNING·evaluator 9.78(P0/P1 0, 변이검증 5종). PR#20→develop, PR#21 승격→main `9cde274`. **라이브 검증: /Logo.png 307→200 image/png**. 교훈: **미들웨어 deny-by-default 하드닝(WI-001) 시 public/ 루트 정적파일이 보호라우트로 잡히는 회귀** — matcher는 `_next/static·_next/image·favicon.ico`만 제외하므로 public 루트파일(Logo.png)은 코드 allowlist 필요.

## ✅ 승격 완료 (2026-06-22) — PR#18 rebase 머지
사용자 지시("코덱스와 협의하여 작업 진행" → "직접 확인해봐" + "양쪽다 머지해")로 승격 실행:
- **머지**: develop→main **PR#18 rebase 머지** → main HEAD `50d9af0`(author=`flowcoder25@gmail.com` **보존** — rebase가 author 유지해 Vercel 인가 통과). main↔develop는 rebase로 SHA 분기 → **back-sync 완료**.
- **Vercel(웹)**: Production 배포 **● Ready** — 신 WI-001~016 코드 라이브(`flowspace-v2.vercel.app` HTTP200, `/api/socket/token` 307=NextAuth 리다이렉트, 500 아님).
- **prod DB**: `prisma migrate deploy` 적용 — WI-013 `Space_status_updatedAt_id_idx` 생성+구 `Space_status_idx` 제거(**Space 7행라 즉시·무락, CONCURRENTLY 불필요**), WI-016 `isShared` no-op(IF NOT EXISTS, prod 컬럼·인덱스 기존재). `_prisma_migrations` 3건 SoT 정합.
- **사전검증 전수 실측**: 기계게이트(tsc0/vitest165/build0) · OCI esbuild 번들+metafile COPY 완전성 · **🔴AUTH_SECRET 해소**(라이브 OCI `socket-v2.144.24.72.143.nip.io`가 로컬 `.env` AUTH_SECRET[44자]로 서명한 JWT 검증 성공 → prod OCI=44자≥32 확정; live 웹 HTTP200 → Vercel==OCI transitive) · WI-016 prod 컬럼 존재 prod DB 직접확인 · codex consult 1R + 적대검증 워크플로우 19에이전트(승격차단 코드결함 0). 산출물: `.flowset/promotion-readiness.md` + `.flowset/promotion-approval.json` + `eval-results/PROMOTION-consult*`.

## ✅ OCI 소켓 배포도 완료 (2026-06-22, 사용자 "ssh키 있잖아" 지시)
- 키 `~/.ssh/flowspace-oci`로 OCI(`ubuntu@144.24.72.143`) SSH 접속 → `git pull origin main`(06cf9c5→50d9af0 FF) + 빌드 + 재배포.
- **신 소켓 컨테이너 `Up (healthy)` 포트 3002** — 로그 정상(Server running·CORS·**실사용자 접속·룸 입장** [Socket] Connected/[Room] 조용현 joined). 로컬 3002=200, 외부 nip.io=200. **신 코드 AUTH_SECRET 검증이 라이브 Vercel 토큰과 end-to-end 작동 실증.**
- **⚠️ OCI 배포 인프라 함정(다음 배포 시 주의)**: (1) `deploy-socket.yml` 자동배포는 `OCI_SSH_PRIVATE_KEY` repo Secret **미설정**이라 과거 전수 실패 → 수동 배포만 가능(키는 로컬 `~/.ssh/flowspace-oci`에 있음). (2) OCI는 **docker-compose v1(1.29.2)** 만 설치(yml의 `docker compose -f` v2 명령 미존재) + **v1 recreate 버그** `KeyError: 'ContainerConfig'`(신 Docker 엔진 비호환). **우회 배포 절차**: `docker-compose -f docker-compose.prod.yml build socket` → `docker-compose -f docker-compose.prod.yml rm -f -s socket` → `docker-compose -f docker-compose.prod.yml up -d --no-build socket`(구 컨테이너 제거 후 fresh up = recreate 경로 회피). 롤백 이미지 `flowspace-v2_socket:rollback-pre-promo`(4759e28a3e68) 보존.

## ✅ OCI 자동배포(CD) 복구 완료 (2026-06-22)
사용자 "flowcoder25@gmail.com으로 자동배포 설정해놔야지" 지시로 복구:
- `OCI_SSH_PRIVATE_KEY` repo secret 등록(`~/.ssh/flowspace-oci` 내용) — **과거 전수 실패의 근본 원인이던 미설정 해소**.
- `deploy-socket.yml` 수정(PR#19 main 반영): `docker compose`(v2 미설치)→`docker-compose`(v1) + recreate 버그(KeyError ContainerConfig) 우회(build→`rm -f -s socket`→`up -d --no-build`) + `workflow_dispatch`(수동 트리거) + 배포 후 헬스체크(실패 시 exit1).
- **테스트 실증**: `gh workflow run deploy-socket.yml` → **✓ deploy 25s 성공**(과거 8초 즉시실패 대비), 로그 `✅ socket healthy`. 향후 main push(server/**·Dockerfile.socket 등) 시 자동 배포됨.

## ✅ V1→V2 도메인 컷오버 (2026-06-22) — Codex 적대 협의·검증 거침
사용자 지시("V1 DB·버셀 죽이고 도메인 유지한 채 V2 넣기" + "코덱스 적대 협의 프로세스")로 실행. **Codex consult(설계) + verify(검증) 양쪽 거침** (`eval-results/CUTOVER-consult*`·`CUTOVER-verify*`).
- **구조**: V1=Vercel `flowspace`+Supabase ref `dqmnlygfulhxhatyoiql`+`space.flow-coder.com` / V2=Vercel `flowspace-v2`+Supabase ref `fqhcnudechuchaazwrzg`+OCI소켓. **DB 분리 확인**(다른 Supabase 프로젝트). Vercel 소유=flowcoder 팀(`vercel login flowcoder25@gmail.com`으로 owner 권한 획득, gh는 yonghyeon-dev).
- **실행(Codex 순서: env→재배포→도메인이동)**: ① OCI Caddy에 `space-socket.flow-coder.com`→:3002(v2-socket 블록에 추가, **sed -i가 ro 바인드마운트 inode 교체→caddy 재시작으로 반영**) ② OCI `.env` CORS += `https://space.flow-coder.com`(소켓 rm+up) ③ V2 env `NEXT_PUBLIC_SOCKET_URL=https://space-socket.flow-coder.com`·`AUTH_URL=https://space.flow-coder.com` ④ V2 재배포 ⑤ 도메인 `space.flow-coder.com` V1→V2 **Vercel REST API**(DELETE v9/projects/{V1}/domains + POST v10/projects/{V2}/domains; CLI `domains add --force`는 이 버전서 400/1-arg버그라 API 사용. 토큰=`%APPDATA%/com.vercel.cli/Data/auth.json`).
- **검증됨**: space.flow-coder.com→V2 서빙(title 일치)·V2 도메인목록 반영·Google OAuth(space redirect 등록, 사용자 확인)·CORS·space-socket 핸드셰이크·AUTH_SECRET 불변.
- **✅ e2e 검증 완료(사용자 '정상확인', 2026-06-22)**: space.flow-coder.com Google 로그인→공간입장→동기화→미디어 정상. Codex 지적 위험(옛 쿠키·소켓 WebSocket·LiveKit) 전부 통과. **컷오버 완료 선언.**
- **V1 = quarantine**(도메인만 제거, 미삭제). **삭제 보류**(Codex: ~2026-07-06[7~14일]+공유자원 감사[Supabase Storage 객체=DB백업 미포함·OAuth클라·webhook·cron]+V1 DB 백업 후 Vercel `flowspace`+Supabase `dqmnlygfulhxhatyoiql` 삭제). 롤백=Vercel move API(`POST /v1/projects/flowspace-v2/domains/space.flow-coder.com/move {projectId:V1}`).
- **WI-020-fix**(로그인 로고 회귀) 완료·승격·라이브검증(별도 섹션) — 컷오버 중 사용자 발견.

## ⚠️ 남은 작업
- **🔴 정식 도메인 DNS 미설정(사용자 — DNS 권한)**: `v2-socket.flow-coder.com`이 **NXDOMAIN**(DNS 레코드 없음, nslookup non-existent). Caddy엔 라우트 존재하나 DNS가 없어 죽음(HTTP000). **라이브는 임시 `socket-v2.144.24.72.143.nip.io`로 운영 중**(Caddyfile 주석 "nip.io 임시(Cloudflare DNS 전)"). flow-coder.com NS=`ns1.cnmnoc.co.kr`. 정식 도메인 쓰려면 DNS 패널에서 `v2-socket` A레코드(→144.24.72.143) 추가 + Vercel `NEXT_PUBLIC_SOCKET_URL` 갱신(현재 nip.io 추정). **방치해도 라이브 정상**(nip.io 작동) — 정식 도메인 전환은 선택.
- **WI-005 실시간 제재 활성화(선택)**: OCI `.env` `SOCKET_INTERNAL_SECRET` 미설정 → enforce degrade(제재는 DB+재접속 차단 동작, 즉시추방만 미작동). 활성=OCI `.env` `SOCKET_INTERNAL_SECRET` + Vercel `SOCKET_INTERNAL_SECRET`(동일)·`SOCKET_INTERNAL_URL` 설정(Vercel env는 사용자 권한 — 내 vercel 계정 team env 쓰기 불가) 후 OCI 컨테이너 재시작.
- **WI-013 prod EXPLAIN**: Space 7행이라 현재 무의미, 데이터 증가 후.

(승격 게이트 전부 해소. 백로그 WI-017~019는 READY 아님. READY 큐 비어 있음.)
- 그 외: 신규 WI는 사용자 요구·실사용·실측에서 발굴. 백로그 WI-017(소켓토큰 폴백)/018(prod env fail-fast)/019(assets GET 응답 정형화)는 READY 아님. 시작 전 `.flowset/HANDOFF.md`+`.flowset/fix_plan.md` 읽기.
- **진행 방식(사용자 확정 2026-06-21)**: 모든 WI는 develop 정상 플로우 — 분기 → 기계게이트(tsc/lint/vitest/build) → 듀얼검증(codex CLI + evaluator-agent) → `.pass` → **develop PR 머지**. 경계/prod 영향 WI는 구현 전 codex consult(process 02). 라이브 반영은 develop→main **승격**(process 07; 승인 + 작성자 `flowcoder25@gmail.com`).

## Done 추가 (이번 세션, 2026-06-22 — 승격前 통합감사 + 신규결함 3건)
- **통합감사**: 큐 소진 상태에서 codex consult(다음단계) + 9축 멀티에이전트 워크플로우 감사(46 에이전트) + 적대검증. **결론: develop은 승격 건전, 승격차단 코드결함 0.** 감사가 P0로 올린 "main에 enforce.ts/auth-secret.ts 없음→장애"류는 **false-positive**(develop⊋main=승격 목적, git merge가 원자적 통합 — 세 파일 develop 트리 존재 확인). 실제 go-live 게이트 = AUTH_SECRET(최대위험)·WI-013/016 prod 적용·SOCKET_INTERNAL_* env. 진짜 신규결함 3건 발굴 → 사용자 선택으로 처리.
- **WI-014-fix**(보안/정보위생): POST `/api/spaces`·PATCH `/api/spaces/[id]`·PATCH+GET `/api/spaces/[id]/(admin/)members` 응답을 select allowlist로 정형화(inviteCode/accessSecret/restricted* 미반환, GET 원칙 정합). **codex r1 FAIL(P1 fixNow: GET admin/members raw가 member-table onRefresh로 PATCH 정형화 무력화) → r2 PASS** · evaluator 9.79. PR#15(impl `e5397fc`/r2 `45b67ae`).
- **WI-015-fix**(Phaser tween 누수): **Phaser는 GameObject.destroy 시 tween 자동제거 안 함** → destroy서 `killTweensOf` 필요. 감사 2건이 **듀얼검증 3R에서 동일 클래스 5곳 확장**: RemotePlayerSprite·**LocalPlayer**(실 호출자)·**InteractiveObject**(repeat:-1 무한 tween)·MainScene scene-active 가드. game/ tweens.add 3파일 전수 커버. **codex 3R FAIL→FAIL→PASS** · evaluator 9.46→9.75→9.78. PR#16(impl `57475c7`/r2 `fe302d3`/r3 `33cdbb0`).
- **WI-016-chore**(마이그레이션 베이스라인): `GeneratedAsset.isShared`(+복합인덱스) 드리프트(2026-03 db push) 정합. schema 무변경·마이그레이션 파일만 추가(`IF NOT EXISTS` 멱등 — consult 놓친위험[인덱스 부재 은닉] 자가치유) + `migration_lock.toml` 최초 추적. 설계 codex consult 1R. **codex WARNING(P3 env-only) · evaluator 9.83**. PR#17(impl `3e8705b`). prod 적용은 WI-013과 함께 승인 게이트.

## Done 추가 (이번 세션, 2026-06-22)
- **WI-013-perf**: `GET /api/spaces` 핫 쿼리용 Space 복합 인덱스(WI-010 공통 defer 해소). schema `@@index([status])` 제거→`@@index([status, updatedAt(Desc), id(Desc)])` + 마이그레이션 `20260622120000_space_hot_query_index`(CREATE→DROP 순, force-add). 설계 codex consult D1~D5(단일 복합·owned/joined 보류·Prisma 네이티브 full·redundant status 제거·prod CONCURRENTLY). 로컬검증 prisma validate/migrate diff/기계게이트 5/5. 듀얼검증 **codex r1 PASS 0 issues·evaluator 9.78**(P3×2 defer: migration 순서[eca0ff2 해소]·D2 joined 보류[prod-gate 후속]) + `.pass`(`c206e20b`). **develop 머지 완료 (PR#14, merge `f8f4ede`)**. **prod 적용/EXPLAIN은 승인 게이트(미실행)**. 교훈: **prisma/migrations gitignore인데 0_init은 force-add 선례** → 신규 마이그레이션도 `git add -f`. **인덱스 마이그레이션은 prod DB 없이 `prisma migrate diff --from-schema-datamodel <old> --to-schema-datamodel <new> --script`로 오프라인 SQL 생성·일관성 검증 가능**(migrate dev는 prod 건드림 — 금지). **DROP→CREATE보다 CREATE→DROP이 안전**(트랜잭션/CONCURRENTLY 양쪽 인덱스 무공백).
- **WI-012-2-refactor**: WI-012 분할 2/2 cleanup(S3~S6, codex consult D6). **server/ 무관.** **S3** 최상위 `game/index.ts` 배럴이 `game/events` payload 타입 4개만 재노출하던 것을 30개 전체 명시 재노출 + cross-module 소비자(editor 2 + avatar 1)를 events 서브배럴 우회→최상위 배럴 통일 + ESLint `**/game/events` 차단(game 내부 상대 `../events`는 미매칭·오탐0, evaluator probe 실증). **S4** useScreenRecorder close() L263+L357 `.catch(()=>{})` 추가(4경로 통일) + AudioContext 스텁 변이검증 2건(`process.on("unhandledRejection")` 누수 검출, L361 `.catch` 제거 시 FAIL 실증). **S5** messages 2라우트(비-admin/admin) cursor를 `src/lib/pagination.ts` 헬퍼 통일 — 응답 계약 보존(admin hasMore 미추가, D5) + 라우트 테스트 11. **S6** POST/api/spaces 커버리지 201/401/400/404/500+GET500(테스트 전용, `route.ts` 무변경). vitest 128→147. 기계게이트 4/4. 듀얼검증 **codex r1 PASS 0 issues·evaluator 9.8**(P3×1 defer=parsePageLimit 엣지 strict improvement) + `.pass`(`16c001a0`). **develop 머지 완료 (PR#13, merge `986747a`)**. 교훈: **ESLint `**/game/events` glob이 alias(`@/.../game/events`)와 상대(`../../game/events`)를 모두 차단하면서 game 내부 `../events`(game 세그먼트 없음)는 미매칭** — 공개 진입점 단일화에 path-string glob으로 충분. **AudioContext close() reject 변이검증은 `process.on("unhandledRejection")` 추적이 결정적**(close는 await 안 돼 동작 차이가 unhandled rejection뿐).
- **WI-012-1-refactor**: 통신 순수계약 protocol 모듈 분리 + 소켓상수 물리이동(WI-012 분할 1/2, codex consult D6). **순수 리팩토링 — 런타임 동작 변경 0.** 신규 `src/features/space/protocol`(socket-events 타입 + socket-constants RECONNECTION_*/MOVE_THROTTLE_MS + 순수 배럴). `socket/internal/types.ts` 삭제→`socket/index.ts` facade(소비자 4개소 무변경). socket-client/use-socket는 protocol 배럴 소비(DEFAULT_NICKNAME만 chat 잔류, D2). chat 배럴 WI-003 임시 re-export 제거. server 9개소·Dockerfile.socket·deploy-socket.yml·communication.md/event-protocol.md/스펙 갱신. enforce 잔류(이미 안정, D1). **핵심: codex r2가 선재 P1 적출** — `server/middleware/auth.ts` 런타임 의존 `src/lib/auth-secret.ts`(WI-001 80a3adb 도입)가 Dockerfile COPY 누락이라 Docker 빌드 esbuild resolve 실패(향후 main 승격 시 OCI 빌드 깨질 결함)였음 → COPY+paths 보강. **esbuild metafile로 서버 번들 런타임 src 입력 전수(3개) COPY 완전성 실증**(socket-events.ts는 type-only라 elide — 내 로컬 esbuild는 전 src 존재라 통과한 맹점을 metafile diff가 결정). 기계게이트 5/5(+서버 esbuild 번들 + metafile). 듀얼검증 **codex 3R 수렴 PASS**(r1 P3-doc → r2 P1-authsecret+P3-원장 → r3 PASS 0 issues)·**evaluator 10.0**(run2 최종HEAD, metafile COPY 완전성 독립 실증) + `.pass`(`2fc1cc67`). 설계 codex consult 1R. **develop 머지 완료 (PR#12, merge `84ea3d8`, impl `3dac762`)**. 남은 S3~S6 → **WI-012-2-refactor**(READY). 교훈: **로컬 esbuild "전 src 존재"는 Docker COPY 완전성을 검증 못 함 → esbuild `--metafile`의 src 입력 ↔ Dockerfile COPY 목록 diff가 결정적**. `import type`-only 파일은 esbuild가 elide → 번들 입력 아님(socket-events.ts).
- **WI-011-test**: `GET /api/spaces` filter 분기 회귀 + 재사용 API 라우트 테스트 하니스 + `useScreenRecorder` unmount 경로 테스트(WI-006 evaluator P3 흡수). **테스트 전용 — 소스(.ts) 무수정**(route.ts/useScreenRecorder.ts 불변, 양 검증자 `git diff HEAD -- src/` 빈결과 확인). 신규 하니스 `src/__tests__/helpers/api-route.ts`(순수 빌더 — `vi.mock` 프리앰블은 파일로컬 호이스팅이라 빌더만 공용화, 설계 codex consult 합의). route.test.ts 17케이스(filter→scope owned/joined/null·all×일반/슈퍼어드민 권한격리, INVALID_FILTER 400+prisma 미접근, status:ACTIVE 상시, 페이지네이션 take/cursor/skip, cursor에도 scope 보존, 응답 exact-key-set allowlist, POST superAdmin 가드). useScreenRecorder unmount 3케이스(stopping중 언마운트 settle[WI-006 흡수, 변이검증]/언마운트후 stopRecording 즉시resolve/saveFile await중 언마운트 mountedRef 가드[고유 delay setTimeout 식별로 변이검증 가능하게]). vitest 108→128(+20). 기계게이트 4/4. **듀얼검증 codex r1 WARNING P3x3 → r2 PASS 0 issues** · **evaluator r1 9.3 → r2 9.625**(잔여 P3x3 전부 defer=WI-012). 두 검증자 r1에서 'mountedRef 가드 smoke false-pass' 동일 수렴 발견 → 선제 강화(superAdmin scope·exact-key allowlist 포함 3건). 설계 codex consult 1R. + `.pass`(fingerprint `cfdd572c`). **develop 머지 완료 (PR#11, merge `2b91419`)**.

## Done 추가 (이번 세션, 2026-06-22)
- **WI-010-perf**: `GET /api/spaces`가 페이지네이션 없이 전체 ACTIVE 스페이스를 로드하던 스케일 부채(P3, WI-009 슈퍼어드민 전역 `scope={}`) 해소. cursor 페이지네이션(`parsePageLimit` 기본 50·최대 100 + `cursor`, `take:limit+1` → `buildCursorPage` → `{spaces,nextCursor,hasMore}`, 기존 spaces 유지=하위호환) + `orderBy [updatedAt desc, id desc]`(id 타이브레이커) + 모든 스코프 일관. 클라: `space-store` `loadMore`+`_reqId` 토큰(stale 응답 무시) + `space-list-view` "더 보기"(전역 가시성 회귀 방지). 신규 순수 헬퍼 `src/lib/pagination.ts`(+테스트). vitest 92→108. 기계게이트 4/4. **듀얼검증 codex 3R 수렴**(r1 FAIL P2/fixNow `isLoadingMore` 고착 → 수정 `529108e` / r2 WARNING P3 역방향 누수 → 선제 해소 `ed73930` / r3 WARNING P3×1 defer) · evaluator WARNING 9.7(최종 HEAD 재검증, P3×4 defer, 변이검증 5종 검출, 보안 sound) + `.pass`(`2ef1dd0b`). 설계 codex consult 1R(경쟁상태 위험 E 지목). **두 검증자 r1 동일 실결함 독립 발견**. 남은 P3→WI-013-perf(인덱스)/WI-012(cursor검증·messages DRY). **develop 머지 완료 (PR#10, merge `47022a6`, impl `ed73930`)**.

## Done 추가 (이번 세션, 2026-06-22)
- **WI-006-fix**: `useScreenRecorder`의 `recorder.onerror`가 handleError+idle+stopTimer만 하고 `stopRecording()`이 보관한 `pendingStopResolveRef`를 settle하지 않아, 'error' 이벤트 후 `onstop` 미발화 시 `stopRecording()` Promise가 영구 pending이던 P3 결함 해소. `onerror`를 정상 abort로 강화: `onstop=null`(중복 저장 차단)+`audioContext close`+`chunks 폐기`+`mediaRecorderRef===recorder`시 null+`pendingStopResolveRef` settle. resolve 시맨틱은 기존 onstop의 `result.status==="error"` 경로와 일관(오류는 error 상태/onError로 전달, `Promise<void>` 계약 유지). 신규 vitest 4(88→92). 기계게이트 4/4 PASS. 듀얼검증 codex **r1 PASS 0 issues**·evaluator WARNING 9.5(P3×3 defer) + `.pass`(`19ecaf43`). 설계 codex consult 1R(settle-in-onerror가 안전망 타임아웃보다 우수 — saveFile 진행 중 premature resolve 회피). evaluator 뮤테이션 테스트로 결함 검출 실증(pre-fix 복원 시 test1·3 5초 타임아웃 FAIL). **develop PR 대기**(commit `5c9fa16`).

## Done 추가 (이번 세션, 2026-06-22)
- **WI-005-fix**: 접속 중 dashboard 제재(ban/kick/mute/unmute/changeRole)가 DB만 갱신하고 별 호스트 socket.io(OCI 단일 인스턴스, Redis 없음)의 살아있는 연결엔 무영향이던 P2 해소. ban=재접속까지 채팅/이동 지속, kick=row 삭제돼도 소켓 잔존, **changeRole 강등 시 인메모리 role이 admin으로 남아 권한 회수 실패**(codex consult 최대 위험 지목)가 근본. 크로스프로세스 채널 = socket httpServer 내부 `POST /internal/enforce` webhook. 다층 방어: `SOCKET_INTERNAL_SECRET` HMAC-SHA256(`{ts}.{body}`) 서명(비-hex/길이불일치/홀수 선제거 후 timingSafeEqual, 시크릿 없으면 503) + replay ±30s + byte size limit + 스키마 검증 + **DB postcondition 재확인**(ban=BANNED/kick=row null/mute=MUTED/unmute=NONE/role=role) 후 소켓 조작(시크릿 유출→임의추방 확대 차단, 409). ban/kick은 **detachSocketFromSpace로 동기 권한 회수**(자기 id 제외 모든 room[공간+파티] 이탈 + socket.data[spaceId/partyId/role/restriction/memberId] 무효화 → 전 핸들러 spaceId 가드라 grace 동안 잔존권한 0) 후 grace(250ms) 물리 disconnect, 주변엔 member:kicked + player:left 사용자 단위 1회(removeUserPresence). leave:space도 클라 인자 무시(invariant #3)·leaveSpace가 socket.data 무효화하도록 하드닝. Next PATCH는 dispatchEnforcement await(serverless fire-and-forget 금지, 2s timeout, throw 없이 realtimeEnforced). 경계: enforce 독립 feature 모듈(순수 contract.ts + Next측 dispatch.ts), server는 배럴 우회 contract.ts만 상대 import/COPY. SOCKET_INTERNAL_SECRET은 AUTH_SECRET과 분리. 신규 vitest 17(71→88). 기계게이트 5/5 PASS(tsc 0/lint 0/vitest 88·88/next build 0/서버 esbuild 번들) + 듀얼검증(codex **3R 수렴**: r1 FAIL P1 grace-window → r2 FAIL P1×2 party잔존+leave경로 → r3 **PASS 0 issues** · evaluator WARNING 9.62, P3×3 전부 defer) + `.pass`(fingerprint `aa917d91`). 설계 codex consult 1R + 검증 codex 3R. 핵심 패턴: **별 호스트(Vercel↔OCC) 크로스프로세스 추방 = HMAC 서명 내부 webhook + DB postcondition 재확인**(시크릿 유출 방어). **즉시 추방의 권한 회수는 disconnect만으로 불충분 — grace 동안 socket.data 캐시가 살아있어 동기 detach 필요**(모든 핸들러가 socket.data.spaceId 가드). **운영**: OCI `.env`에 `SOCKET_INTERNAL_SECRET`(AUTH_SECRET과 다른 값) + Vercel에 `SOCKET_INTERNAL_URL`(공개 socket 도메인)·`SOCKET_INTERNAL_SECRET` 설정 필요(미설정 시 DB만 반영). **develop 머지 완료 (PR#8, merge `a3de864`, impl `e8e714c`)**.
- **WI-004-fix**: `DELETE /api/assets/[id]` path traversal(CWE-22, P2) 차단. DB `filePath`/`thumbnailPath`를 검증 없이 unlink하던 것을 `resolveGeneratedAssetPath()`로 `public/assets/generated/` 경계 격리(백슬래시/null byte 거부 + POSIX URL 정규화 + prefix 검사 + `path.relative` 이중 검사). 경계 밖이면 unlink 스킵+warn, **DB row 삭제는 진행**(오염 row 정리). 헬퍼 `features/assets/internal/safe-path.ts`+배럴, route는 배럴 import(서버 라우트라 sharp/React-pull 무관). vitest 18케이스(53→71). 컨테인먼트 루트는 codex consult로 `public/`→`public/assets/generated/` 타이트화(전 write 경로 generated 귀결 실증→무회귀). 기계게이트 4/4 PASS + 듀얼검증(codex **PASS 0 issues**·evaluator WARNING 9.62, evaluator 16벡터 PoC 독립실행 전부 BLOCK·회귀0 실증, P3×3 전부 defer) + `.pass`(fingerprint `9fae5a70`). 설계 codex consult 1R + 검증 codex 1R PASS. write-side 중앙화는 WI-012 후속. **develop 머지 완료 (PR#7, merge `67531b4`, impl `bc334a7`)**.
- **WI-003-refactor**: 타 모듈 `internal/*` 직접 import 위반 정리(경계 캡슐화). 인앱 cross-module internal import **7건**을 배럴 routing으로 해소 — editor↔game(game 배럴 `TILE_INDEX`/`extractDefaultMapData`/`TilemapResult`, editor 배럴 `EditorSystem`; MainScene `await import` lazy 로드라 정적 순환 없음), socket→chat(chat 배럴 `MOVE_THROTTLE_MS`/`RECONNECTION_*`), stores/editor-store→editor 배럴. **재발 방지 이중 방어**: ESLint `no-restricted-imports`(`@/**/internal[/**]` alias 정밀·오탐0) + `src/__tests__/module-boundaries.test.ts`(import 경로 실해석으로 alias+상대경로 cross-module internal 정밀 차단, vitest 게이트 강제). 서버(별도 esbuild 번들 — 배럴 import 시 React 끌려와 빌드깨짐)·scripts dev툴의 순수 계약/internal import는 범위 밖 → **WI-012-refactor**(protocol 계약 모듈 분리)로 등록. 기계게이트 **5/5** PASS(tsc/lint/vitest 53·53/build/서버 esbuild 번들) + 듀얼검증(codex **4R 최종 PASS**·evaluator WARNING 9.5, P3×4 전부 defer) + `.pass`(fingerprint fc768c9d). 설계 codex consult 1R + 검증 codex 4R 수렴. 스펙: `.claude/specs/architecture/2026-06-22-module-boundary-encapsulation.md`. **develop 머지 완료 (PR#6, merge `edd2918`, feat `58c9025`)**.
- **WI-008-fix**(직전, 2026-06-21): WI-007 P3×2 해소. **PR#5 머지 완료**(`1de51a5`). (이전 HANDOFF가 '머지만 남음'으로 stale했음.)

## main↔develop 정합 + 배포 상태 (2026-06-21)
- develop = WI-001 + WI-002 + WI-007 + **WI-009**(PR#4 `08fc2b9`) + 원장. develop ⊇ main.
- main `2a6e2ed`(PR#3, WI-007) + `38459d5`(빈 재배포 트리거). 라이브 프로덕션 = `38459d5` = **구버전 main + WI-007만**. **WI-001/002/009는 main 미반영**(승격 대상, 라이브에도 없음).
- 현재 체크아웃: `develop` (origin/develop 동기화 `08fc2b9`).

## ⚠️ Vercel 배포 함정 (재발 주의)
- Vercel 프로덕션 = `main` 브랜치 배포. 커밋 작성자가 Vercel 팀 인가 계정이 아니면 프로덕션 배포가 **실패**(`team-members-and-roles`). GitHub Actions 빌드는 성공이어도 별개.
- 이번 해결: 깃 작성자를 `flowcoder25 <flowcoder25@gmail.com>`(repo-scoped)로 변경 후 빈 커밋 푸시 → 배포 success.
- **이후 main 푸시도 인가 작성자 유지 필요.** 되돌리려면 `git config user.email <원래값>`. 근본 해결 = Vercel 팀에 커밋 작성자 추가.

## Done (이번 세션, 2026-06-21)
- **WI-009-feat**: 슈퍼어드민 전역 스페이스 뷰. `GET /api/spaces` filter 분기 명시화(owned/joined/all|null) + 슈퍼어드민 "전체"=모든 ACTIVE(`scope={}`, 일반 사용자 동작 불변) + 미허용 filter 400(`code:INVALID_FILTER`) + 목록 응답 `inviteCode` 제거(목록 UI 미소비 죽은 필드·전역 노출 시 전체 초대코드 누출 차단). `SpaceCard`/`SpaceListView` `isSuperAdmin` prop → myRole 무관 '관리' 노출(서버 세션 권위, 클릭 후 `requireSpaceAdmin` 재검증). 설계 codex consult 1R 반영. 기계게이트 4/4 PASS + 듀얼검증(codex PASS·evaluator 9.8, P3×2 defer→WI-010-perf/WI-011-test) + `.pass`. **develop 머지 (PR#4, merge `08fc2b9`, feat `fb484d6`)**.
- **WI-007-feat**: 스페이스 생성 슈퍼어드민 전용 제한 + `scripts/set-super-admin.mjs` 부트스트랩. 진입점 전수 게이팅(navbar 데스크톱/모바일 + my-spaces toolbar/empty-state) + POST 403 + /spaces/new redirect. 듀얼검증 codex PASS·evaluator 9.85(P3×2 defer→WI-008). main `2a6e2ed`(PR#3) → back-sync develop → 라이브 배포 `38459d5`. 스펙: `.claude/specs/auth/2026-06-21-superadmin-space-creation.md`.
- **kryou2922@gmail.com 슈퍼어드민 지정**(DB). 현재 슈퍼어드민: `admin@flowspace.dev`, `kryou2922@gmail.com`. (JWT라 변경 후 재로그인 필요)
- qa-agent/doc-agent 실행(정책 게이트). 스펙 문서 생성.

## Open Issues (Queue, 우선순위순)
- **WI-008-fix** (READY, 다음 우선): WI-007 P3×2 — `set-super-admin.mjs` 회수 인자 화이트리스트화, `POST /api/spaces` 403 응답 `code` 필드 일관화.
- **WI-003-refactor** (READY): 타 모듈 `internal/*` 직접 import 위반 정리(editor↔game, server→socket/chat).
- **WI-004-fix** (READY): `api/assets/[id]` DELETE 경로 격리(`../` 차단).
- **WI-005-fix** (READY): 접속 중 소켓 ban/kick 실시간 추방(크로스프로세스 설계).
- **WI-006-fix** (READY): `useScreenRecorder.onerror` pending resolve 미종결(P3 무해).
- **WI-010-perf** (READY, WI-009 defer): 슈퍼어드민 전역 스페이스 목록 페이지네이션(`GET /api/spaces` scope={} take/cursor/상한). 기존도 무페이지네이션 — 회귀 아님.
- **WI-011-test** (READY, WI-009 defer): `GET /api/spaces` filter 분기 회귀 테스트 + API 라우트 테스트 하니스(auth/prisma mock) 도입.

## Verification (WI-009-feat = develop 머지 완료)
| Gate | Result | Evidence |
|---|---|---|
| tsc / lint / vitest(52/52) / build | PASS | 세션 실측(feature 트리). CI는 develop PR 미트리거(main 전용) → 로컬 게이트 권위 |
| codex / evaluator | PASS / WARNING 9.8 (P3×2 defer→WI-010/011) | `.flowset/eval-results/WI-009-feat.*` |
| .pass | 생성됨 (fingerprint `2dedbb42`) | commit `fb484d6` |
| merge | DONE | PR#4 → develop `08fc2b9` |

## 직전 완료 (참고)
- WI-007-feat(스페이스 생성 슈퍼어드민 제한, PR#3 → main `2a6e2ed` + 라이브 `38459d5`) / WI-001-fix(보안 8건, PR#1) / WI-002-fix(Phaser 누수, PR#2). 상세는 `fix_plan.md` Done + `.flowset/eval-results/*`.
